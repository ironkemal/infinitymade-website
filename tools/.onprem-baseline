# On-prem tutarlılık kapısı — taban değerleri (tools/check-onprem.sh)
#
# Kural: bu sayılar ARTAMAZ. Azalırsa kapı tabanı kendisi sıkıştırır.
# Her satırın niye burada olduğu: onprem/REGISTER.md §8
#
# Ölçüm: 2026-09-04, index üzerinden (git grep --cached), eşleşen SATIR sayısı.
# Yükseltmek bilinçli bir karardır — gerekçesi sicile yazılır.

# Backend adresi koda gömülü. 11.09.2026 akşamı 25→7 indi (O-01, /api/config'e
# taşındı). 0 artık hedef DEĞİL: 7'nin dökümü —
#   2 bilinçli fallback kodu: api/config.js:9 · supabase-config.js:18
#     (host BURADA sabit yazılı olmak ZORUNDA — kutuya hiç gömülmez, yalnız
#     /api/config hiç dönmezse SaaS'ın eski davranışını taklit eder)
#   2 yorum satırı: supabase-config.js:13 (açıklama) · module/beleg-druck.js:11 (tarihi)
#   3 ayrı özellik, bu maddenin kapsamı DIŞINDA: server.js (O-02) ·
#     dashboard.js B2B webhook (O-09) · index.html pazarlama chatbot (O-04)
# Artış = red.
n8n_host=7

# app.praxura.de, yalnız uygulama yüzeyi (pazarlama sayfaları hariç — Faz 2.0).
app_host=19

# Vercel serverless limiti. 12 = TAVAN, artış deploy'u patlatır.
vercel_fn=12

# Üçüncü-parti <script src>. Bugün yalnız Sentry loader. Faz 2.6 koşullu yapacak.
ext_script=11

# N8N_ env referansı. Hedef 0 (Faz 1.2 kabul kriteri).
n8n_env=3

# Sabit Supabase proje adresi. 1 = api-backend/test_schema.js:5 (env fallback'i).
# Sicil bunu 0 sanıyordu; 04.09. ölçümünde çıktı — test dosyası, düşük risk, kayıtlı.
supabase_co=1

# CDN host'ları. 0 ve 0 kalmalı (Konsey 2026-08-13 S3, yerelleştirme geri alınamaz).
cdn_host=0

# Kutu paketindeki image satırı (onprem/docker-compose.yml).
# 7 = db · auth · rest · realtime · storage · kong + kendi api'miz.
# Upstream 11 fremd konteyner sunuyor; 5'i bilinçli olarak dışarıda
# (studio · meta · imgproxy · supavisor · functions — gerekçeler compose başlığında).
# Artış reddedilir. Kapı NOTICE.md'ye BAKMAZ — lisans satırını eklemek
# insanın işi (O-42), makinenin değil. Taban bilinçli olarak yükseltilirse
# gerekçesi sicile yazılır.
onprem_image=7

# Koda gömülü gönderen mail adresi (api-backend/). Hedef 0.
# 6 = server.js:3962 :3983 :4155 :4193 :4297 :4344
# Ölçüm 11.09.2026: praxura.de SPF `-all` + DMARC `p=quarantine` → kutudan
# çıkan bu mailler alıcıda spam'e düşer, gönderimde hata görünmez (O-51).
absender_fest=6
