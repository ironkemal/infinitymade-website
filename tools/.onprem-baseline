# On-prem tutarlılık kapısı — taban değerleri (tools/check-onprem.sh)
#
# Kural: bu sayılar ARTAMAZ. Azalırsa kapı tabanı kendisi sıkıştırır.
# Her satırın niye burada olduğu: onprem/REGISTER.md §8
#
# Ölçüm: 2026-09-04, index üzerinden (git grep --cached), eşleşen SATIR sayısı.
# Yükseltmek bilinçli bir karardır — gerekçesi sicile yazılır.

# Backend adresi koda gömülü. 11.09.2026 akşamı 25→7 indi (O-01, /api/config'e
# taşındı). 0 artık hedef DEĞİL: 7'nin dökümü —
#   2 bilinçli fallback kodu: api/config.js:9 · supabase-config.js:18
#     (host BURADA sabit yazılı olmak ZORUNDA — kutuya hiç gömülmez, yalnız
#     /api/config hiç dönmezse SaaS'ın eski davranışını taklit eder)
#   2 yorum satırı: supabase-config.js:13 (açıklama) · module/beleg-druck.js:11 (tarihi)
#   3 ayrı özellik, bu maddenin kapsamı DIŞINDA: server.js (O-02) ·
#     dashboard.js B2B webhook (O-09) · index.html pazarlama chatbot (O-04)
# Artış = red.
#
# ⚠️ 12.09.2026 — 7→8, BİLİNÇLİ (O-02 kısmen kapandı). Artan tek satır YORUM:
# api-backend/ai/tasks/series-scheduler.js'in başlık yorumu, KALDIRILAN
# `N8N_AI_SERIES_URL` fallback'ini ("...HARDCODED n8n.infinitymade.de-Adresse...")
# tarihi gerekçe olarak anlatıyor — canlı kod satırı DEĞİL, gerçek dış çağrı
# SIFIRA indi (aiPayload artık Azure'a gidiyor, n8n'e hiç gitmiyor). Sayaç
# yorum/kod ayrımı yapamıyor, bu yüzden kapı SKIP_ONPREM_GATE=1 ile geçildi.
n8n_host=8

# app.praxura.de, yalnız uygulama yüzeyi (pazarlama sayfaları hariç — Faz 2.0).
app_host=15

# Vercel serverless limiti. 12 = TAVAN, artış deploy'u patlatır.
vercel_fn=12

# Üçüncü-parti <script src>. Bugün yalnız Sentry loader. Faz 2.6 koşullu yapacak.
ext_script=11

# N8N_ env referansı. Hedef 0 (Faz 1.2 kabul kriteri).
#
# ⚠️ 12.09.2026 — 3→5, BİLİNÇLİ (O-02 kısmen kapandı). `N8N_AI_SERIES_URL`'in
# gerçek kod kullanımı (process.env okuması + hardcoded fallback) TAMAMEN
# kaldırıldı — server.js artık api-backend/ai/tasks/series-scheduler.js
# üzerinden doğrudan Azure OpenAI'a gidiyor. Artan 2 satır, ikisi de bu
# kaldırmayı anlatan YORUM (server.js'in yeni açıklama bloğu + series-
# scheduler.js'in başlık yorumu, ikisi de eski env var adını tarihi bağlamda
# anıyor). İkinci n8n satırı (`N8N_WEBHOOK_URL`, booking bildirimi, PII
# taşımıyor) henüz taşınmadı — Faz 1.2'nin "grep N8N_ → 0" hedefi bu yüzden
# hâlâ açık, ama G1 riski taşıyan satır kapandı. SKIP_ONPREM_GATE=1 ile geçildi.
n8n_env=5

# Sabit Supabase proje adresi. 1 = api-backend/test_schema.js:5 (env fallback'i).
# Sicil bunu 0 sanıyordu; 04.09. ölçümünde çıktı — test dosyası, düşük risk, kayıtlı.
supabase_co=1

# CDN host'ları. 0 ve 0 kalmalı (Konsey 2026-08-13 S3, yerelleştirme geri alınamaz).
cdn_host=0

# Kutu paketindeki image satırı (onprem/docker-compose.yml).
# 8 = db · auth · rest · realtime · storage · kong · api · caddy (11.09.2026,
# Faz 2.1b — caddy eklendi, onprem/NOTICE.md'ye satırı aynı commit'te girdi).
# Upstream 11 fremd konteyner sunuyor; 5'i bilinçli olarak dışarıda
# (studio · meta · imgproxy · supavisor · functions — gerekçeler compose başlığında).
# Artış reddedilir. Kapı NOTICE.md'ye BAKMAZ — lisans satırını eklemek
# insanın işi (O-42), makinenin değil. Taban bilinçli olarak yükseltilirse
# gerekçesi sicile yazılır.
onprem_image=8

# vercel.json CSP'sindeki "https://…" token sayısı (O-52, 11.09.2026 ölçüldü).
# Kapı bu dosyaya ilk kez bakıyor; hedef sıfır DEĞİL — SaaS'ın kendi bulut
# adresleri orada meşru. Amaç sessiz büyümeyi yakalamak, listeyi boşaltmak
# değil. onprem/Caddyfile bu listeyi KOPYALAMAZ, kendi dar CSP'sini tutar.
csp_host=53

# Koda gömülü gönderen mail adresi (api-backend/). Hedef 1 (bilinçli fallback).
# 11.09.2026 gecesi O-51/O-66 kapatıldı: altı yer artık api-backend/lib/mail.js
# → getMailFrom()'dan geçiyor, kutuda install.sh'ın sorduğu SMTP_FROM kullanılır.
# 2 = getMailFrom()'un kendi fallback satırı (`'noreply@praxura.de'`, SaaS'ta
# SMTP_FROM hiç set edilmediği için davranış SaaS'ta AYNI kalıyor) + dosyanın
# başındaki açıklayıcı yorum satırı (eski durumu anlatıyor, kod değil).
# Artış reddedilir — yeni bir sabit adres eklenirse getMailFrom() atlanmış demektir.
absender_fest=2
