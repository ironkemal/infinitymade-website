# 2026-08-30

## Woran gearbeitet wurde

Kein Code. Dieser Eintrag sichert den offenen Stand — und behebt einen Verlust,
der zwischen den Sitzungen entstanden ist.

Der Arbeitsstand selbst ist unverändert seit dem 25.08.: Branch `kalender-ux`,
neun Commits, `main` unberührt, Arbeitsverzeichnis sauber, nichts hängt
ungepusht herum.

## Erledigt

**Die 21 Ops-Karten waren weg — jetzt liegen sie im Repo.**

Sie lagen unter `/tmp/ops-tasks-kalender.json`, weil `ops/.env.ops` fehlte und
sie deshalb nie ins Dashboard geschrieben werden konnten. `/tmp` ist inzwischen
geleert. Genau davor stand eine Warnung in `.plans/STAND.md` — sie hat den
Verlust nicht verhindert, weil eine Warnung kein Ort ist.

Neu: `ops/ingest/2026-08-30-kalender-feedback.json`, **22 Karten** (10 Eltern,
12 Kinder), geprüft auf gültige Kategorien und aufgelöste Elternbezüge.
Sie enthalten keine Patientendaten und keine Schlüssel, gehören also ins Repo.

Eine Karte ist beim Neuschreiben dazugekommen: die Bezeichnung
„Podologische Behandlung gross" hing vorher als Nebensatz in einer anderen
Karte, obwohl es eine eigene Rückfrage an die Praxis ist.

Sobald `ops/.env.ops` existiert:

```sh
node ops/tools/ingest.mjs --json ops/ingest/2026-08-30-kalender-feedback.json --dry
node ops/tools/ingest.mjs --json ops/ingest/2026-08-30-kalender-feedback.json
```

Das Skript überspringt Karten, deren Titel schon offen auf dem Board steht —
ein zweiter Lauf schadet also nicht.

## Was offen ist

### Braucht dich

1. **Browser-Testdurchgang.** Sechs gebaute Funktionsblöcke sind maschinell
   geprüft (`npm run probe`: 72 Prüfungen, 0 Fehlschläge), aber **nie mit
   echten Daten und angemeldetem Nutzer**. Ablauf steht in `.plans/STAND.md`
   und als Ops-Karte. Bitte mit einem **Testkonto**, nicht dem aus den
   QA-Skripten.
2. **Entscheidung Kompaktmodus.** Klein (Kalender aus der Regel nehmen) oder
   richtig (Zeilenhöhe als CSS-Variable, im JS auslesen). Der Fehler trifft die
   Tagesansicht heute schon.
3. **`ops/.env.ops`** — Service-Role-Key des Ops-Projekts
   (`farkaejociddtgqkusvm`), damit die 22 Karten aufs Board kommen.
4. **Supabase-Zugang**, damit die Migration laufen und der Schema-Dump
   aufgefrischt werden kann.

### Braucht Fachauskunft, nicht Code

**Verordnungen Muster 13** — der einzige ungebaute Block der Rückmeldung.
Technisch wäre er günstig (`heilmittel_items jsonb` existiert in beiden
Verordnungstabellen, die „+"-Mechanik gibt es in der Podologie-Maske). Offen:

- Wie viele Heilmittel darf Muster 13 je Verordnung tragen?
- Stimmen die Höchstmengen (6, bei Nagelspange 8)? **Steht in keinem Dokument
  im Repo** — der Heilmittelkatalog liegt nicht hier. Eine falsche Grenze im UI
  erzeugt Verordnungen, die die Kasse absetzt.
- Was heisst „Podologische Behandlung gross"? Laut `SPEC-RULES.md:72` ist 78020
  der Fall *Therapiezeit über 20 Minuten*, kein zweiter Name für die
  Komplexbehandlung. Der Code bildet das ab; unklar ist, was umbenannt werden
  soll.

### Eigener Deploy-Weg (Backend)

**Therapeuten-Statistik ist tot und würde Blocker mitzählen.**
`statistik.routes.js` Abfrage 8 selektiert `employee_id` — die Spalte gibt es in
`bookings` nicht (es ist `user_id`). Und sie filtert `is_internal` nicht. Beides
gehört in einen Fix, sonst repariert man die Statistik und verfälscht sie im
selben Moment.

### Aufräumen

- Migration `sql-melih/2026-08-25-kostentraeger-typ.sql`, danach Schema-Dump.
  Nichts ist kaputt, solange sie nicht läuft.
- 18 CSS-Klassen sind durch den Wegfall der Kachelwand verwaist.
- Die zehn neuen Module tragen deutsche Texte; das `T`-Wörterbuch liegt in
  `dashboard.js` und ist für Module nicht erreichbar. Grundsätzlicher Punkt.
- **`git config core.hooksPath .githooks`** ist ein Einmal-Schritt je Rechner.
  Auf diesem war er nicht gesetzt — bei Kemal vermutlich auch nicht, dann läuft
  die Größenschranke bei ihm weiterhin nicht.

### ⚠️ Unabhängig von allem: Sicherheit

Zugangsdaten im Klartext in elf Dateien, davon mindestens sieben von git
getrackt, in einem laut `CLAUDE.md` öffentlichen Repo. **Passwort ändern wirkt
sofort, alles andere ist Aufräumen.** Seit dem 22.08. unverändert offen.

## Was noch getan werden muss

1. Testdurchgang (siehe oben) — davon hängt der Merge nach `main` ab.
2. `ops/.env.ops`, dann die 22 Karten einspielen.
3. Die drei Fachfragen an `gkv-302` bzw. an die Praxis stellen; erst danach
   Block D bauen.

## Commits

Keine. Der Stand vom 25.08. gilt unverändert — Liste in `.plans/STAND.md`.
