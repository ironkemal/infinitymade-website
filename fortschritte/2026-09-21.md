# 2026-09-21

> Stand beim Schreiben: Diese Datei enthält **Ops #302**, **Ops #303** und **Ops #300** (IK-Suche).
> Am selben Tag lief parallel Ops #301 (`krankenkassen.ik_number`, siehe
> `konsey/tutanak/2026-09-21-krankenkassen-ik-nachtrag.md`) — wer dazu arbeitet, ergänzt sie hier,
> es wird keine zweite Tagesdatei angelegt.

## Woran gearbeitet wurde

Ops #302: Heilmittel 78020 „Komplexbehandlung" ist in der Suche nicht auffindbar (Beta-1, Meeting
18.09.2026). Schrittweise mit Freigabe nach jedem Schritt, mit `gkv-302`, `podoloji` und
`fonksiyon-ustasi`. Ausdrücklich **nicht** gebaut: „mehrere Heilmittel hinzufügen" für die
Podologie (regelwidrig, #303).

Die Wissensfassung mit Begründung, verworfenen Alternativen und Lernpunkten steht in
`wissensbank/sitzungen/2026-09-21_ops-302_komplex-suche-78020.md`.

Ops #303: Board-Karten #200 und #202 auf „nur Physio/Ergo (+Logo)" eingrenzen — Board-Pflege und
Regelklärung, **kein Code-Fix**. Schrittweise mit Freigabe nach jedem Schritt, mit `gkv-302`
(Regel gegenprüfen). Wissensfassung:
`wissensbank/sitzungen/2026-09-21_ops-303_heilmittel-aufteilung-nur-physio-ergo.md`.

Ops #300: Im Feld „Krankenkasse / Kostenträger" findet die Suche keine IK (Beta-1, Meeting 18.09.2026).
Schrittweise mit Freigabe nach jedem Schritt, mit `gkv-302`, `fonksiyon-ustasi` und einem Konsey
(`muhalif`, `deger-mi`, `fonksiyon-ustasi`, `gkv-302`, `podoloji`, `db-ustasi`). Wissensfassung:
`wissensbank/sitzungen/2026-09-21_ops-300_ik-suche-kassenfeld.md`.

## Erledigt

### Ursache geklärt: „Komplexbehandlung" ist nicht der Name von 78020
„Podologische Komplexbehandlung" ist das **verordnete Heilmittel c)** (HeilM-RL § 27a Abs. 4 Nr. 3,
Katalog DF/NF/QF). 78010 und 78020 sind die **Abrechnungsleistungen** dafür („Podologische
Behandlung (klein/groß)", Anlage 1a/Anlage 2): c) mit Therapiezeit ≤ 20 Min → 78010, > 20 Min →
78020; a)/b) immer 78010. Das Katalog-Label war also schon amtlich richtig; ihm das Wort
„Komplex" zu geben hätte eine falsche Regel gelehrt und mit dem nicht abrechenbaren 78003
(offizieller Name ebenfalls „Podologische Komplexbehandlung") kollidiert. Die Zeilenangabe im
Ticket (`verordnung-podo.js:59-61`) war falsch; der Text steht in `module/verordnung-regeln.js:73-76`.

### Suchanker statt Umbenennung (lokal umgesetzt)
`kat: 'Podologische Komplexbehandlung'` an 78010 **und** 78020 (beide Preisfenster) →
`heilmittel_katalog.kategorie`; die RPC durchsucht `kategorie` bereits, also keine RPC- oder
Schemaänderung. Label und `kuerzel` unverändert (`kuerzel` würde die HPNR im Dropdown verdrängen).
Dateien: `api-backend/billing/codes/podologie_positions.js`, `api-backend/sync_heilmittel_katalog.js`
(`kategorie: p.kat || null`), neue Seed-Migration `api-backend/db/migrations/0039_…`,
`api-backend/db/erwartete-zaehler.json` (`bis_version` `0039`).

### Test
`module/heilmittel-suche-komplex.test.js` (7 Tests) prüft die Seed-Ausgabe, die in die DB geht:
„Komplex" findet 78010 und 78020 in beiden Fenstern, Labels bleiben amtlich, Anker steht nur dort
und ist wörtlich `POD_KATALOG.c`, `kuerzel` leer, 78003/78006 fehlen. **Gegenversuch:** ohne Fix
schlagen 3 Tests fehl. `npm test`: 1007 / 335 / 25 grün. `npm run probe`: Exit 0, 274 ✓. Review-Agent: keine
Befunde.

### wissensbank
`wissensbank/SPEC-RULES.md`: neue Regel „Maßnahme ≠ Leistung"; `wissensbank/REGISTER.md`: Kette **Z-13**.
Alle Fundstellen gegen die Originale gelesen. Neuer Ordner `wissensbank/sitzungen/` für
Sitzungsnotizen (bewusst eingetragene Ausnahme in der README — keine Quellen); `wissensbank/` ist
als Obsidian-Vault vorbereitet (`.obsidian/` lokal, in `.gitignore`).

### Funktionskarte
`funktionen/INDEX.json` neu erzeugt (`--check` OK). Der Diff enthält auch Änderungen aus früheren
Commits (`calcAbrechnungsfallTotals`, `buildSLLAMessage`), nicht nur #302. `fonksiyon-ustasi` hat
„warum und wo" gemeldet.

### Ops #303 — Regel gegen die Originale geprüft
HeilM-RL § 12 Abs. 2 S. 1 (15.05.2025, iK 05.08.2025): Aufteilung auf bis zu 3 vorrangige Heilmittel
nur Physio und Ergo, und nur soweit der Katalog in der Diagnosegruppe mehrere vorrangige
Heilmittel vorsieht. **Satz 2 (Logopädie) ist eine eigene Regel** (max. 3 Behandlungszeiten bzw.
Einzel-/Gruppenbehandlungen), nicht „sinngemäß" Satz 1. Die Podologie steht in keinem der Sätze;
„beides zugleich" ist dort das eigene Heilmittel c). Ergänzende Heilmittel (§ 12 Abs. 3) kennt der
Katalog nur bei Physio (12 Stellen) und Ergo (6) — bei Logo und Podologie 0 (von mir nachgezählt).
Die KVN-Ausfüllhilfe Muster 13, die `SPEC-RULES.md` als Quelle nennt, liegt nicht im Archiv. Zitate
gegen die `.txt`-Quellen gelesen; was nur `gkv-302` berichtet hat und ungeprüft ist, steht in der
Sitzungsnotiz.

### Ops #303 — Befunde in den Karten
- „Komplexbehandlung (78020)" ist ungenau: 78020 = „Podologische Behandlung (groß)"; die HPNR mit
  dem Namen „Komplexbehandlung" ist 78003 (nicht abrechenbar). Dasselbe Muster wie bei #302.
- „am 14.09. beantwortet": 14.09. ist das Commit-Datum des Code-Kommentars (`5fbfd81`), die Antwort
  von `gkv-302` ist vom 18.09. (`9e66ded`).
- Der Kommentar steht in `module/verordnung-maske.js` um Zeile 525-533, nicht bei 482-490.
- #202 nannte `.pod-hm-row` als Vorlage für einen Plus-Button: die Klasse existiert im Code nicht
  mehr (0 Treffer). **Es gibt keinen „+"-Button für weitere Heilmittel in der Podologie.**
- #202: „`heilmittel_items` in beiden Tabellen, keine Migration" ist veraltet (`verordnungen`
  gelöscht 04.09.); Physio/Ergo/Logo führen `heilmittel` als einzelnes Textfeld — für „mehrere
  Heilmittel" gibt es kein fertiges Datenmodell.

### Ops #303 — Board-Karten umgesetzt (Ops-Board, nicht im Repo)
#200: Kategorie Podoloji → Physiotherapie, Titel und Notiz mit „nur Physio/Ergo (+Logo)", Regel und
Quellen. #202: Titel ergänzt, alte Frage an gkv-302 als BEANTWORTET (14.09./18.09.) markiert,
veraltete Technik-Angaben korrigiert. #303: auf erledigt, Abschlusstext oben in der Notiz. Alte
Notizen jeweils unverändert am Ende. Vor dem Schreiben Backup gezogen, danach Titel/Notiz/Kategorie
gelesen und Priorität, Zuständiger, Elternkarte, `done` gegen das Backup verglichen (unverändert).

### Ops #303 — Code-Kommentare korrigiert
`module/verordnung-maske.js` und `module/verordnung-podo.js`: „S. 2 sinngemäß für Logopädie" →
Satz 2 ist eine eigene Regel. Nur Kommentare, gleiche Zeilenzahl, kein Verhalten geändert
(`node --check` ok, sonst nichts ausgeführt). Commit `3c0f04e`.

### Ops #300 — Ursache und Kurswechsel
`sucheKassen()` (`module/krankenkasse-suche.js`) durchsucht nur `name`/`kurz`, nie `ik` (gemessen:
`"105830016"` → `[]`). Der naheliegende Fix wäre trotzdem falsch: Auf Muster 13 steht die **Karten-IK**,
und die steht nur in `kostentraeger` (Verweis `abrechnender_kt_ik`), nicht in `krankenkassen.ik_number`;
`rzPatKasseIk` speichert `kostentraeger_ik` (selbst gelesen). Am Seed geprüft: alle 1043 `kostentraeger`-Zeilen
sind `payer_type='gkv'`, auch Rechenzentren — nur die VKG-Bedingung trennt sie. Konsey-Ergebnis: View
`kostentraeger_auswahl` + Präfixsuche. Eine Konsey-Prämisse („`ik_number` 16/94, 4 falsch", Stand REGISTER
06.09.) war durch #301 überholt (live 76/94); die dafür geplante „Lieferung 1" (Autofill stilllegen) wurde gestrichen.
Entscheidungen von Melih: Suche ab **3** Ziffern, keine Namenssuche bei IK-Eingabe, Freitext bleibt,
gefülltes Feld nicht überschreiben und Abweichung sichtbar machen.

### Ops #300 — Gebaut und am Feld angeschlossen
`module/krankenkasse-suche.js`: `IK_MIN_ZIFFERN = 3`, `ikAusEingabe()`, `aufgeloesteIk()`,
`sucheKostentraeger()` (Limit 300: der größte 3-Ziffern-Präfix „108" hat im Seed 189 Treffer). Danach der
Anschluss: `sucheKassenfeld()` (Weiche: reine Ziffern ≥ 3 → View `kostentraeger_auswahl`, serverseitig pro
Eingabe nach Präfix; sonst Namenssuche), `ikAnzeige()`, `hinweisZeile()` — unter dem IK-Feld bleibt „Karte X →
rechnet ab bei Y" stehen (de/en/tr, Sprache aus `<html lang>`), ein schon gefülltes Feld wird nicht überschrieben
und die Abweichung sichtbar. Fehlt die View, kommt eine leere Liste und eine Warnung; `sucheKassenfeld()` wirft
nie. Tests zuerst (rot, richtiger Grund), dann grün: 32 in `module/krankenkasse-suche.test.js`; `npm test`
1030 / 344 / 25; `npm run probe`: 90/90 Module laden im Browser, 274 ✓, 0 ✗. Review: Agent (3 Anmerkungen, zwei
umgesetzt), zweite Prüfung des SQL durch `db-ustasi` (🔴 `service_role` im `REVOKE`, behoben). Dateien:
`module/krankenkasse-suche.js` + Test, `dashboard.js` (nur Cache-Buster, Zeilenzahl gleich), Funktionskarte.

### Ops #300 — Migration 0040 (View), nicht angewandt
`api-backend/db/migrations/0040_kostentraeger_auswahl_view.sql`: `security_invoker`, `REVOKE ALL` inkl.
`service_role`, `GRANT SELECT` nur `authenticated`, kein Index. Zähler-neutral **nachgerechnet, nicht gemessen**
(`erwartete-zaehler.json`, `bis_version` `0040`, `_hinweis_0039`). Filter offline am Box-Seed nachgerechnet:
1043 → **893** Zeilen, keine bekannten Rechenzentren drin (die frühere Zahl „~961" war die Zahl der
Annahmestellen-IKs). Rund ein Dutzend ausgeschlossener Zeilen trägt einen schlichten Kassennamen (AOK NORDWEST,
DAK ×2, TK …) — dort kein Treffer, keine falschen Daten. Dazu: `db/REGISTER.md`-Eintrag, `migrations/README.md`
Kural 6 („abgeleitete Zählerneutralität"). **Das SQL wurde nirgends ausgeführt** (kein Postgres lokal).

## Offen / blockiert

- **Committet (lokal auf `main`), noch nicht gepusht, `0039` nicht live.** Solange gilt „Komplex findet

## Offen / blockiert

- **Committet, `0039` nicht live.** Solange gilt „Komplex findet
  78020" nur im Test, nicht in der echten Suche. `sync_heilmittel_katalog.js --check` wird bis dahin
  rot sein (Code steht vor der DB) — erwartet; vom Agenten berichtet, nicht selbst gelaufen (braucht
  DB-Zugang).
- Onprem-Gates (O-79 Seed, O-90 Zähler): der Pre-commit-Hook hat den Commit **nicht beanstandet**
  (Gate-Ausgabe selbst nicht separat gesehen).
- **Abrechnungslücke (gkv-302, per Repo-grep bestätigt):** Die > 20-Min-Regel wird bei c) nicht
  geprüft — `module/podologie-abrechnung.js:907-913` sperrt 78020 nur bei a)/b), die Therapiezeit
  ist nirgends als Wert erfasst. ~15,4 € (2025) / ~15,8 € (2026) je Sitzung möglich, in Prüfstufe 4
  rückforderbar. Nicht Teil von #302, eigene Karte.
- Suchlücken „Hornhautabtragung"/„Nagelbearbeitung" und „gross" ↔ „groß" (kein `unaccent`, betrifft
  auch `search_diagnosen`); `pruefeHpnrWahl()` warnt nicht bei 78020 ohne Leitsymptomatik; Sackgasse
  ohne Diagnosegruppe. Nichts davon geändert.
- Nicht geprüft: die Live-DB, `search_diagnosen` (ß/ss), Physio/Ergo/Logo.
- Im Working Tree liegen fremde Änderungen (Kassenfeld/IK-Suche, `konsey/`); sie gehören nicht in
  den Commit für #302.

- **Ops #303 — `SPEC-RULES.md`** (Abschnitt „Muster 13 … Heilmittel-Limit", 18.09.): Logopädie steht
  beim ergänzenden Heilmittel im Geltungsbereich (falsch, der Katalog kennt keins), die KVN-Quelle
  fehlt im Archiv, die Bedingung aus Satz 1 („soweit der Katalog mehrere vorrangige Heilmittel
  vorsieht") fehlt. Nicht korrigiert — die Datei wurde zur selben Zeit für #302 bearbeitet (seit
  `8df4143` committet); die Korrektur steht aus.
- **Ops #303 — Board:** #302 trägt noch den Fix-Text „Alias für 78020"; #209 hängt noch unter #200,
  gehört inhaltlich zu #302. Nicht angefasst. Der Abschlusstext von #303 nennt den #302-Alias noch
  als offen; #302 wurde inzwischen anders gelöst (Suchanker, `8df4143`).
- **Ops #303 — #94 (erledigt):** Zuordnung „a → 78020, c → 78010" in der Karte passt nicht zur Regel;
  **Code nicht geprüft.**
- **Ops #303 — ungeprüft:** § 63 SGB V Modellvorhaben als Ausnahme; Rahmenvertrag § 125a (nicht im
  Archiv); Blanko (§ 13a), § 12 Abs. 5/7 und zwei G-BA-Vorgänge nur von `gkv-302` berichtet; die
  Meeting-Dokumente (Stelle 02:05:03) nicht durchsucht.

- **Ops #300 — Race auf dem Git-Index:** Mein gestagter Stand (`module/krankenkasse-suche.js`, Test,
  Funktionskarte) wurde vom `git commit` der #301-Session mitgenommen und steht in **`fa86f5b`**
  („Ops #301") — Historie nicht umgeschrieben. `2c820b1` zieht die Karte nach. Künftig mit
  `git commit -- <Pfade>` arbeiten, solange andere Sessions im Arbeitsbaum committen.
- **Ops #300 — in der App noch wirkungslos:** Frontend und Migration sind committet, aber die View ist
  **nicht live angewandt** (Supabase-MCP nicht autorisiert). Bis dahin sieht der Podologe weiter eine leere
  Liste bei IK-Eingabe (eine Warnung in der Konsole, Namenssuche unberührt). Nichts gepusht.
- **Ops #300 — ungeprüft:** Live-Zustand der DB (`krankenkassen` nur über die öffentliche Route gelesen); das
  **SQL selbst** (nie ausgeführt); ob die Live-Zeilenzahl der View von den 893 am Seed abweicht (Ersatzkassen: Seed
  Q4, live Q2); das vermutete PostgREST-Zeilenlimit (1000 < 1043) — für den neuen Weg irrelevant, für
  `ladeKostentraegerNamen()` ungeklärt; das Verhalten im echten Browser mit Login. Die Browser-Probe hat nur
  gezeigt, dass das Modul lädt, nicht den Ablauf am Feld.
- **Ops #300 — Konsey:** Tutanak `konsey/tutanak/2026-09-21-ik-suche-kassenfeld.md` (`8f6db9b`) mit Nachtrag,
  Eintrag in `konsey/KARARLAR.md`. Die Runde lief zunächst in ein Sitzungslimit (429) und wurde neu gestartet.
  Die uncommittete #301-Zeile in `KARARLAR.md` und ihr Tutanak gehören der anderen Session, nicht mitcommittet.


- **Migrationsnummer:** Die Migration hieß zuerst `0038_seed_…`; Kemal hatte auf `origin/main` parallel
  `0038_empfaenger_zertifikate` veröffentlicht (gleiche Nummer). Deshalb auf **0039** umnummeriert
  (noch nirgends angewandt, Kural 2 nicht berührt). Die #300-Sitzung hat lokal eine uncommittete
  `0039_kostentraeger_auswahl_view` — sie muss auf 0040 wechseln, wenn sie nach dieser hier landet.
- **Live-Anwendung blockiert:** Das Supabase-MCP hatte in dieser Sitzung keinen gültigen Token
  (`Unauthorized`, selbst geprüft). Lesende Gegenprobe von `db-ustasi` (öffentlicher Katalog): 94 Zeilen,
  Drift 0 in 1880 Zellen, „Komplex" liefert heute 0 Treffer, kein Trigger/View/Abrechnungs-Leser auf
  `kategorie`. Anwenden: MCP neu autorisieren oder den Inhalt der Migration im Supabase-SQL-Editor
  ausführen. Bis der Code auf `main` ist, darf niemand `sync_heilmittel_katalog.js` im Schreibmodus aus
  altem Code laufen lassen (setzt `kategorie` zurück auf NULL).
- Im Working Tree liegen fremde Änderungen (Kassenfeld/IK-Suche, `konsey/`); sie gehören nicht in
  den Commit für #302.

## Was noch getan werden muss

- **Melih:** Push auf `main` freigeben (Watchtower baut das Backend-Image in ~60 s neu; zur Laufzeit
  ändert sich nichts, `kat` wird erst vom Sync-Skript verwendet). Danach `0039` live anwenden (per MCP,
  vorher `db-ustasi` als Gegenprobe), live prüfen mit `search_heilmittel('Komplex','podologie')`, Datum
  in den Kopf der Migration eintragen (zulässig, solange noch keine Box sie ausgeführt hat — gleiche
  Begründung wie im Kopf von 0015; danach wäre es ein Verstoß gegen Kural 2), `sync … --check`
  laufen lassen.
- **Melih:** entscheiden, welche Folgekarten ins Ops-Dashboard kommen — A (Suchlücken, eine gemeinsame
  Karte), B (> 20 Min bei c, wichtigste), C (`pruefeHpnrWahl`), D (Sackgasse ohne DG; vorher Beta-1
  fragen, wie oft die Diagnosegruppe leer ist).
- **Obsidian:** `wissensbank/` einmal als Vault öffnen.

- **Melih:** entscheiden, ob `3c0f04e` (reine Kommentare) mit auf `main` gepusht wird.
- **`gkv-302`/`wissensbank`:** `SPEC-RULES.md`-Abschnitt zu Muster 13 korrigieren (siehe oben); klären,
  ob die KVN-Ausfüllhilfe Muster 13 archiviert werden soll, sonst die Quelle durch
  `gemeinsam/heilmittel-richtlinie/praxiswissen-heilmittel.txt:1040-1064` ersetzen.
- **Melih:** entscheiden, ob #302/#209/#94 im Board nachgezogen werden (bewusst keine neuen Karten
  angelegt).
- **Vor einem „mehrere Heilmittel"-Feature für Physio/Ergo:** Datenmodell klären (`db-ustasi`) und
  das Limit hart ins UI setzen — die DTA-Schicht begrenzt nicht (`EHE` 1-n), ein Verstoß kommt erst
  in Prüfstufe 4 als Absetzung zurück.
- **Melih (Ops #300, blockiert die Wirkung):** Migration `0040` live anwenden (MCP-Token fehlt; SQL-Editor
  geht auch). **Vorher** die Abfragen aus dem Kopf der Datei: `active IS NOT TRUE` → 0 erwartet; Zeilenzahl der
  View am Seed ~893, live kann abweichen. **Nachher:** kein Treffer für „gkv informatik", „IQVIA", „Medent",
  „Rezeptprüfstelle", „AZE Emmendingen", „BITMARCK"; `SELECT * FROM kostentraeger_auswahl WHERE ik = '100167999'`
  → eine Zeile mit `abrechnender_kt_ik = 105830016`. **Reihenfolge:** View live, **dann** das Frontend pushen.
- **Ops #300 — nach dem Anwenden:** `db/SCHEMA.sql` (Abschnitt „VIEWS", Kopfdatum), `SCHEMA-RLS.sql`-Kopf
  und der Vermerk „SaaS: angewandt <Datum>" im Migrationskopf — **zweiter Commit**, weil der Dump ein Live-Abzug
  ist, mit `SKIP_MIGRATION_GATE=1` (keine neue Migration gestaged).
- **Ops #300 — von Hand im Browser prüfen** (Muster-13-Maske, Login nötig): `100167999` tippen → „DAK-Gesundheit",
  im IK-Feld `105830016`, darunter „Karte 100167999 → rechnet ab bei 105830016"; danach eine andere Kasse wählen
  (Abweichungs-Hinweis); Privatpatient (Freitext ohne Treffer); Sprache auf en/tr umstellen. Auf Wunsch macht das
  der `canli-test`-Agent.
- **`gkv-302` (offen, Ops #300):** Können die ~12 ausgeschlossenen IKs mit schlichtem Kassennamen (AOK NORDWEST,
  DAK-Gesundheit ×2, Techniker Krankenkasse …) Karten-IKs sein? Wenn ja, fehlen sie in der Auswahl.
- **Backlog (#300):** Karten-IK in `prescriptions.krankenkasse_ik` erfassen; `das_ik`-Rest; Zeilenlimit bei
  `ladeKostentraegerNamen()` (`dashboard.js`) prüfen.

## Commits

- `8df4143` feat(podologie): „Komplex" findet 78010 und 78020 in der Heilmittelsuche (Ops #302)
- `ae61dcb` chore(funktionen): Funktionskarte aufgefrischt (Ops #302-Sitzung)
- Nachtrag: docs-Commit, der diesen Abschnitt und den Stand in der Sitzungsnotiz nachzieht
- Ops #303: `3c0f04e` docs(verordnung) — zwei Code-Kommentare, Satz 2 HeilM-RL § 12 Abs. 2
- Ops #303: docs-Commit mit den Ergänzungen in dieser Datei, der Sitzungsnotiz und der Übersicht
- Ops #300: `fa86f5b` (enthält den ersten Teil meines Codes, siehe oben) · `2c820b1` chore(funktionen) ·
  `a2d8e68` feat(db) Migration 0040 · `4078c8c` feat(kassen) Anschluss am Feld · `8f6db9b` docs(konsey) ·
  docs-Commit mit diesem Abschnitt, der Sitzungsnotiz und der Übersicht
- Nicht gepusht. Fremde Änderungen im Working Tree (Kassenfeld/IK-Suche, `rezept-felder`) sind nicht
  Teil dieser Commits.

## Nachtrag (21.09.2026): Migrationen umnummeriert
Kemal hat `0038_empfaenger_zertifikate` gepusht. Unsere drei lokalen Migrationen heißen deshalb jetzt **0039** (Ops #302, Heilmittel-Katalog), **0040** (Ops #300, View `kostentraeger_auswahl`) und **0041** (Ops #301, `krankenkassen.ik_number`). Alle „0038/0039/0040"-Nennungen in dieser Datei sind auf die neuen Nummern verschoben. Details: `konsey/tutanak/2026-09-21-krankenkassen-ik-nachtrag.md`, Nachtrag 5.

## Commits

Siehe `git log --grep "Ops #302"` — die Hashes ändern sich, weil die Commits für den Push auf
`origin/main` aufgesetzt (cherry-pick, Migration umnummeriert) wurden.

**21.09.2026 ~23:08:** Alle lokalen Commits (#300, #301, #303 und die umnummerierten Migrationen) sind zusammen mit Kemals §302-Verschlüsselung und der veröffentlichten #302-Fassung als Merge `ff39026` auf `origin/main`. Migrationen 0039/0040/0041 liegen im Repo, sind auf Prod aber **nicht angewandt**. Details: `konsey/tutanak/2026-09-21-krankenkassen-ik-nachtrag.md`, Nachtrag 6.
