# 2026-08-11

## Woran gearbeitet wurde

Loop-Liste `Podoloji/loop-tasks-kassieren.md` (Kassieren · Zuzahlung · Rechnungen,
Meeting 08.08.2026), die vier restlichen Punkte: **2, 7, 8, 9**. Damit ist die Liste
vollständig — 10 von 10 abgehakt.

Die Commits tragen das Datum **10.08.2026**, die Arbeit lief über Mitternacht. Die
früheren Punkte derselben Liste (1, 3, 4, 5, 6, 10) stehen in `fortschritte/2026-08-10.md`.

---

## Erledigt

### Aufgabe 2 — Preise zentral und versioniert (Commit `1146f7a`)

**Das Problem:** Die gedruckte Patientenrechnung und die §302-Datei an die Kasse holten
ihre Beträge auf **getrennten Wegen**. Die Druckrouten fragten `heilmittel_tarif` gar
nicht ab, die §302-Route schon. Ein geänderter Tarif liess beide auseinanderlaufen — der
Patient bekam eine Rechnung über einen anderen Betrag, als die Kasse gemeldet bekam.

**Entscheidung Melih:** Katalog gewinnt. `heilmittel_tarif` bleibt liegen und wird nur
noch als optionaler Preis-Override für Physio gelesen. Die Zuzahlung kommt **immer** aus
dem Katalog, nie mehr aus dem Ja/Nein-Feld `zuzahlung_pflicht` — das verlor die exakten
veröffentlichten Beträge (Podologie 78010 = 3,52 €).

Geändert:
- `api-backend/billing/preise/resolver.js` (neu) — `resolvePreis()`, `findTarifForDate()`
- `api-backend/billing/preise/resolver.test.js` (neu, 18 Fälle)
- `api-backend/billing/codes/physio_positions.js` — `PHYSIO_PREISFENSTER`,
  `findPosition()` datumsfähig
- `api-backend/billing/api/abrechnung.routes.js` — alle sechs Preisstellen umgehängt,
  `findPriceForDate()` entfernt, `zuzahlung`/`zuzahlung_frei` im `/positions`-Payload
- `api-backend/billing/dta/builder.js` — zweite Zuzahlungsformel raus
- `dashboard.js` — beide Abrechnungs-Vorschauen

**Zwei echte Geldfehler mit behoben:**

1. In `dta/builder.js` stand `s.zuzahlungProPos || s.einzelbetrag * 0.10`. Bei einer
   Zuzahlung von **genau 0 €** — also einer zuzahlungsfreien Position — ist 0 falsy, die
   Rechnung kippte in den 10-%-Zweig und meldete der Kasse eine Zuzahlung, die es nicht
   gibt. Die Praxis bekam entsprechend weniger ausgezahlt.
2. `GET /billing/positions` lieferte das Feld `zuzahlung` gar nicht aus. Die
   Abrechnungs-Vorschau im Dashboard rechnete deshalb **immer** `brutto × 10 %` — auch
   für zuzahlungsfreie Positionen, wo die gedruckte Rechnung korrekt 0 € auswies.

**Externe Quelle vorher geprüft** (Regel aus dem Loop-Prompt): GKV-Anlage 2 Podologie neu
abgerufen. Übersichtsseite (Stand 20.07.2026) listet weiterhin dasselbe Dokument
`20250617_Podologie_Anlage_2.pdf`, neueste Änderungsvereinbarung weiterhin 16.06.2025.
PDF selbst geladen und ausgelesen: „i.d.F. vom 01.07.2025", genau zwei Preisfenster
(§2 ab 01.07.2025, §3 ab 01.07.2026), kein drittes. Zehn Stichproben stimmen exakt mit
`GKV-PODOLOGIE-PREISE-2025-2026.md` überein. **Unverändert — keine Anpassung nötig.**

### Aufgabe 7 — Ausfallrechnung-Entwurf (Commit `d395876`)

Die Aufgabe war als „Vorlage entwerfen" formuliert. Beim Lesen zeigte sich: **die
Ausfallrechnung existiert bereits fast vollständig** und ist fachlich richtig gerahmt —
Privatrechnung an den Patienten, Schadensersatz statt Leistungsentgelt, deshalb keine
Umsatzsteuer (§ 1 Abs. 1 Nr. 1 UStG), kein HPNR und kein Kostenträger auf dem Dokument.
Vorhanden waren: Tabelle `ausfallrechnungen`, Einstellungen auf `profiles.ausfall_*`,
vier Routen, Vorlage, 18 Tests, UI-Dialog, GoBD-Buchung.

Statt neu zu bauen: `Podoloji/AUSFALLRECHNUNG-ENTWURF.md` (neu) hält den Sollzustand fest
— inklusive Abgleich-Checkliste, falls Stefans Muster später doch noch kommt.

### Aufgabe 8 — die vier Lücken geschlossen (Commit `0224090`)

- **L1:** Die Absagefrist wurde **nur im Browser** geprüft. Wer den Aufruf direkt absetzte,
  konnte auch für eine rechtzeitige Absage abrechnen oder bei ausgeschalteter
  Ausfallgebühr. Genau die Bedingung, die die Forderung trägt, war serverseitig ungeprüft.
  Jetzt: `billing/ausfall/frist.js` (neu) lehnt mit 422 ab, bewusstes Übersteuern per
  `override: true` wird in `notes` protokolliert. `no_show` braucht keine Fristprüfung.
- **L2:** `leads.ausfallvereinbarung_am` (neu). Das Dokument nimmt im Text Bezug auf „die
  mit Ihnen getroffene Ausfallvereinbarung" — das System wusste nichts davon. Fehlt sie,
  warnt der Dialog sichtbar, **blockiert aber nicht** (Entscheidung Melih: viele Praxen
  führen die Vereinbarung auf Papier).
- **L3:** Zeitzonenfehler. `fmtDate()` rendert jetzt in `Europe/Berlin`. Der Server läuft
  in UTC — ein Termin um 00:30 Berliner Zeit stand mit dem **Vortagsdatum** auf der
  Rechnung, also auf einem Dokument, dessen Zweck es ist, dieses Datum zu behaupten.
- **L4:** Neun optionale Abfragen von `.single()` auf `.maybeSingle()` (CLAUDE.md).

### Aufgabe 9 — Mahnung für Ausfallrechnungen (Commit `44cf84b`)

Der bekannte Blocker stimmte: `mahnungen.prescription_id` ist `NOT NULL`, eine
Ausfallrechnung hat aber kein Rezept. Schemaumbau war freigegeben.

- Migration: `prescription_id` nullable, `ausfallrechnung_id` neu, CHECK
  `num_nonnulls(...) = 1` („genau eine Quelle")
- `mahnung.template.js`: neuer Parameter `forderungsart`. Bei Ausfall entfällt die
  **IK-Nummer** in der Fusszeile — das ist kein GKV-Dokument — und es steht nirgends mehr
  „Zuzahlung"
- `billing/mahnwesen/stufe.js` (neu): Mahnstufe wird gegen die Historie geprüft
- `dashboard.js`: beide Forderungsarten in einer Liste, unterschieden durch ein Badge

**Zwei Nebenfunde mit behoben:**

1. Stufe 3 — die „letzte Mahnung", die das Inkassobüro ankündigt — liess sich als
   **allererste** Mahnung verschicken, ohne dass der Patient je eine Zahlungserinnerung
   bekommen hatte. Eine niedrigere Stufe zu wiederholen bleibt erlaubt, nur das
   Überspringen nach oben wird verhindert.
2. `GET /mahnwesen/offene` lieferte die `id` der Mahnung nicht mit. Dadurch war
   `data-mw` im Frontend `undefined` und die Knöpfe **„Bezahlt" und „Abschreiben" liefen
   ins Leere** — vermutlich seit es sie gibt.

---

## Offen / blockiert

### 🔴 Die drei Migrationen sind NICHT gelaufen — und der Code ist trotzdem live

Melih hat versehentlich gepusht, bevor das SQL eingespielt war. Vercel deployt sofort,
Watchtower zieht das Backend in ~60 s. Der neue Code greift also auf Spalten zu, die es
in der Datenbank nicht gibt.

**Gegen `db/SCHEMA.sql` (erzeugt 10.08.2026, letzte Migration `20260810142703`)
nachgeprüft — alle drei fehlen tatsächlich:**

| Fehlt | Was dadurch kaputt ist |
|---|---|
| `belegliste.zahlart`, `prescriptions.zuzahlung_zahlart` (v32) | Kassieren schlägt fehl |
| `leads.ausfallvereinbarung_am` | Ausfallrechnung erstellen → 404/500 |
| `mahnungen.ausfallrechnung_id` | Mahnwesen-Bildschirm → 500 |

Abhilfe liegt bereit: **`SUPABASE-JETZT-AUSFUEHREN.sql`** (Commit `8acb226`) — fasst alle
drei in einer idempotenten Transaktion zusammen, gefahrlos wiederholbar, verändert keine
Bestandsdaten.

**Kein Datenschaden:** der Code ist mit Fehlern abgebrochen, es wurde nichts Falsches
geschrieben. Wollte in der Zwischenzeit ein Beta-Kunde kassieren, hat das für ihn sichtbar
nicht funktioniert; die Zuzahlung ist schlicht nicht gebucht und lässt sich normal
nachholen.

### 🔴 Schema-Dump nicht getazelt — Regel verletzt

CLAUDE.md verlangt seit 10.08.: *„Her şema değişikliğinden sonra döküm aynı commit'te
tazelenir."* Meine zwei Migrationen ändern das Schema, `db/SCHEMA.sql` und
`db/SCHEMA-RLS.sql` sind aber **nicht** aktualisiert.

Grund: In dieser Sitzung war **kein Supabase-MCP-Werkzeug verfügbar** (gesucht, nur
Notion- und Browser-Werkzeuge vorhanden). Ich konnte die Live-DB weder abfragen noch den
Dump neu erzeugen. Zweitens wäre eine Tazeleme jetzt ohnehin falsch — die Migrationen sind
noch gar nicht angewendet, ich hätte den kaputten Zwischenstand eingefroren.

**Reihenfolge, die eingehalten werden muss:** erst `SUPABASE-JETZT-AUSFUEHREN.sql` in
Supabase, **dann** Dump tazelen. Nicht umgekehrt.

### Rechtsfragen — bewusst nicht entschieden

Es gibt in diesem Projekt keinen `legal-de`- und keinen `gkv-302`-Fachagenten. Drei Punkte
sind echte Fach-/Rechtsfragen und wurden deshalb gemeldet statt geraten:

1. **10-€-Verordnungspauschale bei rein zuzahlungsfreien Rezepten** — offene Fachfrage aus
   `PREISE-ANALYSE.md`. Beide Wege sagen heute „ja" und sind wenigstens einig. Vor der
   ersten echten §302-Abrechnung bestätigen lassen.
2. **Mahngebühr / Verzugszinsen auf ein Ausfallhonorar** — nichts gebaut.
3. **Wirksamkeit der 24-Stunden-Absagefrist** — hängt am Wortlaut der Ausfallvereinbarung,
   nicht am Code. Die 24 h sind nur eine änderbare Vorgabe.

### Bewusst nicht angefasst

- **Physio-Preishöhen.** Mir lag nur das Podologie-Dokument vor. Die Werte in
  `physio_positions.js` sind unverändert.
- **`physio_positions.js` `gueltig_ab: 2026-01-01`** ist eine **übernommene, nie geprüfte
  Annahme** — sie stand schon im Dateikopf und `seed_tarifs.js` schreibt sie seit jeher so
  in die DB. Gegen Anlage 2 Physiotherapie ist sie nie abgeglichen worden.
- **Podologie 78210/78220/78230/78300/78400 im 2026er Fenster.** Sieht nach Fehler aus
  (`deprecated: true`, aber `gueltig_bis: '9999-12-31'`) — **ist aber richtig**: die
  Anlage 2 nennt für diese Positionen sehr wohl einen 01.07.2026-Preis, weil eine
  Verordnung von vor dem 01.10.2025 noch 2026 behandelt werden darf. Nicht „repariert".
- **`PATCH /mahnwesen/:id/status`** setzt weiterhin nur den Status der Mahnung, nicht den
  der Forderung. Absicht: eine bezahlte Ausfallrechnung muss über
  `PATCH /billing/ausfall/:id/status` laufen, nur dort entsteht der GoBD-Beleg.

### Nebenfund, gehört ins Ops-Dashboard (Teknik)

Für die Tabelle **`heilmittel_katalog`** gibt es **nirgends im Repo ein `CREATE TABLE`**.
Sie existiert nur in Supabase und in den Erwartungen von `sync_heilmittel_katalog.js`. Für
den geplanten On-Prem-Umzug ist das eine echte Lücke — auf einem frischen Server liesse
sie sich nicht anlegen. Nur gefunden, nicht behoben.

---

## Prüfstand

- **243 Tests grün, 0 rot** über alle 14 Testdateien unter `api-backend/billing/`.
  Neu dazu: `preise/resolver.test.js` (18) · `ausfall/frist.test.js` (16) ·
  `mahnwesen/stufe.test.js` (21) · zwei Zeitzonen-Fälle in `pdf/ausfallrechnung.test.js`.
- `node --check` auf allen geänderten Dateien ✅
- Vercel-Funktionen weiterhin **12/12** (`find api -name "*.js" -not -path "api/_lib/*"`) ✅
- G8 eingehalten: alles Neue liegt in `api-backend/`, keine neue Cloud-Kette ✅
- i18n de/en/tr für vier neue Schlüssel ✅ · Cache-Busting `?v=20260810c` ✅
- Migrationen gegen `db/SCHEMA.sql` gegengeprüft ✅ — `mahnungen.prescription_id` ist dort
  `NOT NULL` (mein `DROP NOT NULL` ist also nötig), `ausfallrechnungen` existiert (FK
  gültig), `leads` hat keine `ausfall*`-Spalte.

**Ungeprüft — ausdrücklich nicht getestet:**

- **Nichts davon lief je gegen die echte Datenbank oder im Browser.** Die Migrationen sind
  nicht angewendet, es gab keinen End-to-End-Durchlauf.
- Die Route-Änderungen in `ausfall.routes.js` und `mahnwesen.routes.js` sind **nur gelesen
  und syntaktisch geprüft**, nicht ausgeführt. Getestet sind die herausgezogenen reinen
  Funktionen (`frist.js`, `stufe.js`, `resolver.js`), nicht die Routen drumherum.
- Der Abgleich „Druckweg und §302-Weg liefern denselben Betrag" ist als Test formuliert,
  aber **gegen den Katalog, nicht gegen echte Rezeptdaten**.

---

## Was noch getan werden muss

**Melih, in dieser Reihenfolge:**

1. **`SUPABASE-JETZT-AUSFUEHREN.sql` im Supabase-SQL-Editor ausführen**
   (Projekt `njvuclullotbksskpwgk`, nicht das Ops-Projekt). Danach den auskommentierten
   Kontrollblock am Ende laufen lassen — alle fünf Zeilen müssen `true` zeigen.
   **Das ist dringend, es sind drei kaputte Funktionen in der Produktion.**
2. **Schema-Dump tazelen** — `db/SCHEMA.sql` + `db/SCHEMA-RLS.sql` neu erzeugen,
   Datum und „LETZTE MIGRATION" im Kopf nachziehen.
3. **`git push`** — offen sind noch `8acb226` (Sammelskript) und dieser Bericht.
4. Nach dem Deploy im Browser gegenprüfen: Kassieren mit Zahlart · Ausfallrechnung für
   eine kurzfristige Absage (Warnung bei fehlender Vereinbarung sichtbar?) · Ausfallrechnung
   für eine **rechtzeitige** Absage (muss jetzt gesperrt werden, mit Übersteuern-Knopf) ·
   Mahnwesen-Bildschirm zeigt beide Forderungsarten.
5. Die drei Rechtsfragen oben klären, bevor die erste echte §302-Abrechnung rausgeht.

---

## Commits

| Hash | Was |
|---|---|
| `1146f7a` | feat(billing): Preise und Zuzahlung zentral aufloesen |
| `d395876` | docs(ausfall): Entwurf und Sollzustand der Ausfallrechnung |
| `0224090` | feat(ausfall): Absagefrist serverseitig pruefen, Vereinbarung nachweisen |
| `44cf84b` | feat(mahnwesen): Ausfallrechnungen mahnen, Mahnstufe gegen Historie pruefen |
| `8acb226` | chore(db): Sammelskript fuer die drei offenen Migrationen |

21 Dateien, +1547 / −196 Zeilen (ohne das Sammelskript und diesen Bericht).

---
---

# 2026-08-11 — zweiter Auftrag des Tages

## Woran gearbeitet wurde

Loop-Liste `Podoloji/loop-tasks-teknik-hijyeni.md` (Ops-Dashboard → Teknik →
„Teknik borç ve site hijyeni", Meeting 08.08.2026), alle 7 Punkte. Es ging um
technische Schulden und Site-Hygiene, nicht um Fachlogik: verschwindende
Zuweisungen im Ops-Pano, tote WhatsApp-Werbung, Encoding, Google Fonts, 404-Seite,
noindex.

Ergebnis: **6 von 7 abgearbeitet, Punkt 3 bleibt offen** und braucht eine Rückfrage.
Punkt 1 ist bewusst als *Falle gestellt* geschlossen — die Ursache ist weiterhin unbekannt.

## Erledigt

### Punkt 1 — Falle für verschwindende Zuweisungen (Commit `c8ade0f`)

**Was:** Neue Tabelle `ops_todos_audit` mit zwei Triggern, die jede Änderung an
`assignee`, `parent_id` und `done` sowie jedes Löschen einer Aufgabe protokollieren —
mit Verursacher (`auth.uid()`) und Zeitstempel. Im Pano zeigt ⋮ → **Verlauf** die
letzten 30 Einträge einer Karte.

**Warum:** Zuweisungen verschwinden gelegentlich von selbst, und ohne Protokoll lässt
sich nach dem Vorfall nichts mehr rekonstruieren. Reparieren kann man erst, wenn man
weiß was passiert ist — also erst messen.

**Dateien:** `ops/schema-audit.sql` (neu), `ops/board.js`, `ops/SETUP.md`,
Cache-Version in `ops/*.js` + `ops/index.html` auf `?v=20260811a`.

**Nebenbefund beim Lesen des Codes:** Eine Karte unter ein Thema zu hängen wirkte
unterschiedlich, je nachdem wie man es tat. Per Drag & Drop übernahm die Karte die
Zuweisung des Themas (`ops/board.js:419`), per ⋮-Menü behielt sie ihre eigene. Der
Drag-Weg überschrieb also stillschweigend eine Zuweisung — genau das Muster, das
gemeldet wurde. Beide Wege verhalten sich jetzt gleich, und wenn dabei eine Zuweisung
wechselt, sagt das Board es per Hinweis. **Das ist kein Beweis, dass es die Ursache
war** — nur eine plausible und jetzt beseitigte Möglichkeit.

### Punkt 2 — tote WhatsApp-Werbung entfernt (Commit `0708abc`)

**Was:** In `demo-dashboard.html` den Schalter „WhatsApp Erinnerungen an Patienten"
(Einstellungen → Benachrichtigungen) gelöscht und im Tarif-Text „Rechnungen,
Kassenbuch, WhatsApp & Termine" das WhatsApp gestrichen.

**Warum:** WhatsApp/Twilio ist seit 2026-05-20 eingestellt, die Tabellen sind gedroppt.
`demo-dashboard.html` ist keine tote Datei, sondern die von `index.html:1154` verlinkte
öffentliche Demo — die Seite warb also weiter mit einer Funktion, die es nicht gibt.

**Dateien:** `demo-dashboard.html`.

Nicht angefasst: `dashboard.html:4969` („Via WhatsApp teilen" ist ein Teilen-Knopf,
kein Feature), Backend-Reste in `server.js` / `ai/router.js` / `api/admin/data.js`,
sowie `script.js` und `index-old.html` — Letztere gehören zur alten InfinityMade-Seite.

### Punkt 4 — UTF-8 geprüft und abgesichert (Commit `53b12a6`)

**Was:** Gemessen statt vermutet. Alle `*.html`/`*.js` im Root sind gültiges UTF-8
(`iconv`-Durchlauf, kein Treffer). Die Auslieferung stimmt ebenfalls: `curl -I` gegen
die Live-Domains liefert `application/javascript; charset=utf-8` für `dashboard.js`
und `text/html; charset=utf-8` für `dashboard.html` und `praxura.de/`. Einen Build-
oder Kopierschritt gibt es nicht, Vercel liefert statisch aus. **Es liegt also aktuell
kein Encoding-Fehler vor.**

Neu angelegt sind `.editorconfig` (UTF-8, LF) und `.gitattributes` (LF im Repo,
Binärdateien geschützt) — damit ein Editor die Umlaute künftig nicht mehr unbemerkt
als Latin-1 zurückschreiben kann. Heute ändert das nichts: es gibt keine einzige
CRLF-Datei im Repo.

40 Dateien haben ein BOM am Dateianfang (u. a. `login.html`, alle Blog-Seiten). Das ist
erlaubt und im Browser unschädlich; nach Absprache **bewusst gelassen**, sonst stünden
40 Dateien ohne Nutzen im Verlauf.

**Dateien:** `.editorconfig`, `.gitattributes` (beide neu).

### Punkt 5 — Google Fonts selbst gehostet (Commit `299f22c`)

**Was:** `demo-booking.html` lud Fraunces, Plus Jakarta Sans und JetBrains Mono von
`fonts.googleapis.com` / `fonts.gstatic.com`. Die drei Zeilen sind durch
`<link rel="stylesheet" href="fonts/system-fonts.css">` ersetzt.

**Warum:** DSGVO — beim Aufruf ging die IP des Besuchers ohne Einwilligung an Google.

Es musste nichts heruntergeladen werden: alle drei Schriften liegen bereits selbst
gehostet in `fonts/` und werden von `index.html` und rund zehn weiteren Seiten so
eingebunden. `demo-booking.html` war die einzige Produktseite, die noch das CDN nutzte.

**Dateien:** `demo-booking.html`.

### Punkt 6 — 404-Seite angelegt (Commit `c42b205`)

**Was:** Neue `404.html` im Repo-Root, im Praxura-Look: `fonts/system-fonts.css` +
`assets/system.css`, Knöpfe `.btn-primary-arrow` / `.btn-secondary` aus dem
Designsystem, deutscher Text, `noindex, nofollow`, Links zu Startseite, Kontakt,
Anmelden, Blog, Hilfe und Impressum (alle Ziele auf Existenz geprüft).

Im Stylesheet der Seite steht **kein einziger Farbwert** — alles kommt aus den
CSS-Variablen, damit die Seite bei einer Farbänderung automatisch mitzieht.
`vercel.json` schreibt nichts Abweichendes vor, eine `404.html` im Root greift bei
Vercel automatisch für unbekannte Pfade.

**Dateien:** `404.html` (neu).

### Punkt 7 — noindex bestätigt (Commit `0d21a3a`)

`login.html`, `onboarding.html` und `employee-signup.html` haben jeweils in Zeile 6
und 7 `robots` und `googlebot` auf `noindex, nofollow`. Nachgeprüft, kein Code nötig.
Die neue `404.html` hat beides ebenfalls bekommen.

## Offen / blockiert

### Punkt 3 — „Google Kalender"-Label: keine Verwechslung auffindbar

Gemeldet war, in den Einstellungen stehe „Google Kalender", gemeint sei aber die
Gmail-/Mail-Verbindung. Geprüft wurde:

- `api-backend/server.js:309–386` — `/calendar/google-auth`, `/gmail/connect` und der
  gemeinsame Callback sind über `flowType` im signierten State sauber getrennt. Gmail
  schreibt `profiles.b2b_from_email` + `set_gmail_token`, Kalender schreibt
  `calendar_integrations`. Keine gemeinsame Tabelle, kein gegenseitiges Überschreiben.
- Die drei Verbinden-Knöpfe zeigen auf die richtigen Routen: `dashboard.js:13253` und
  `kalender.js:628` auf `/calendar/google-auth`, `dashboard.js:12758` auf `/gmail/connect`.
- Die Gmail-Oberflächen heißen auch Gmail: „E-Mail-Konto", „Gmail-Konto",
  „Mit Google verbinden" (`dashboard.html:942, 1042, 4779`). Nirgends steht dort „Kalender".
- Die Statusanzeigen lesen jeweils aus der richtigen Quelle (`dashboard.js:12194`, `:13226`).

**Ergebnis: nichts geändert.** Ein Label auf Verdacht umzubenennen wäre schlimmer als
es stehen zu lassen. Es braucht einen Screenshot oder den genauen Bildschirm aus dem
Meeting vom 08.08.2026.

### Punkt 1 hat weiterhin keine Ursache

Der Audit-Trail ist eine Falle, kein Fix. Bis der nächste Vorfall auftritt und im
Verlauf sichtbar wird, bleibt offen, wer oder was die Zuweisungen entfernt. Und
solange das SQL-Skript nicht eingespielt ist, zeichnet die Falle gar nichts auf.

### Nebenfund, gehört ins Ops-Dashboard (Teknik)

`kalender.js:628` ruft `/calendar/google-auth?userId=…` auf. Die Route verlangt aber
einen Bearer-Header oder `?token=` (`api-backend/ai/auth.js:19`), sonst antwortet sie
mit `401 Missing bearer token`. Der Verbinden-Knopf auf `kalender.html` dürfte also
ins Leere laufen; der in `dashboard.js` macht es richtig. Nicht mitgefixt, weil er
nicht zu dieser Liste gehört. **Ungeprüft** — nur aus dem Code gelesen, nicht geklickt.

## Prüfstand — was wirklich geprüft wurde

| Was | Wie geprüft |
|---|---|
| `ops/board.js` und `ops/app.js` | `node --check` — Syntax in Ordnung |
| `ops/schema-audit.sql` | **ungeprüft** — nur gelesen, nicht ausgeführt (fremdes Supabase-Projekt) |
| „Verlauf"-Dialog, Zuweisungs-Hinweis | **ungeprüft** — nicht im Browser geklickt |
| Datei-Kodierung | `iconv -f UTF-8 -t UTF-8` über alle Root-Dateien, sauber |
| Auslieferungs-Header | `curl -I` gegen `app.praxura.de` und `praxura.de`, `charset=utf-8` bestätigt |
| CRLF im Repo | Suche über alle Text-Dateien, kein Treffer |
| WhatsApp-Reste in `demo-dashboard.html` | Grep, keine Treffer mehr |
| Google-Fonts-Reste in `demo-booking.html` | Grep, keine Treffer mehr |
| Links und Farben der 404-Seite | Zieldateien auf Existenz geprüft, Grep auf Farbwerte (nur `theme-color`-Meta) |
| 404-Seite live | **ungeprüft** — geht erst nach dem Deploy |
| noindex-Metas | Grep in den drei Dateien, beide Zeilen vorhanden |

## Was noch getan werden muss

1. **Melih: `ops/schema-audit.sql` einmalig ausführen** — Supabase → Projekt
   `praxura-ops` → SQL Editor → Inhalt einfügen → Run. ⚠️ **Nicht** im Produkt-Projekt
   (`njvuclullotbksskpwgk`). Bis dahin protokolliert die Falle nichts.
2. **Melih: `git push`** — sechs Commits liegen lokal, dazu die noch offenen aus dem
   ersten Auftrag des Tages.
3. **Melih: Screenshot zu Punkt 3** — welcher Bildschirm zeigt das falsche
   „Google Kalender"-Label? Ohne das bleibt der Punkt offen.
4. Nach dem Deploy gegenprüfen: eine erfundene Adresse aufrufen (zeigt die 404-Seite?)
   und `demo-booking.html` öffnen (sehen die Schriften unverändert aus?).
5. Der `kalender.js:628`-Nebenfund gehört als eigene Karte ins Ops-Dashboard (Teknik).

## Commits

| Hash | Was |
|---|---|
| `c8ade0f` | feat(ops): Audit-Trail fuer Aufgaben-Aenderungen einbauen |
| `0708abc` | chore(demo): tote WhatsApp-Werbung aus der oeffentlichen Demo entfernen |
| `53b12a6` | chore: UTF-8 und LF verbindlich festlegen (.editorconfig, .gitattributes) |
| `299f22c` | fix(dsgvo): demo-booking laedt Schriften lokal statt von Google |
| `c42b205` | feat(web): 404-Seite im Praxura-Look anlegen |
| `0d21a3a` | docs(podologie): noindex-Punkt bestaetigt, Stand der Loop-Liste ergaenzt |

16 Dateien, +733 / −30 Zeilen (inklusive der beiden Loop-Dateien, die vorher nur
unversioniert im Arbeitsverzeichnis lagen).
