# 2026-08-16

## Terminkalender-Fenster an den Praxisablauf angepasst — Ops-Karte „Podoloji · Hoch"

Grundlage ist die Besprechung vom 12.08.2026. Nausads Kernsatz begründet die ganze Karte:
er ist fast ausschliesslich im Terminkalender, die Bereiche „Ärzte" und „Patienten" öffnet
er nach eigener Aussage „sehr sehr selten". Alles, was dort an Information fehlt oder einen
zusätzlichen Klick kostet, wiederholt sich dutzendfach am Tag.

Acht Punkte aus einem einzigen Fenster, deshalb eine Karte.

## Erledigt

### Neu: `module/termin-verordnung.js`

Die Verordnungsauswahl stand **zweimal wortgleich** in `dashboard.js` — einmal im
Seitenbereich (`openBookingActionModal`), einmal im Neu-Termin-Fenster
(`loadBkVerordnungen`). Beide Kopien sind durch dieses Modul ersetzt. Damit wirken die
folgenden Änderungen an beiden Stellen, statt an einer davon vergessen zu werden.
`dashboard.js` ist dabei von 25.588 auf 25.533 Zeilen geschrumpft (Konsey 2026-08-13, die
Datei wächst nicht mehr).

### 1 · Dienstleistung wird bei gewählter Verordnung nicht mehr gefragt

Die Verordnung enthält die Leistung bereits. Das Feld wird jetzt ausgeblendet und
stattdessen ein Hinweis mit der übernommenen Leistung gezeigt. Wichtig: der **Wert bleibt
gesetzt** — `bookings.service_id` ist weiter Pflicht. Zugeordnet wird über das bereits
vorhandene `findMatchingServiceId()`. Findet der Katalog keine passende Leistung, bleibt
das Feld sichtbar; sonst scheiterte das Speichern an einer Pflichtangabe, die niemand
sehen kann.

### 2 · Arzt war leer — Datenfehler, nicht Kosmetik

Das Feld `bkRxArzt` steht seit jeher in `dashboard.html`, **gefüllt wurde es nie**. Die
Buchungsabfragen holten `prescriptions.arzt_id` gar nicht mit. Beide Abfragen laden jetzt
`arzt_id` und `aerzte(arzt_name,fachrichtung)`; der Seitenbereich zeigt Name und
Fachrichtung.

### 3 · Sitzungsanzeige war unsichtbar

„Offen" war auf `var(--border)` gesetzt — in beiden Themen praktisch nicht wahrnehmbar.
Jeder Zustand trägt jetzt eine eigene Themenfarbe **und** eine eigene Randart (durchgezogen
/ gestrichelt), damit die Unterscheidung auch ohne Farbwahrnehmung trägt. Punkte von 28 auf
32 px, Schrift fetter. Keine festen Hex-Werte (CLAUDE.md, Dark Theme).

### 4 · Frequenzwarnung — warnt, blockiert nicht

Bei „alle vier Wochen" liess sich die zweite Sitzung auf den Folgetag legen. Jetzt kommt
eine Rückfrage mit dem konkreten Abstand; **„Weiter" legt den Termin trotzdem an**. Das ist
so gewollt (Nausad wörtlich), und es ist auch sachlich richtig: die Frequenz ist eine
ärztliche Vorgabe, kein Abrechnungsverbot, und Nachholtermine sind Alltag.

Die Toleranzen sind **Produktvorgabe, nicht aus der Heilmittel-Richtlinie abgeleitet** —
bewusst grosszügig, weil eine zu enge Regel den Hinweis zum täglichen Wegklicken macht und
damit wertlos. `gkv-302` wurde dazu **nicht** befragt; vor einer Verschärfung ist das
nachzuholen. Konkret: „alle N Wochen" → 80 % des Intervalls; „N× wöchentlich" →
`floor(7/N) − 1` Tage. Unbekannter Freitext prüft gar nicht.

12 Testfälle in `module/termin-verordnung.test.js`, Gesamtsuite 55/55 grün.

### 5 · Zähler der aktiven Verordnung zieht sofort nach

Nach dem Verknüpfen der Sitzung wird `verordnungen:changed` gemeldet (`module/signal.js`).
Steht der Seitenbereich offen, lädt er den Termin frisch und baut sich neu auf. Vorher
brauchte es einen Seiten-Neustart.

### 6 · Zuzahlung: Euro-Zeichen druckt direkt

Status (offen / bezahlt / befreit) stand bereits im Seitenbereich — es fehlte der direkte
Druck. Neuer Knopf in allen drei Zuständen, auch bei Privat. Backend:
`GET …/zuzahlungsrechnung?print=1` hängt das `print()`-Skript an die Vorlage, damit der
Druckdialog von selbst aufgeht (`api-backend/billing/api/abrechnung.routes.js`).

### 7 · Befund aus dem Termin — war schon da

`verdrahteFussbefundKnopf()` in `module/fussbefund.js` hängt seit dem 14.08. am
Seitenbereich und öffnet den Fussbefund für genau diesen Termin. Sichtbar bei Podologie mit
Patientenakte. **Nichts gebaut, nur geprüft — und zwar nur im Code, nicht am Bildschirm.**

### 8 · Neu: `module/termin-druck.js` — Terminzettel DIN A5

Kommende Termine des Patienten auf einem A5-Blatt, Knopf im Seitenbereich. Bewusst **ohne**
Diagnose, ICD und Verordnungsdaten: der Zettel verlässt die Praxis und geht verloren — es
steht nur darauf, was der Patient braucht (Art. 5 Abs. 1 lit. c DSGVO). Eigenes Fenster,
Browser-Druck, kein Server und kein PDF-Dienst — läuft damit auch on-premise ohne Internet
(G8).

## Nicht Gegenstand der Karte, nur festgehalten

Der Knopf „Termin starten" **bleibt**. Nausad hat der Zeiterfassung ausdrücklich
zugestimmt (Hausbesuche, Sitzungsdauer).

## Offen / blockiert

- **Nichts davon ist am Bildschirm geprüft.** Es laufen Syntaxprüfung, Typecheck und die
  Unit-Tests — mehr nicht. Der Live-Durchlauf auf `app.praxura.de` steht aus.
- **Lücke bei Punkt 4:** die Frequenzprüfung hängt am Termin-Fenster. Wird eine Sitzung per
  Drag & Drop aus der Seitenleiste in den Kalender gezogen (`handleRxSessionDropToModal`),
  ist `window._bkGewaehlteRx` nicht gesetzt und es wird **nicht** geprüft. Gehört als eigene
  Zeile ins Ops-Dashboard.
- **Paralleles Arbeiten an derselben Datei:** während dieser Sitzung hat eine zweite Sitzung
  `dashboard.js` und `dashboard.html` bearbeitet (Ärzte-Register, `module/arzt-register.js`).
  Die Änderungen liegen unverändert im Arbeitsbaum, meine Bearbeitung lief zeilenweise über
  Textersetzung statt über Zeilennummern, um sie nicht zu zerstören. Vor dem Commit gehört
  beides zusammen durchgesehen. **Das ist der Grund, hier langsam zu machen** — zwei Agenten
  in einer 25.000-Zeilen-Datei ist genau die Konstellation, in der am 14./15.08. schon
  einmal Arbeit verloren ging (siehe `2026-08-15.md`, Punkt 1).

## Was noch getan werden muss

1. **Kemal:** die acht Punkte am Bildschirm durchgehen; die Frequenzwarnung braucht eine
   echte Verordnung mit `frequenz`-Text.
2. `gkv-302` zu den Toleranzen in `frequenzMindestabstand()` befragen, bevor jemand sie
   verschärft.
3. Drag-&-Drop-Lücke als Ops-Karte anlegen.
4. `funktionen/INDEX.json` tazelemek — zwei neue Module, „harita güncelle".

## Commits

Noch keine — Arbeitsbaum enthält zusätzlich die fremde Ärzte-Register-Arbeit.

---

## Belegnummer wird lesbar: `<Patientennummer>-<Verordnungsnummer>`

Grundlage ist dieselbe Besprechung vom 12.08.2026. Nausad rechnet in seinem alten Programm
mit „Patient 1, dessen erste Verordnung" — `1-1`, `1-2`, `2-1`. Bei uns stand dort bisher der
Anfang der UUID (`a3f9c2e1-4`): auf keinem Papier zu finden, nicht fortlaufend, für die
Kassenkorrespondenz wertlos.

### Zuerst gefragt, dann geschrieben

`gkv-302` hat die Spezifikation geprüft, bevor eine Zeile Code entstand. Ergebnis: Belegnummer
ist SLLA→INV, Feld 4, `..10 AN M` (Anlage 1 TP5 V21 § 5.5.3.1, S. 45). **Keine
Zeichenbeschränkung** — wo die Spezifikation eine will, schreibt sie sie hin (Diagnosegruppe:
„nur Ziffern 0-9 und Buchstaben"; Rechnungsnummer: Bindestrich ausdrücklich erlaubt). Der
Bindestrich ist kein EDIFACT-Trennzeichen. Eindeutigkeit nur **innerhalb der Gesamtrechnung**
(Richtlinien-Text 20.11.2006 § 4 Abs. 1), also kein Problem, wenn `1-1` nächstes Jahr wieder
vorkommt. Keine Nullfüllung, kein Ziffernzwang. Das Format ist zulässig.

Zusätzlich fiel dabei § 4 Abs. 1 auf: die Nummer im Datensatz **muss** auf dem Urbeleg stehen.
Mit `12-3` ist das zum ersten Mal von Hand machbar.

### Die eigentliche Gefahr lag nicht in der Spezifikation

Die Belegnummer wurde nirgends gespeichert, sondern an drei Stellen aus `id.slice(0,10)`
neu errechnet — und die Zuordnung einer Kassenrückmeldung (ZAA) hing an derselben Ableitung
(`abrechnung.routes.js`, Upload-Route). Hätte man nur das Format getauscht, wären
Rückmeldungen zu **früher eingereichten** Dateien ins Leere gelaufen: `prescription_id: null`,
Rezept bleibt „in Abrechnung", Podologie-Verordnung wird nie „abgesetzt". Kein Fehler auf dem
Bildschirm — nur fehlendes Geld. Deshalb ist die Nummer jetzt eine **geschriebene Spalte**,
keine Ableitung; die ZAA-Zuordnung kennt beide Schlüssel (neue Nummer + alter UUID-Anfang),
ein Stichtag ist nicht nötig.

### Was in der Datenbank steht (`20260815233848_verordnungsnummer_belegnummer`)

- `verordnungsnummer` und `belegnummer` in **beiden** Verordnungstöpfen (`prescriptions`,
  `verordnungen`).
- Der Zähler läuft **über beide Töpfe**, nicht je Tabelle. Beide hängen an derselben
  Patientenakte und können in derselben DTA-Datei landen; ein getrennter Zähler gäbe demselben
  Patienten zweimal die 3 → `preflight` P:01007 „Belegnummer doppelt", die Erzeugung bliebe
  stehen. Heute hat noch kein Patient beides — eine interdisziplinäre Praxis hätte es am
  ersten Tag.
- Vergabe im Trigger mit `pg_advisory_xact_lock` je `(owner_id, lead_id)`, genau wie bei der
  Patientennummer vom 15.08. Wechselt eine Verordnung den Patienten, wird die Nummer neu
  vergeben; eine bereits eingereichte `belegnummer` bleibt unangetastet.
- Bestand nachnummeriert nach `ausstellungsdatum`, dann `created_at` — die Reihenfolge, in der
  die Rezepte im Ordner liegen. Geprüft: 0 Doppelte, 0 Lücken, 0 ohne Nummer.
- Bereits abgerechnete Zeilen haben `belegnummer = left(id,10)` bekommen, damit eine späte
  ZAA-Datei zur alten Lieferung weiter zugeordnet wird.

### Zwei stille Löcher nebenbei geschlossen

1. **Längenprüfung fehlte.** `preflight` prüfte nur Vorhandensein und Dopplung. `id.slice(0,10)`
   war immer exakt 10 Stellen, die Grenze also nie erreichbar — mit dem neuen Format schon
   (7-stellige Patientennummer + `-` + 3-stellige Verordnungsnummer = 11). Überlänge ist ein
   Syntaxfehler: die Kasse weist die **ganze Datei** ab. Neue Regel `P:01009` (≤ 10 Stellen,
   Bindestrich nur zwischen zwei Zeichen — die Gliederungszeichen-Regel der Rechnungsnummer,
   freiwillig übernommen).
2. **`1-1` war für den ZAA-Leser unsichtbar.** Das Klartext-Muster verlangte
   `[A-Za-z0-9\-]{4,20}`; drei Zeichen fielen durch. Ausgerechnet die Absetzung des ersten
   Rezepts des ersten Patienten wäre still verschwunden. Untergrenze auf 1.

`npm test` 55 grün · `preflight.test.js` 48 · `smoke.test.js` 30 · `parser.test.js` 17 ·
`typecheck` sauber. Trigger gegen die Produktions-DB geprüft (Testzeilen angelegt, Nummern
1 und 2 über beide Töpfe bestätigt, wieder gelöscht). Schema-Dump und Funktionskarte aufgefrischt.

### Offen

- **Im Browser noch nicht nachgeprüft** — kein Commit, kein Deploy. Der Arbeitsbaum enthält
  weiterhin die fremde Ärzte-Register-Arbeit.
- Die Nummer steht noch **nirgends in der Oberfläche**. Für § 4 Abs. 1 („Nummer auf dem
  Urbeleg") gehört sie auf `rezeptvorderseite.template.js` und in die Verordnungsliste —
  eigene Karte, `dashboard.js` darf dafür nicht wachsen.
- `begleitzettel.template.js` wird nach wie vor nirgends aufgerufen; der Anlage-4-Begleitzettel
  entsteht heute nicht. Unverändert offen, unabhängig von dieser Arbeit.
- `error-translations.js`: „Fehlercode 14 = Belegnummer doppelt" liess sich in V21 nicht
  belegen. Wenn die Übersetzung falsch ist, führt sie den Anwender in die Irre — nachprüfen.

---

## Slot-Raster 30 → 15 Minuten — Konsey-Karar, P1

Ausgangsfrage war eine andere: **wie lang soll ein Podologie-Termin im Kalender blockieren?**
Anlage 1a/1b/1c nennt zwei Zeiten — 78010: Regelleistungszeit 35 Min (20 Min Therapiezeit +
15 Min Vor-/Nachbereitung), 78020: 50 Min (35 + 15). Drei Vorschläge lagen auf dem Tisch:
Regelleistungszeit blocken (A), Therapiezeit + getrenntes Pufferfeld (B), nur Therapiezeit (C).

### Entschieden: A bleibt — aber die Frage war die falsche

`konsey/tutanak/2026-08-16-podoloji-slot-suresi.md` · Sitzende: `gkv-302`, `podoloji`,
`muhalif`, `deger-mi`, `fonksiyon-ustasi`, Aussenblick (Gemini, ohne Projektkontext).

Alle fünf stimmten für **A** — die Slotdauer bleibt 35/50, `GKV_LEISTUNGSKATALOG` ist
unverändert. Massgeblich war ein Satz aus Anlage 1a Teil 1 Nr. 4 (i.d.F. 17.06.2024, gleicher
Wortlaut in Anlage 1c 01.07.2025): *„Die notwendige Vor- und Nachbereitung … darf nicht
innerhalb der Therapiezeit durchgeführt werden."* Damit fällt C weg. B fällt weg, weil die
Spalte `services.buffer_time` **nicht existiert** — `booking.js` liest sie trotzdem und bekommt
immer 0; ein Puffer innerhalb des Blocks ist ohnehin A, ausserhalb liegt er ausserhalb des
`no_overlapping_bookings`-Schutzes.

**Der eigentliche Fund kam von `fonksiyon-ustasi`:** der Kapazitätsverlust steckte nicht in der
Dauer, sondern in der **Rasterweite**. `step` war überall fest 30 und kein einziger Aufrufer
schickte etwas anderes. Nach einem 35-Min-Termin (09:00–09:35) fiel der 09:30-Kandidat aus der
Überlappungsprüfung — der nächste Vorschlag war erst 10:00. **25 Minuten Leerlauf pro Patient**,
und zwar unabhängig davon, welche der drei Optionen man gewählt hätte.

Diese eine Tatsache hat in der Blindspot-Runde drei Mitglieder umgestimmt: `muhalif` zog seinen
eigenen Preset-Vorschlag zurück, `deger-mi` gab die Haltung „erst fragen, dann bauen" auf
(*„ich hielt Nichtstun für kostenlos"*), `podoloji` verlangte `step=15`. Korrektur des
Vorsitzes an `muhalif`: unter `step=30` machen 25 und 35 Minuten **sehr wohl** einen
Unterschied (25 lässt den 09:30-Kandidaten leben) — die Presets sind deshalb nicht gestrichen,
sondern hinter P1 gestellt.

### Geändert

`SLOT_STEP_MINUTES = 15` als einzige Quelle in `api-backend/server.js:502-507`; Signatur von
`getAvailableSlots`, `get-slots`, `create` und `ai-suggest-series` hängen daran. Das Frontend
schickt **bewusst kein `step` mehr** (`dashboard.js`, `booking.js`) — das Raster hat genau
einen Ort. Nebenbei ist damit ein stiller Widerspruch geschlossen: `/booking/create` prüft die
Zugehörigkeit zur Vorschlagsliste (`server.js:827`); hätte das Frontend 15er-Vorschläge
gemacht und `create` gegen 30 geprüft, wäre ein 09:45-Termin abgelehnt worden.

In `api-backend/booking/from-request.js:27` steckte dadurch ein **echter Bug**: `slotIstFrei`
prüfte gegen ein festes 30er-Raster, eine Wunschzeit wie 09:45 galt als belegt und die
Terminanfrage wurde abgelehnt, obwohl der Slot frei war.

Nachgewiesen per Simulation, die `SLOT_STEP_MINUTES` aus der Datei liest: 78010/35 Min →
erster Vorschlag jetzt **09:45** statt 10:00 · 78020/50 Min → 10:00 (unverändert, korrekt) ·
Physio 25 Min und Logo 30 Min → keine Regression · Logo 45 Min → 09:45 statt 10:00 (Zugewinn).
Jeder Slot des alten 30er-Rasters existiert auch im neuen, deshalb bleibt die
„vorherigen Vorschlag bevorzugen"-Wertung von `ai-suggest-series` stabil.
`node --check` sauber, `from-request.test.js` 12/12.

### Nicht angefasst — bewusst

Die Dauer-Sperre (`locked: true`) bleibt. Der Einwand von `podoloji` — *„mein Patient braucht
50 Minuten, das System sagt 35, das gibt am ersten Tag eine Beschwerde"* — ist bekannt und
wird in Kauf genommen: kommt die Beschwerde, ist sie der Auslöser für P3 und wir haben einen
echten Bedarfsnachweis statt einer Vermutung. Ebenfalls nicht: das schraffierte 20+15-Bild im
Kalender (`podoloji` hält es für Rauschen), freie Zahleneingabe (Supportlast), und die
Klickziele des Kalenders (`dashboard.js:2637-2638`, `2679-2681`, `2820`), die weiterhin auf
30 Minuten stehen — manuelles Anlegen bleibt vorerst im 30er-Raster.

**Dauerregel aus der Sitzung (`gkv-302`):** die Slotlänge bestimmt **niemals** die Wahl
zwischen 78010 und 78020. Sonst entsteht eine *erzeugte* Therapiezeit-Behauptung
(*„Die Therapiezeit wird … vom Leistungserbringer ermittelt"*, Podologie-FAK 2023 Nr. 24).
Heute ist das erfüllt: `therapiezeit` ist reines Etikett, die Auswahl ist eine Checkbox
(`dashboard.js:24628`).

### Offen

- **Nicht im Browser geprüft, kein Commit, kein Deploy.** Der Nachweis ist reine Rechnung —
  der Durchlauf mit echten Daten (Termin 09:00 anlegen, 09:45 als Vorschlag sehen) steht aus.
- Die Slotliste hat sich verdoppelt (16→33, 19→39). `booking.html` und das `calendar-widget`
  scrollen bereits; **`booking-request.css:668` `.br-slots` hat weder `max-height` noch
  `overflow`** — die Seite wird lang. Eigene Ops-Karte (Teknik, niedrig).
- P2 (tote `buffer_time`-Lesung entfernen) und P3 (Presets „Solo 35/50" / „mit Assistenz
  25/40", Mechanik über `price_config.durations` ist bereits vorhanden) liegen als Karten
  bereit. P3 ist an einen Auslöser gebunden, nicht an einen Termin.
- Frage an die Beta-Podologen, in der von `podoloji` korrigierten Form: *„Wie viele Minuten
  reservierst du im Kalender für einen 78010-Patienten — und ist Reinigung/Dokumentation
  darin enthalten?"* Sie blockiert nichts mehr, sie löst nur P3 aus.

---

## Sitzungsliste im Seitenbereich zeigte 13 statt 18 — die Liste log nicht

Kemal meldete: im Aktionsportal stehen höchstens 13 Sitzungen, obwohl es mehr gibt; Vorschlag
war, die Liste zu verlängern oder zu blättern. **Beides hätte nichts geändert.** In der
Datenbank existierten wirklich nur 13 Zeilen.

### Was tatsächlich vorlag

Verordnung `2c27eb4c` über **18 Einheiten**, **13 Zeilen** in `prescription_sessions` — alle
13 in einer einzigen Einfügung um `23:35:06`, fünf Minuten **nach** dem Anlegen der Verordnung
um `23:30:30`. `saveRezept()` schreibt seine Zeilen in derselben Sekunde; diese hier stammen
also von `linkBookingsToPrescriptionSessions()`, dem Serien-Buchungspfad. Und der legt eine
Zeile je **Termin** an, nicht je verordneter Einheit.

Damit gab es zwei Anlegeregeln und keinen Abgleich:

| Pfad | Regel | wann |
|---|---|---|
| `saveRezept()` | eine Zeile je `anzahl_einheiten` | nur beim Anlegen |
| `linkBookingsToPrescriptionSessions()` | eine Zeile je Termin | bei jeder Serie |

Läuft der zweite zuerst — oder der erste nie, weil die Verordnung über einen anderen Weg
entstand — hat die Verordnung so viele Sitzungen, wie zufällig gebucht wurden.

### Warum das nicht kosmetisch ist

Fünf Einheiten liessen sich nicht auf den Kalender ziehen. Das war der harmlose Teil. Nach
der Regel in `sitzungsfortschritt.js` gilt eine Verordnung erst als fertig, wenn die
verordneten Einheiten erbracht sind — fehlen die Zeilen, wird sie nie `completed`, nie
`abrechnung_status = 'bereit'`, erscheint nie in der §302-Liste und wird **nie abgerechnet,
ohne Fehlermeldung.** Exakt das Muster vom 12.08.2026.

### Gebaut: `module/sitzung-abgleich.js`

Fehlende Nummern aus 1…`anzahl_einheiten` werden ergänzt, sobald der Seitenbereich eine
Verordnung lädt und sobald eine Serie gebucht wird. Drei bewusste Festlegungen:

- **Es wird nie gelöscht.** Eine Zeile oberhalb der Sollzahl kann ein echter Termin sein
  (Nachbehandlung, nachträglich gesenkte Einheitenzahl). Eine zu viel gezeigte Sitzung kostet
  einen Blick; eine gelöschte Sitzung mit Termin kostet die Abrechnung.
- **Abgeschlossene Verordnungen bleiben unberührt** (`completed` · `billed` · `cancelled`).
  Notwendig, nicht vorsichtig: im Bestand steckt eine bereits **abgerechnete** Verordnung ohne
  Sitzungszeilen — ihr offene Sitzungen nachzulegen hätte eine fertige Abrechnung wieder als
  „in Behandlung" erscheinen lassen.
- **`upsert` auf `(prescription_id, session_number)`**, dem vorhandenen Eindeutigkeits-
  schlüssel (in `db/SCHEMA.sql` nachgesehen, nicht geraten). Öffnen zwei Mitarbeiter dieselbe
  Verordnung gleichzeitig, läuft der zweite ins Leere statt in ein Duplikat.

Ergänzt der Abgleich etwas, meldet er `verordnungen:changed`; der Seitenbereich baut sich neu
auf. Beim zweiten Durchlauf fehlt nichts mehr, also meldet niemand mehr — keine Schleife.

13 Testfälle in `module/sitzung-abgleich.test.js`, Gesamtsuite **69/69** grün.

### Offen

- **Nicht im Browser geprüft.** Syntax, Typecheck und Unit-Tests laufen — mehr nicht.
- **21 Altverordnungen mit *null* Sitzungszeilen erreicht die Selbstheilung nicht.** Sie hängen
  an keinem Termin, also öffnet niemand den Seitenbereich für sie und der Abgleich läuft nie.
  Alle stammen aus Mai/Juni und sind nie in Behandlung gegangen; eine davon ist `billed` und
  müsste ohnehin ausgenommen bleiben. Wenn sie bereinigt werden sollen, braucht es eine
  einmalige Migration — **bewusst nicht gelaufen**, das ist eine eigene Entscheidung.
- Der gemeldete Fall (18/13) heilt beim nächsten Öffnen des Termins von selbst, weil dort
  Termine hängen.

---

## Frequenzprüfung neu — diesmal mit Quelle statt mit Schätzung

Kemal fragte nach, ob die Warnung bei Frequenzabweichung schon existiert; er erinnerte sich,
sie einmal als Aufgabe vergeben zu haben. **Im Repo war sie nicht.** `d646245` und `c9776bb`
benutzen die Frequenz nur, um Serientermine zu *verteilen*; `preflight.js` prüft mit `V:01007`
lediglich, ob das Feld überhaupt gefüllt ist. Niemand verglich je Terminabstände.

Der heute Vormittag gebaute Anlauf deckte nur die halbe Anforderung ab — und die Zahlen darin
waren **erfunden** (80 % des Intervalls, `floor(7/N)−1`). Das ist in einer Abrechnungswarnung
nicht tragbar, also wurde vor dem Weiterbauen die Quelle gelesen.

### Was die Quelle sagt

`Podoloji/20230524_Podologie_FAK_bf.txt` Nr. 11, mit Verweis auf § 16 Abs. 4 Satz 5
Heilmittel-Richtlinie:

- Abweichungen **bis 2 Werktage** sind ohne Information des Arztes möglich.
- Darüber ist **Einvernehmen mit dem Arzt** herzustellen und **auf der VO zu dokumentieren**.
- Eine Unterbrechung **kürzer als 12 Wochen** lässt die Verordnung **gültig** bleiben.
- Nr. 36: bei DF/NF/QF darf der Arzt von der 4–6-wöchigen Frequenz nach oben und unten abweichen.

Damit war meine Vormittagsannahme in zwei Punkten falsch: die Toleranz ist **2 Werktage**
(nicht 20 % des Intervalls), und die kritische Grenze für „zu selten" liegt bei **12 Wochen** —
nicht, wie ich zwischenzeitlich vermutet hatte, bei 14 Tagen. Kemals Beispiel „alle fünf Wochen
statt wöchentlich" tötet die Verordnung also **nicht**; es ist eine dokumentationspflichtige
Abweichung, keine Ungültigkeit. Genau dieser Unterschied wäre bei einer geschätzten Zahl
falsch im Produkt gelandet.

### Gebaut: `module/frequenz-pruefung.js`

- **Sollabstand als Korridor**, nicht als Punkt. „1-2x wöchentlich" heisst 4–7 Tage; ein
  einzelner Wert hätte die Hälfte aller korrekten Termine angemeckert.
- **Werktage statt Kalendertage**, wie die Quelle rechnet — ein Termin, der über ein
  Wochenende rutscht, ist kein Verstoss.
- **Beide Nachbarn**: der Termin davor *und* der danach, mit Datum, Sitzungsnummer und
  Abstand in Tagen und Werktagen. Das war Kemals eigentliche Bitte („bir önceki randevusu bu
  zaman, bir sonraki randevusu bu zaman, arada bu kadar gün var").
- **Drei Urteile**: `zu_dicht` · `zu_selten` · `unterbrechung` (>12 Wochen, eigene Überschrift
  und Zitat der Richtlinie). Die Richtung „zu selten" gab es vorher überhaupt nicht.
- Jede Meldung benennt die Folge — Absetzung durch die Kasse — und endet mit „Sind Sie sicher …".
- **Blockiert nie.** Unverändert Nausads Entscheidung vom 12.08.
- Greift jetzt auch beim Ziehen einer Sitzung auf den Kalender und beim Bearbeiten eines
  bestehenden Termins: massgeblich ist `bkSelectedRxId`, nicht mehr der zuletzt angeklickte
  Datensatz. Damit ist die am Vormittag notierte Drag-&-Drop-Lücke geschlossen.

Die erfundene Rechnung in `termin-verordnung.js` ist ersatzlos entfernt, der alte Test dazu
gelöscht. 22 neue Testfälle in `module/frequenz-pruefung.test.js`; die Schwellen 2 und 84 sind
eigens festgenagelt, damit niemand sie beiläufig verstellt. Gesamtsuite **79/79** grün.

### Offen

- **Nicht im Browser geprüft.**
- **Der 2-Werktage-Wert stammt aus dem PODOLOGIE-Katalog.** Die 12 Wochen stehen in der
  Heilmittel-Richtlinie und gelten allgemein, die Toleranz möglicherweise nicht. Bevor die
  Prüfung für Physio/Ergo/Logo scharf geschaltet wird, gehört sie von `gkv-302` gegengeprüft.
  Für die aktuelle Vertikale (Podologie) ist sie belegt.
- Unabhängig davon aufgefallen und **nicht** angefasst: bei zwei Patienten liegen exakte
  **Doppel-Verordnungen** (gleiche Diagnosegruppe, ICD und Ausstellungsdatum) — bei einem
  22 Kopien, drei davon `billed`, zwei mit gesetztem `dmrz_exported_at`. Ob davon je etwas an
  eine Kasse ging, ist offen und gehört zu `gkv-302`. Die fehlende Schutzmassnahme ist eine
  Dublettenwarnung **beim Speichern der Verordnung**, nicht bei der Terminvergabe.

---

## Vier Aufräumarbeiten aus der Rückmeldung von `fonksiyon-ustasi`

Kemal fragte, ob der Agent bei neuem Code überhaupt gefragt wird. Er wurde es nicht — in
dieser Sitzung galt die Anweisung, Agenten nur auf Zuruf zu starten, und ich habe statt
dessen `funktionen/INDEX.md` von Hand gelesen, das aber nicht gesagt. Die Regel steht jetzt
fest in `CLAUDE.md` (Abschnitt „`fonksiyon-ustasi` iki yönlü çalışır"): **immer fragen, und
nach dem Bauen mit Absicht und Einsatzort zurückmelden.** Der Agent wurde nachträglich
über alle vier Module unterrichtet und hat dabei vier Befunde geliefert — alle vier sind
hier abgearbeitet, ohne Ops-Karte.

### 1 · `saveRezept` folgt jetzt derselben Regel wie alle anderen

`dashboard.js` legte in `saveRezept` die Sitzungszeilen mit einem eigenen `.insert()` an —
dieselbe Regel wie `gleicheSitzungenAb`, nur ein zweites Mal geschrieben und ohne
`onConflict`. Heute harmlos (läuft nur beim Anlegen), aber jede künftige Korrektur hätte an
zwei Stellen gemacht werden müssen. Ersetzt durch den Aufruf des Moduls.

### 2 · Eine Druckstrecke statt zwei

`printSeriterminConfirmation` (A4, Serienbestätigung) hatte einen komplett eigenen
HTML-Bauplan. Folge: Anrede, Öffnungszeiten und Bankverbindung, die inzwischen auf dem
A5-Zettel stehen, fehlten dort. `module/termin-druck.js` kann jetzt `format: 'A4'`,
`titel` und `logoUrl`; beide Wege benutzen dieselbe Vorlage. Das Briefkopf-Logo der
Serienbestätigung ist dabei **nicht** verloren gegangen, es ist auf den A5-Zettel
mitgewandert. Der Praxiskopf wird von `terminzettelPraxis()` einmal gebaut.

### 3 · Die Serienplanung liest endlich die Verordnung

Der eigentliche Fund. `frequenzToSeries()` (`dashboard.js`) gibt für **alles** ausser
„täglich" und „alle N Wochen" sieben Tage zurück — bei „3x pro Woche" also eine
Wochenserie. Die Wochentage musste die Praxis von Hand ankreuzen; tat sie es nicht, entstand
eine Serie, welche die neue Frequenzprüfung anschliessend zu Recht beanstandet.

Der Agent nannte das „unsere Prüfung warnt vor unserer eigenen Serie". Das stimmt im
Ergebnis, aber nicht in der Schuldfrage: die Warnung ist **richtig** — eine 3×/Woche-
Verordnung wöchentlich zu bedienen *ist* eine Frequenzabweichung. Falsch war der Planer.
`verteileWochentage()` schlägt jetzt beim Wählen einer Verordnung die Wochentage vor
(Mo + 3 Termine → Mo/Mi/Fr), `setFreqValue` setzt die Wiederholung. **Vorschlag, kein
Zwang** — die Haken bleiben bedienbar.

`sitzungenProWoche()` liest die Zahl direkt aus dem Text statt sie aus dem Tagesabstand
zurückzurechnen; über den Umweg wurde aus „3x pro Woche" (Abstand 2 Tage) vier Termine.
Der Test hat das gefangen, bevor es in den Planer kam.

### 4 · Die Schwellen standen zweimal im Repo — mit falscher Quellenangabe

`Podoloji/podologie-hpnr-reference.js` führte dieselben zwei Werte (2 Werktage, 12 Wochen)
schon, zitiert als „FAQ #22". **Im Katalog ist Nr. 22 „entfallen"**; die Aussage steht in
Nr. 11. Der Zufallsfund ist zugleich die beste Bestätigung, die es geben konnte: zwei
unabhängige Lesungen desselben Dokuments kommen auf dieselben Zahlen. Die Datei ist als
nicht-autoritativ markiert und verweist auf `module/frequenz-pruefung.js`, die Quellenangabe
ist berichtigt.

### Stand

86/86 Tests grün (28 davon in `frequenz-pruefung.test.js`), `dashboard.js` bei 25.516 Zeilen
(Grenze 25.555), Funktionskarte auf 1.633 Einträge aufgefrischt.

**Weiterhin nichts davon am Bildschirm geprüft.**

### 5 · Nachtrag — der Agent fand beim Gegenlesen einen echten Fehler

Nach der Rückmeldung meldete `fonksiyon-ustasi` einen Kopie-Kandidaten: `parseFrequenzWoche`
(`dashboard.js`) und das neue `sitzungenProWoche` lasen beide „wie viele Sitzungen pro
Woche" — mit **unterschiedlichem Ergebnis**. Nachgemessen, und es war schlimmer als gemeldet:

```
4-6 wöchig        → 6      (gemeint: alle 4-6 WOCHEN)
alle 4-6 Wochen   → 6      ← vom Agenten nicht gesehen: der Wächter
                             /alle\s*\d+\s*Wochen/ greift bei Spannen nicht
10 Einheiten      → 10     (Rückfall auf „erste Zahl im Text")
```

`4-6 wöchig` ist die typische Frequenz der Podologie-Diagnosegruppen DF/NF/QF (FAK Nr. 36) —
also ausgerechnet in der aktuellen Vertikale. Der Schaden blieb begrenzt, weil der Wert nur
in einen Hinweis floss („Serie: 10 × · 6× pro Woche — bitte Wochentage wählen"): keine
falschen Daten, aber eine falsche Anweisung an die Praxis.

`parseFrequenzWoche` ist gelöscht; `openBookingFromRxPreset` benutzt jetzt dieselbe
`uebernimmSerienfrequenzAusRx` wie die Verordnungsauswahl und schlägt die Wochentage vor,
statt zum Ankreuzen aufzufordern. Dabei musste die Reihenfolge im Block gedreht werden — der
Starttermin wird jetzt **vor** der Serienplanung gesetzt, sonst verteilt die Wochentaglogik
ab dem heutigen statt ab dem geplanten Wochentag. Zwei Regressionstests halten beide
Fehleingaben fest.

Nebenbefund für später: in den Echtdaten steht bei mehreren Verordnungen `wöchlich`
(Tippfehler). Beide Parser geben dafür `null` zurück — also keine Planung und keine
Frequenzprüfung. Das ist das sichere Verhalten, aber die Eingabemaske sollte den Tippfehler
gar nicht erst zulassen.

Stand nach dem Nachtrag: **88/88 Tests**, `dashboard.js` 25.537 Zeilen (Grenze 25.555 —
entgegen der Schätzung des Agenten *gewachsen*, weil der ersetzte Block länger kommentiert
ist als die gelöschte Funktion), Funktionskarte aufgefrischt.

---

## Rechnungskette Faz 3 — Privat/Selbstzahler, USt und die Rechnungsnummer

**Auslöser:** fünf Ops-Karten aus der Podologie-Spalte, alle mit Bezug auf Konsey
2026-08-10 (Faz 3) und 2026-08-12 (Kova 2). Aufgabe war zuerst nur zu prüfen, ob sie
erledigt sind.

**Erster Befund: die Karten standen auf erledigt, der Code sagte etwas anderes.**
Von fünf Karten waren zwei gar nicht umgesetzt (USt-Felder, `verordnung_id`), zwei
halb (Brücke, § 14-Pflichtfelder), eine fertig (Steuerstatus der Praxis). Wer sich
auf das Board verlassen hätte, hätte auf einer Rechnungskette aufgebaut, die es nicht
gab. Die Karten sind jetzt mit dem tatsächlichen Stand nachgeführt.

### Was tatsächlich gefehlt hat

**1. Die Podologie konnte ihre Rechnung nicht an die Verordnung hängen.**
`invoices.prescription_id` zeigt auf `prescriptions` — den Physio/Ergo/Logo-Topf. Der
Podologie-Bezug wurde als Klartext in `notes` geschrieben („Verordnung vom 12.08.2026
(ID: …)"), also nicht joinbar. Neu: `invoices.verordnung_id` mit FK auf `verordnungen`,
dazu ein CHECK, dass immer höchstens einer der beiden Bezüge gesetzt ist. Die Töpfe
bleiben getrennt (db/README.md Falle 2) — es kam nur ein zweites Bezugsfeld dazu.

**2. Keine einzige Steuerangabe auf der Rechnung.** Das ging, solange nur
GKV-Zuzahlungen abgerechnet wurden. `legal-de` hat die Ops-Karte an einer Stelle
korrigiert: eine einzelne `ust_satz`-Spalte auf der Rechnung reicht nicht, weil
§ 14 Abs. 4 Nr. 7 UStG das **nach Steuersätzen aufgeschlüsselte** Entgelt verlangt und
eine Podologie-Sitzung medizinische und kosmetische Positionen mischen kann. Deshalb
sitzt der Satz jetzt **pro Zeile** in `line_items` (jsonb, kein DDL) und die
eingefrorene Gruppensumme in `invoices.tax_summary`.

**3. Die Rechnung las ihre Steuerangaben zur Druckzeit aus dem Profil.** Wer den
Befreiungstext in den Einstellungen ändert, hätte damit rückwirkend Rechnungen aus dem
Vorjahr anders gedruckt (§ 146 Abs. 4 AO, GoBD Rz. 107 ff.). Steuerhinweis,
Steuernummer und USt-IdNr. werden jetzt mit der Rechnung eingefroren; das Profil ist
nur noch Fallback für Altrechnungen.

**4. Die Rechnungsnummer wurde im Frontend hochgezählt** — als einzige Rechnungsart
im Projekt. `belegliste`, `mahnungen` und `ausfallrechnungen` bekommen ihre Nummer seit
jeher per Trigger (db/README.md Falle 6), `invoices` lief daran vorbei und zählte in
`generateInvNumber()` mit `MAX+1`. Zwei Folgen: zwei gleichzeitige Speicherungen konnten
dieselbe Nummer ziehen, und — schwerer — `saveInvoice()` machte **immer** ein INSERT.
Wer eine bestehende Rechnung öffnete und speicherte, legte eine zweite Zeile mit neuer
Nummer für dieselbe Leistung an. Solange keine USt ausgewiesen wurde, blieb der Schaden
begrenzt; mit Punkt 2 wäre daraus § 14c UStG geworden. `legal-de`: „das Risiko entsteht
genau durch die Arbeit, die ihr gerade macht."

Neu: Tabelle `nummernkreise` + `naechste_nummer()` (`INSERT .. ON CONFLICT DO UPDATE ..
RETURNING`, echte Zeilensperre) + Trigger `set_invoice_nummer()`. Die Nummer wird einmal
vergeben und beim UPDATE unveränderlich fortgeschrieben. `generateInvNumber()` ist
gelöscht. Der Zähler wurde aus dem Bestand vorbelegt, die 18 Altrechnungen mit Nummer
haben ihre `rechnung_nr` nachgetragen bekommen.

Dazu `invoice_festschreibung()`: ab `status <> 'draft'` sind die inhaltlichen Felder
gesperrt, Korrektur läuft über Storno + Neuausstellung. Bewusst offen bleiben `status`,
`payment_*`, `paid_at` und `notes` — daran hängt der Kassieren-Ablauf.

**5. Die Brücke von der Behandlung zur Rechnung.** Der Podologe hatte den Kontext
genau einmal auf dem Bildschirm — beim Dokumentieren der Sitzung — und musste ihn
danach im Rechnungen-Panel von Hand wiederherstellen. Neu: Knopf „Rechnung" an der
Verordnungszeile (nur ausserhalb der Kasse), der die noch nicht abgerechneten
Behandlungen vorbefüllt öffnet, mit Leistungsdatum je Zeile.

Zwei Punkte kamen von `podoloji` und haben den Entwurf geändert:

- **Doppelabrechnung war nicht verhinderbar.** „Ist diese Behandlung schon berechnet?"
  liess sich nicht beantworten. Neu: `podologie_behandlungen.invoice_id`, gesetzt erst
  **nach** erfolgreichem Speichern.
- **Der GKV-Preis darf privat nicht durchschlagen.** `verordnungenLaden()` löst HPNR
  gegen den GKV-Katalog auf; privat wird frei kalkuliert und liegt höher. Ein still
  übernommener Kassenpreis heisst: der Podologe rechnet unter Wert ab und merkt es
  nicht. Die Brücke zieht den Preis deshalb aus den **eigenen Leistungen** der Praxis
  (`services`, über `code`/`gkv_position_nr`) und lässt die Zeile sonst bei 0,00 € mit
  sichtbarem Hinweis stehen. `gkv-302` hatte denselben Punkt schon 2026-08-10 von der
  anderen Seite: HPNR ist ein Vertragskode und ausserhalb des Vertrags bedeutungslos.

**6. Die USt-Frage wird klinisch gestellt, nicht steuerrechtlich.** „Art der Leistung:
Medizinisch (Heilbehandlung) / Kosmetisch" statt „§ 4 Nr. 14a UStG". Die Unterscheidung
trifft der Podologe ohnehin täglich; die Paragraphen kennt er nicht.

Hier gab es eine echte Spannung zum Konsey-Beschluss. Der Beschluss sagt „Selbstzahler
→ Vorgabe 19 %". `podoloji` hat darauf hingewiesen, dass steuerlich nicht der **Zahler**
entscheidet, sondern das **therapeutische Ziel** — ein Selbstzahler mit Diabetes erhält
eine Heilbehandlung. `legal-de` kam unabhängig zum selben Ergebnis: `invoice_type`
beantwortet *wer zahlt*, `ust_satz` *wie besteuert wird*, und beides gehört nicht auf
eine Spalte. Der Beschluss wurde **nicht** neu aufgemacht: seine eigentliche Bedingung —
die Software nimmt die Befreiung nie von selbst an, die Auswahl ist sichtbar und wird
geloggt — bleibt erfüllt. Der Vorschlag kommt jetzt nur aus dem klinischen Anker
(Verordnung/Diagnosegruppe/ICD/HPNR vorhanden → medizinisch, sonst kosmetisch) statt
aus dem Zahlertyp. Die getroffene Wahl wird mit der Rechnung eingefroren.

Beim Kleinunternehmer ist die Zeile ausgeblendet — dort wird nie Steuer ausgewiesen.
Der Grund wird trotzdem pro Zeile gespeichert: § 19 Abs. 2 UStG rechnet den steuerfreien
Heilbehandlungsumsatz aus dem Gesamtumsatz heraus, und wer die Grenze reisst, braucht
die Unterscheidung rückwirkend.

### Neue Dateien

| Datei | Rolle |
|---|---|
| `module/rechnung-steuer.js` | USt-Rechnung, Steuerhinweis, Vorauswahl, Einstellungen-Dropdown (aus `dashboard.js` hierher gezogen) |
| `module/rechnung-steuer.test.js` | 24 Prüfungen — u. a. Rundung je Steuersatz-Gruppe statt je Zeile |
| `module/rechnung-bruecke.js` | Verordnung → Rechnung, Privatpreis aus `services`, Verknüpfung der Sitzungen |

`dashboard.js`: 25.552 Zeilen (Grenze 25.555). Der neue Code liegt vollständig in den
Modulen; in `dashboard.js` blieben Verdrahtung und die Änderungen an `saveInvoice()`,
die durch die gelöschten `TAX_EXEMPT_*`-Blöcke und `generateInvNumber()` bezahlt wurden.

### Was bewusst offen bleibt

- **Knopf „Speichern + Rechnung" direkt an der Behandlung.** `podoloji` hält ihn für den
  wichtigeren der beiden Einstiege (Selbstzahler zahlt am Stuhl). Er braucht aber den
  Verordnungs-losen Weg — und der braucht `podologie_behandlungen.patient_id`, weil der
  Patientenbezug heute ausschliesslich über die Verordnung läuft. Das ist eine
  Erweiterung des Konsey-Beschlusses, keine Ausführung davon → nicht eigenmächtig.
- **Nicht im Browser geprüft, kein Commit, kein Deploy.** Belegt sind: 24/24 Unit-Tests,
  Syntaxprüfung, Modul-Import, Migrationen in der Produktions-DB.
- Steuerberater-Frage aus dem 2026-08-12-Backlog bleibt offen (`legal-de` hat sie auf
  drei Punkte verdichtet — Ausfallhonorar/Materialverkauf, Nachweis der Indikation ohne
  Verordnung, § 19 Abs. 2 Trennung).

---

## Termin-Seitenbereich überarbeitet (Auftrag Kemal, abends)

Kemal ging den Seitenbereich rechts im Terminkalender von oben nach unten durch und
benannte pro Block, was fehlt oder stört. Zwei Punkte davon waren keine Wünsche,
sondern kaputte Funktionen — beide sind erst beim Nachsehen aufgefallen.

### Erledigt

- **Patientensuche im Kopf** (`module/termin-aktionen.js`, `verdrahteAktionsPatientensuche`).
  In den Seitenbereich kam man bisher nur über einen bestehenden Termin; ein Patient
  ohne Termin war unerreichbar, obwohl genau der einen braucht. Auswahl öffnet den
  nächsten Termin, sonst die Terminanlage mit vorausgewähltem Patienten.
  Das Feld ist gleichzeitig die Namensanzeige — der Name stand vorher ein zweites Mal
  mitten im Panel, diese Zeile ist entfallen.
- **Panel 380 → 440 px**, Block „Aktive Verordnung" auf `auto-fit`. Die rechte Spalte
  lief vorher aus dem Bild; jetzt stapelt sie sich zur Not, geschoben wird nie.
- **Stammdaten-Raster in der Patientenkarte entfernt** — acht Zeilen, die beim
  Behandeln fast nie die Frage sind. Stattdessen zwei Knöpfe (Patientendaten,
  Verlauf), beide öffnen die Patientenakte. Der Terminverlauf ist aus demselben Grund
  aus dem Panel verschwunden; die No-Show-Warnung sitzt jetzt neben dem Verlauf-Knopf,
  weil sie der Grund ist, ihn zu drücken. Adresse nur noch bei Hausbesuch.
- **Blättern zwischen Verordnungen** (`‹ 2/3 ›` in der Rezeptinfo). Hat ein Patient
  mehrere laufende Verordnungen, zeigte das Panel stumm die des angeklickten Termins.
  Der Pfeil ruft `openBookingActionModal(booking, { rxId })` erneut auf — Rezeptinfo,
  Sitzungsübersicht und der Block „Aktive Verordnung" wechseln gemeinsam mit.
- **„⧉ übernehmen"** — Angaben einer Verordnung in eine neue Muster-13-Maske
  vorschreiben (Folgeverordnung). Ausstellungsdatum heute, Unterschrift leer, **kein**
  automatisches Speichern: die Maske geht auf, der Mitarbeiter gleicht mit dem Papier ab.
- **„Folgetermin buchen" entfernt** — auf Kemals Entscheidung. Was der Knopf tat, stand
  nirgends, und die beiden Wege daneben (Einheit ziehen, Serie verteilen) machen dasselbe
  mit Bezug zur Verordnung.
- **„Offene Einheiten als Serie verteilen"** über der Liste der unvergebenen Einheiten.
  Kein neuer Ablauf: derselbe KI-Weg wie nach dem Rezept-Scan
  (`openBookingFromRxPreset` → Vormittag/Nachmittag-Frage → Vorschläge → anlegen),
  nur mit den noch OFFENEN statt allen verordneten Einheiten. Über
  `window._physioFlow` hängt `linkBookingsToPrescriptionSessions()` die Termine an
  dieselbe Verordnung, deshalb wandern sie danach von „Unvergebene" nach „Termine".
- **Gewählter Termin steht über „Bearbeiten/Löschen/Verschieben".** Bei sechs Terminen
  sah man dem Knopf nicht an, welchen er trifft.
- **Verschieben im Seitenbereich** — ruft das vorhandene `startMoveBooking()`; der
  Modus war bisher nur aus dem Termin-Dialog erreichbar.
- **Uhrzeit im Terminblock** (Tages- und Wochenansicht) und **Verschieben auf 5 Minuten
  genau** statt auf das 30-Minuten-Raster — `module/kalender-raster.js`. Die Vorschau
  nennt die Zielzeit, weil man sie der Position allein nicht ansieht.

### Zwei kaputte Funktionen, die dabei auffielen

- **Terminzettel A5 lud nie.** Die Abfrage wählte `bookings.employee_name` — die Spalte
  gibt es nicht (`db/SCHEMA.sql`). PostgREST antwortete 400, der Knopf meldete nur
  „Termine konnten nicht geladen werden". Therapeut kommt jetzt aus `user_id`.
  Bei der Gelegenheit das Briefformat gebaut, das Kemal beschrieben hat: Anrede,
  Terminliste, darunter Öffnungszeiten (gleiche Tage zusammengefasst) und Bankverbindung.
- Siehe eigener Abschnitt unten — die Geschlechts-Kodierung.

### Offen

- **Nicht im Browser geprüft.** Belegt sind Syntaxprüfung, ID-Abgleich HTML/JS,
  Schema-Abgleich und 98/98 Unit-Tests. Die Kette „Verordnung blättern" und
  „Serie verteilen" hängen an mehreren vorhandenen Funktionen und gehören nach dem
  Deploy von `canli-test` durchlaufen.

---

## Geschlecht: drei Kodierungen, zwei davon kaputt

`fonksiyon-ustasi` meldete beim Eintragen der neuen Module beiläufig, dass das Feld
`geschlecht` im Projekt uneinheitlich kodiert sei. Kemal wollte es auf eine Fassung
bringen. Beim Nachsehen war es kein Schönheitsfehler.

`leads.geschlecht` trägt seit jeher `CHECK (geschlecht = ANY (ARRAY['m','f','d']))`.
(Der Agent hatte gemeldet, der CHECK sei nicht angewendet — das war falsch, er steht
in der Produktions-DB.) Zwei Schreibpfade kannten ihn nicht:

1. **Schnellanlage** (`#sfGeschlecht`) schrieb „männlich" / „weiblich" / „divers"
   ausgeschrieben → jeder Patient, bei dem jemand das Geschlecht auswählte, liess sich
   **nicht anlegen**; der INSERT scheiterte am CHECK.
2. **Rezept-OCR** lieferte „w" für weiblich (deutsch gedacht, die Spalte will „f") →
   im KI-Rezept-Ablauf liess sich **keine Patientin anlegen**.
3. Dazu ein toter Vergleich `geschlecht === 'weiblich'` in der Therapeutinnen-
   Vorauswahl — der Hinweis „Patient wünscht weiblichen Therapeuten" kam nie.

### Gebaut

| Datei | Rolle |
|---|---|
| `module/geschlecht.js` | einzige Quelle: Codes, Beschriftung, Anrede, Select-Optionen, `normalisiereGeschlecht()` |
| `module/geschlecht.test.js` | 10 Prüfungen; die wichtigste ist die Invariante „was herauskommt, überlebt den CHECK" |
| `api-backend/lib/geschlecht.js` | dieselbe Regel serverseitig — `api-backend/` ist ein eigenes Docker-Image mit eigener COPY-Liste und kann das Frontend-Modul nicht importieren. Bewusste Kopie, in beiden Dateien vermerkt. |

Angeschlossen: beide `<select>` (Optionen kommen jetzt aus dem Modul, nicht mehr
handgeschrieben aus dem HTML), Patientenakte speichern und laden, Schnellanlage,
Rezept-Confirm, `server.js`, der OCR-Prompt, `patientenkarte.js` (eigene Tabelle
gelöscht) und `termin-druck.js` (Buchstabenlogik gelöscht, delegiert jetzt).

### Datenbank

Werte **unverändert** — m/f/d + NULL, es gab keinen abweichenden Bestand
(5× m, 5× f, 103× ohne Angabe; in `metadata` nichts). Kein ENUM: jede andere begrenzte
Spalte dieses Schemas (`status`, `rezept_typ`, `insurance_type`, `fahrt_status`) ist
`text` + CHECK, und ein ENUM liesse sich später nur umständlich erweitern.

Migration `leads_geschlecht_kodierung_dokumentieren` setzt ausschliesslich einen
`COMMENT ON COLUMN`: die Kodierung war nur im CHECK sichtbar und nirgends erklärt —
genau deshalb hat sie dreimal jemand geraten. Sie steht jetzt in der DB **und** im
Dump, also auch dort, wo jemand ohne DB-Zugang nachsieht.
`db/SCHEMA.sql`, `db/SCHEMA-RLS.sql` und `db/README.md` sind fortgeschrieben.

### Offen

- **Nicht im Browser geprüft.** Beide reparierten Pfade (Schnellanlage mit
  Geschlechtsangabe, Rezept-Scan einer Patientin) sind die Probe aufs Exempel und
  gehören nach dem Deploy durchlaufen.
- `profiles.anrede` ist ein anderes Feld (Anrede des Praxisinhabers, nicht des
  Patienten) und wurde **nicht** angefasst.
