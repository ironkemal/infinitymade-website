# 2026-08-15

## Beta-Rückmeldung zur Patientenverwaltung — sechs Korrekturen

Kemal hat die Karte `7619eb49` am Bildschirm durchgetestet. Sechs Befunde, alle bestätigt.

### 1 · Beschriftungen zeigten den Schlüssel statt des Textes

„Festnetz", „Handy", „Geschlecht" und die Spalte „Geburtsdatum" standen als `lead_festnetz`
usw. da. Die Wörterbucheinträge waren beim ersten Anlauf am 14.08. geschrieben worden, gingen
aber verloren, als ein parallel laufender Agent `dashboard.js` aus seiner eigenen Kopie neu
schrieb. Beim Nachziehen hatte ich nur die Feldzugriffe wiederhergestellt, nicht die drei
Sprachtabellen. Jetzt in de/en/tr, dazu `lead_patientennr`.

### 2 · Kassenliste brach im „B" ab

Nicht die Datenquelle — die Tabelle führt **94 Kassen**, alle da. Meine Obergrenze in
`sucheKassen()` stand auf 30, und die Liste ist alphabetisch: der Anwender sah ein Fünftel
des Bestandes und schloss auf die falsche Datenbank. Grenze auf 300; das Dropdown scrollt
ohnehin (`max-height` in `dashboard.css`). Ein Test hält die Regression fest.

### 3 · Patientennummer — Migration `20260815085338_leads_patientennummer`

Gewünscht: fortlaufend ab 1, Bestand nachnummeriert, neue Patienten zählen weiter, und in
der Tabelle **ganz vorn und klein**.

- Bestand nachgetragen nach `created_at` — die älteste Akte bekommt die 1.
  (77 · 24 · 7 · 5 Patienten je Praxis, lückenlos, geprüft.)
- Vergabe im **Trigger**, nicht im Client: legen zwei Mitarbeiter gleichzeitig an, lesen
  beide dasselbe Maximum und die Nummer wäre doppelt. `pg_advisory_xact_lock` je `owner_id`
  serialisiert genau diesen Moment; zwei Praxen behindern sich dabei nicht.
- **Je Praxis** bei 1 beginnend, nicht global: sonst begänne der zweite Kunde bei 3.000 und
  könnte aus seiner Nummer die Kundenzahl des Herstellers ablesen.

### 4 · Status von Hand änderbar

Der Status entsteht weiter von selbst; er ist jetzt zusätzlich anklickbar, damit man ihn
richtigstellen kann, wenn etwas schiefgelaufen ist. Geändert wird dabei die **massgebende
Verordnung** — die dringlichste des Patienten —, nicht „der Patient": einen Patientenstatus
gibt es nicht. Dafür liefert `ladeStatusJePatient()` jetzt auch deren ID mit.

### 5 · Sprung aus dem Verlauf setzt den Patienten

Bisher wechselte der Klick nur das Panel, der Anwender musste dort erneut suchen — damit
sparte der Sprung nichts. Jetzt wandern Patient und Datum mit:
Fußbefund über den vorhandenen `oeffneFussbefundFuerTermin`-Weg (Patient vorgewählt),
Podologie über `selectedVordId`, Kalender springt auf das Datum des Termins.
Behandlungszeilen zeigen dabei auf ihre **Verordnung**, nicht auf sich selbst — die
Podologie-Liste wählt über die Verordnungs-ID aus.

### 6 · „Status"-Knopf liess sich nicht klicken

Der Zuhörer hing am Ende von `loadPodologieBilling()`: erst die Liste zeichnen, dann noch
Kassenliste, Heilmittel-Selects und Kataloge — und ganz zuletzt `addEventListener`. Wirft
irgendetwas dazwischen, sind die Schaltflächen sichtbar, aber tot. Genau dieses Bild.
Die Delegation hängt jetzt **einmal an `document`** und ist davon unabhängig, wie weit das
Rendern kommt; Fehler beim Öffnen des Dialogs landen sichtbar im Toast statt still in der
Konsole.

### Nicht bestätigt: die Versichertennummer

Gemeldet als „geht nur, wenn hinten eine 0 steht". Das ist richtig so: die letzte Stelle
**ist** die Prüfziffer, und für `A12345678` ist sie rechnerisch 0 — jede andere Endziffer
gehört zu einer anderen Nummer. Bei einer echten Karte stimmt die Endziffer von selbst.

### Architektur

`renderLeads` (80 Zeilen) ist nach `module/patientenliste.js` gewandert — dieselbe
Umzingelung wie beim Fußbefund. `dashboard.js` bleibt damit unter der Grenze
(**25.655 Zeilen**), obwohl sechs Korrekturen dazukamen.

Schema-Dump aktualisiert, Funktionskarte neu erzeugt, `npm test` 31 grün, `typecheck` sauber.
**Im Browser noch nicht nachgeprüft.**

## Meldung „Patientenliste leer" — geprüft, kein Datenverlust

Vor dem Push gemeldet: in einem Konto sei die Patientenliste leer. Verdacht lag auf den
Migrationen dieses Tages.

**Die Daten sind vollständig.** Gegen die Produktions-DB geprüft:

| Prüfung | Ergebnis |
|---|---|
| Patienten je Praxis | 77 · 24 · 7 · 5 — zusammen 113, keine Lücke |
| Verwaiste Verordnungen / Termine / Fußbefunde / Rezepte | **0** in allen vier Tabellen |
| RLS-Policies auf `leads` | unverändert (owner + Team, wie seit jeher) |
| `business_id` zeigt ins Leere (bizScope-Falle) | 0 Zeilen |
| `patientennummer` nach der Migration | überall gesetzt, je Praxis lückenlos ab 1 |
| `updated_at` der Karteien | Juni/Juli — die Nummernvergabe hat sie nicht angefasst |

Also kein Schreibfehler, sondern Anzeige. Zwei Wege führen dorthin, beide sind jetzt zu:

**1. Ein stehengebliebener Filter sah aus wie Datenverlust.** Die neue Filterleiste
(Bereit · Abgerechnet · Teilabsetzung · Absetzung · Storniert) leert die Liste vollständig,
solange keine Verordnung einen dieser Zustände hat — und aktuell hat das noch keine. Die
Liste zeigte dann denselben Text wie eine leere Kartei. Jetzt unterscheidet sie:
„113 Patienten vorhanden, aber keiner passt zur aktuellen Auswahl" **mit Knopf
„Filter zurücksetzen"**. Eine leere Liste muss sagen, warum sie leer ist.

**2. Der Abrechnungsstatus konnte die ganze Kartei verdecken.** In `loadLeads()` stand
`await ladeStatusJePatient(...)` VOR `renderLeads()`. Schlägt dieser Aufruf fehl, wird nie
gezeichnet — ein Schmuckfeld der Zeile hätte die Patientenliste unsichtbar gemacht. Steht
jetzt in `try/catch`: ohne Status wird gezeichnet, nur ohne Etikett.

Nebenbei ist `patientMatchesQuery` (25 Zeilen) nach `module/patientenliste.js` gewandert;
`dashboard.js` steht bei **25.640** Zeilen.

⚠️ Offen: welches Konto und welcher Bildschirm gemeint war, ist nicht bestätigt. Beide
Ursachen oben sind unabhängig davon echte Fehler und behoben — sollte die Liste danach
weiter leer sein, braucht es die Konsolenmeldung aus dem betroffenen Konto.

## „Status lässt sich nicht auswählen" — die Ursache liegt tiefer

Gemeldet: in der Patientenliste sei der Status nicht anklickbar. Stimmt, und der Grund ist
kein Anzeigefehler:

> **Keiner der 113 Patienten hat eine Verordnung.** Im ganzen System gibt es drei
> Verordnungen, und **alle drei haben `lead_id = NULL`** — sie hängen an keinem Patienten.

Der Abrechnungsstatus gehört zur Verordnung. Wer keine hat, hat nichts zu setzen; die
Schaltfläche erschien deshalb gar nicht erst. Das ist konsistent — sah aber wie ein Defekt aus.

### Der eigentliche Fehler: Verordnungen ohne Patientenakte sind nicht abrechenbar

`lead_id` wurde **nur** gesetzt, wenn der Anwender den Patienten aus der Vorschlagsliste
anklickte. Wer den Namen tippte und weiterging, speicherte eine Verordnung, die bloss einen
Freitextnamen trug (`patient_name`). Beim Speichern sagte das niemand — und §302 weist genau
diese Verordnungen zurück:

    Verordnung 5f3a…: kein Patient aus der Kartei verknüpft.
    (abrechnung.routes.js, mapVerordnungToDtaShape)

Eine solche Verordnung lässt sich also **nie** einreichen. Sie sieht vollständig aus, steht in
der Liste, sammelt Behandlungen — und fällt erst beim Erzeugen der Kassendatei durch. Genau
die Sorte stiller Einnahmeverlust, gegen die der Rest dieser Kette gebaut wurde.

**Behoben beim Speichern der Verordnung:**
- Trifft der getippte Name **genau einen** Patienten, wird nachgebunden — der Klick war nur
  vergessen, die Absicht ist eindeutig.
- Bleibt es dabei mehrdeutig oder unbekannt und ist es eine **GKV**-Verordnung, wird nicht
  gespeichert: „Bitte den Patienten aus der Liste auswählen — ohne Patientenakte lässt sich
  die Verordnung später nicht abrechnen." Bei Privat/Selbstzahler bleibt der Weg offen.
- Die Statuszelle sagt jetzt „keine Verordnung" statt eines Strichs, mit Erklärung im
  Tooltip. Ein Strich sah aus, als wäre die Schaltfläche kaputt.

### Wo der Status überhaupt gesetzt wird — die vier Tore

| Übergang | Auslöser |
|---|---|
| `aktiv` → `abrechenbar` | alle Einheiten der Verordnung behandelt (automatisch) |
| → `abgerechnet` | §302-Datei erzeugt — **nur maschinell**, nie von Hand |
| → `abgesetzt` | ZAA-Rückmeldung der Kasse eingelesen |
| → `teilabsetzung` / `storniert` | von Hand, mit Betrag bzw. Grund |

Von Hand erreichbar an zwei Stellen: „Status"-Knopf je Verordnung in der
Podologie-Abrechnung, und ab jetzt auch über das Etikett in der Patientenliste — dort wird
die **massgebende** (dringlichste) Verordnung des Patienten geändert.

### Offen

Die drei bestehenden Verordnungen ohne `lead_id` sind weiterhin nicht abrechenbar. Sie tragen
einen Freitextnamen; ein Nachbinden per Namensabgleich wäre möglich, ist aber ein Eingriff in
Bestandsdaten und wartet auf Zustimmung.

## Stiller Einnahmeverlust: leere Platzhalter-Sitzungen (Ops-Karte vom 12.08.)

Erste Karte aus dem Beta-2-Gespräch. Vor der Umsetzung gegen Code **und** Produktionsdaten
nachgeprüft, weil die Karte drei Tage alt war.

### Der Fehler — bestätigt, unverändert

`saveRezept` legt beim Speichern so viele Zeilen in `prescription_sessions` an, wie Einheiten
verordnet sind, zunächst alle ohne `booking_id`: Platzhalter für „noch nicht geplant". Die
Fertig-Prüfung in `markPrescriptionSession` fragte aber nur

    „gibt es hier noch eine Zeile, die nicht `done` ist?"

Ein nie verplanter Platzhalter antwortet darauf für immer mit ja. Kette:
Rezept nie `completed` → `abrechnung_status` nie `bereit` → die §302-Liste
(`dashboard.js`, `.eq('abrechnung_status','bereit')`) zeigt es nie → **es wird nie
abgerechnet, ohne eine einzige Fehlermeldung.**

### Was die Karte zu gross geschätzt hatte

| Karte (12.08.) | Befund heute |
|---|---|
| Aufräum-Migration, Klasse A/B, `gkv-302`-Freigabe, K4 | 28 leere Zeilen in 7 Rezepten, **alle Klasse A** — kein `abrechnung_id`, keine DTA berührt |
| „Rezepte hängen fest" | **noch keines.** Keine Serie ist bisher vollständig abgehakt |
| Backfill nötig | nicht nötig — simuliert: kein Rezept kippt nach der neuen Regel |
| Betroffene Kundendaten | 6 von 7 Rezepten sind `demo@praxura.de`; **eines** gehört Beta-2 |

Die Falle ist also scharf, aber noch nicht zugeschnappt. Deshalb **kein Datenmigrationsschritt**:
nach der Korrektur blockieren die Platzhalter nichts mehr, die Kapazitätsprüfung zählt sie
ohnehin nicht mit (`booking_id IS NOT NULL`), und die §302-Positionen entstehen aus `done`-Zeilen.
Sie zu löschen wäre Bewegung ohne Wirkung an Daten, die an der GoBD-Grenze liegen.

### Die neue Regel — `module/sitzungsfortschritt.js`

    offen    = Sitzungen MIT Termin im Status `planned`
    erbracht = Sitzungen im Status `done`
    fertig   = offen === 0  UND  erbracht >= verordnete Einheiten

Beide Hälften sind nötig. Ohne die erste der Fehler oben; ohne die zweite wäre ein Rezept über
6 Einheiten schon nach dem ersten abgehakten Termin abrechnungsbereit, weil die übrigen fünf
noch ungebucht sind. `cancelled` und `no_show` gelten als erledigt, nicht offen — die Einheit
ist damit aber auch nicht erbracht, und die zweite Hälfte hält das Rezept in Behandlung, bis
sie nachgeholt ist.

**Bewusst nicht automatisiert:** bricht der Patient nach 5 von 6 Einheiten ab, bleibt das Rezept
in Behandlung. Eine Teilabrechnung ist nach §302 zulässig, aber eine Entscheidung des Menschen —
dafür gibt es den Knopf „Als bereit markieren". Automatisch abrechnen, was nie zu Ende behandelt
wurde, wäre schlimmer als der Fehler, der hier behoben wird.

### Dateien

- **neu** `module/sitzungsfortschritt.js` — Regel + Statuswechsel, mit der Begründung im Kopf
- **neu** `module/sitzungsfortschritt.test.js` — 5 Fälle, grün; hält vor allem die
  Gegenprobe fest (abgebrochene Serie wird **nicht** automatisch bereit)
- `dashboard.js` — `markPrescriptionSession` ruft nur noch das Modul.
  **25.640 → 25.621 Zeilen**, Basis rutscht mit
- `dashboard.html` — `?v=20260815e`

⚠️ **Im Browser noch nicht nachgeprüft.** Der Weg braucht einen abgehakten letzten Termin einer
vollständig gebuchten Serie; in den Daten gibt es diesen Zustand aktuell nirgends.

### Was das für den §302-Livetest mit Beta-2 heisst

Sein Rezept `2c27eb4c` (KG-Gerät, **18** Einheiten, 12 Termine, Barmer-IK gesetzt) erreicht die
Abrechnungsliste auch nach dieser Korrektur nicht von selbst — 12 < 18 ist eine unfertige Serie,
keine fertige. Das ist die gewollte Antwort, aber für den Livetest muss man es wissen: entweder
die restlichen Einheiten buchen oder bewusst „Als bereit markieren" (Teilabrechnung).

### Warum die Verordnungen keinen Patienten hatten — nachgegangen

Rückfrage: „Ich habe beim Anlegen doch einen Patienten ausgewählt." Stimmt. Der Fehler lag
nicht beim Anwender.

Die drei Verordnungen im System stammen vom **23.06., 24.06. und 14.07.** Die Patienten-
bindung im Podologie-Formular (`podNewLeadId`) kam erst am **26.06.** dazu (Commit `e80eb45`)
— die ersten beiden konnten also gar keine haben. Bleibt die vom 14.07., und die führt zur
eigentlichen Ursache:

> Die Patientensuche in diesem Formular liest `leadsCache`. Dieser Zwischenspeicher wurde
> **nur** gefüllt, wenn jemand vorher das Patienten-Panel geöffnet hatte
> (`switchPanel`, `if (id === 'kunden') loadLeads()`).

Wer sich anmeldet und direkt in die Podologie-Abrechnung geht, findet dort eine leere
Patientensuche — „Keine Treffer", obwohl 113 Patienten in der Kartei stehen. Man tippt den
Namen von Hand, das Formular nimmt ihn als Freitext, und gespeichert wird eine Verordnung
ohne `lead_id`: unabrechenbar, ohne dass irgendwo etwas gesagt wird.

**Behoben:** die Suche lädt jetzt selbst nach (`ensureLeadsCache()`), statt darauf zu warten,
dass ein anderer Bildschirm das für sie erledigt. Zusätzlich ist das tote
`<datalist id="podPatientList">` am selben Feld entfernt — es wurde nie befüllt, und
`attachPatientSearch` nimmt das `list`-Attribut ohnehin weg; kommt das Rendern aber nicht bis
dorthin, blieb das leere Browser-Menü als einziges sichtbares Angebot stehen.

Die Rezeptmaske war davon nicht betroffen: sie lädt ihre Patienten selbst und zeigt sogar
einen Hinweis, wenn keine da sind.
