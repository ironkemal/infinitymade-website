# 2026-08-27

## Woran gearbeitet wurde

Kein Ops-Kartenauftrag, sondern ein im Betrieb aufgefallener Fehler: beim
Drucken eines Belegs aus dem Seitenbereich erschien

> **Rechnung kann nicht gedruckt werden** — Für eine Rechnung sind folgende
> Angaben gesetzlich vorgeschrieben (§ 14 Abs. 4 UStG) … Bitte tragen Sie die
> Angaben unter **Einstellungen → Praxisdaten** nach.

Der Hinweis war inhaltlich richtig, der Weg dorthin nicht.

## Befund

Drei Fehler in einer Meldung:

1. **Sie kam von aussen.** Der Riegel sass im Server
   (`api-backend/billing/api/abrechnung.routes.js`, `pflichtangabenHinweisHtml`)
   und wurde als **komplette HTML-Seite** mit Status 400 ausgeliefert. Der
   Anwender klickte „Drucken", bekam einen fremden Tab auf einer fremden Domain
   (`n8n.infinitymade.de`), las dort einen Text, musste den Tab schliessen, im
   Dashboard die Einstellungen suchen und den ganzen Weg erneut gehen. Ein
   fremder Tab kann nicht navigieren und nichts nachtragen — er kann nur ein
   Text sein.
2. **Der genannte Ort existiert nicht.** „Einstellungen → Praxisdaten" gibt es
   nicht; IBAN, Steuernummer und USt-IdNr. stehen unter **Einstellungen →
   Finanzen → Rechnungsdaten** (`dashboard.html`, `#settingsBillingSection`).
3. **Zwei Regeln, die sich widersprachen.** Der Server verlangt IBAN **und**
   „Steuernummer *oder* USt-IdNr." (`fehlendePflichtangaben`). Die einzige
   Frontend-Prüfung, die es gab (`dashboard.js`, `proceedToOwnerCheck` beim
   Nichterscheinen), verlangte `iban && steuernummer` — eine Praxis, die nur
   die USt-IdNr. führt, bekam dort eine Warnung ohne Anlass.

Vorab bei `fonksiyon-ustasi` gefragt. Ergebnis: ein Dialog-Helfer existiert
(`showHtmlModal`, `dashboard.js`), ein „zur Einstellung springen"-Helfer nicht
(nur einmal als Inline-`onclick` im IK-Banner), ein zentraler Druck-Öffner
ebenfalls nicht — die Belegrouten wurden an **zwei** Stellen mit `window.open`
aufgerufen.

## Erledigt

**1. Neu: `module/beleg-druck.js`**

- `fehlendePflichtangaben(profil)` — wörtlich die Serverregel. Beide Seiten
  müssen zusammen geändert werden; steht als Kommentar in beiden Dateien.
- `istRechnungsartig(typ)` — die vier `RECHNUNGS_TYPEN` des Servers plus
  `quittung_zuzahlung` (heisst Quittung, ist aber wegen Fälligkeit und
  Bankzeile eine Rechnung und wird serverseitig immer geprüft).
- `ladePraxisAbrechnungsProfil()` — modulweit zwischengespeichert. Liest
  **die Inhaberzeile** (`getOwnerId()`), nicht `currentProfile`: Bankverbindung
  und Steuernummer gehören der Praxis; bei einem angemeldeten Mitarbeiter sind
  sie in der eigenen Zeile leer, und die Prüfung hätte jeden Druck blockiert,
  obwohl der Server ihn zulässt.
- `belegDruckUrl()` — beide Routen an einer Stelle, Token kodiert (ein JWT kann
  `+` und `/` enthalten; unkodiert bricht der Query-String und der Server
  antwortet 401 statt zu drucken).
- `oeffneBelegDruck()` — Vorprüfung, dann `window.open`, dann Popup-Meldung.

**Bewusst nur bei gesehenem Profil blockiert:** liefert die Abfrage nichts
(RLS, Netz), entscheidet weiter der Server. Ein Hinweis zu wenig ist besser als
ein Druck, der ohne Grund verweigert wird.

**2. Der Hinweis steht jetzt im Dashboard** — `showHtmlModal` mit dem Knopf
„Zu den Rechnungsdaten", der über den neuen Helfer `gehZuEinstellung(sectionId,
focusId)` in die Einstellungen springt, den Abschnitt anscrollt und den Cursor
in `#setIban` setzt. Der bisher einzige Vertreter dieses Musters — der
Inline-`onclick` im IK-fehlt-Banner — hängt jetzt ebenfalls daran.

**3. Beide Aufrufstellen hängen am selben Weg.** `openZuzahlungsrechnung()` ist
auf die Belegart zusammengeschrumpft; der `.rx-drucken-item`-Handler ruft
denselben Öffner. Damit gibt es keinen Druckweg mehr, der die Vorprüfung
umgeht.

**4. Serverseitiger Riegel bleibt** — er ist die einzige Stelle, die ein Client
nicht umgehen kann. Aber er ist ab jetzt Rückfallebene, nicht Bedienweg; das
steht als Kommentar über der Funktion, und die falsche Wegbeschreibung im Text
ist korrigiert.

**5. Die widersprüchliche Regel bei `proceedToOwnerCheck`** ruft jetzt
`fehlendePflichtangaben()` auf, statt sie ein zweites Mal (und falsch)
auszuformulieren.

**6. Cache:** `abrechnungsprofilCacheLeeren()` hängt am Speichern der
Rechnungsdaten — sonst blockierte der Druck weiter mit dem alten Stand.
`dashboard.html` auf `dashboard.js?v=20260827b` angehoben (die Lehre vom
26.08.: wer eine Datei ändert, muss die Version an der Importstelle anheben).

**Geänderte Dateien**

- neu `module/beleg-druck.js` · `module/beleg-druck.test.js`
- `dashboard.js` — Import, `gehZuEinstellung`, `belegDruckDeps`, beide
  Aufrufstellen, IK-Banner, `proceedToOwnerCheck`, Cache beim Speichern
- `dashboard.html` — Versionsanhebung
- `api-backend/billing/api/abrechnung.routes.js` — Kommentar + Wegbeschreibung
- `funktionen/INDEX.json` · `INDEX.md` — Karte neu erzeugt (1691 Funktionen)

**Geprüft**

- `npm test` → **152/152 grün** (13 neu)
- `npm run typecheck` → stumm
- `node --check` auf allen drei geänderten JS-Dateien
- `dashboard.js` **25.357 → 25.355 Zeilen** (Schranke 25.376 gewahrt)

## Die Regel, die daraus folgt

**Eine Bedingung, die der Anwender im Dashboard erfüllen muss, wird im
Dashboard geprüft und im Dashboard gemeldet — mit dem Weg dorthin als Knopf.**
Serverseitige Prüfungen bleiben als Netz bestehen, aber eine Server-HTML-Seite
in einem fremden Tab ist keine Benutzerführung.

Beim nächsten Beleg-/Exportweg also zuerst fragen: kann der Anwender das, was
hier fehlt, überhaupt im geöffneten Tab nachtragen? Wenn nein, gehört die
Meldung nach Hause.

## Offen

- **Nicht im Browser gesehen.** Geprüft sind die reinen Funktionen und die
  Syntax, nicht die Darstellung. Der Durchlauf, der zählt: Praxis ohne IBAN →
  Beleg drucken → Dialog erscheint **im** Dashboard → Knopf springt in die
  Rechnungsdaten mit Cursor in der IBAN → nachtragen, speichern, drucken geht
  ohne Neuladen (Cache-Leerung).
- Der Dialogtext steht deutsch im Modul, nicht im Wörterbuch von
  `dashboard.js` — wie der umgebende Bestand. **EN/TR fehlen.**
- `showHtmlModal` ist weiterhin nicht exportiert; das Modul bekommt es als
  Abhängigkeit hereingereicht. Sechs handgeschriebene Overlays im Bestand
  (`abrechnung-freigabe.js`, `rechnung-zahlung.js`, `kiosk.js`,
  `openKassierenDialog`, `_patientAddressModal`) warten weiter auf ein
  gemeinsames `module/dialog.js` — **Entscheidung Kemal**, nicht nebenbei.

---

# 2026-08-27 (Nachtrag) — Terminkalender: doppelte Spalte, eigene Spalte zuerst

## Woran gearbeitet wurde

Meldung aus der Praxis: in einer Ein-Personen-Praxis zeigte die Tagesansicht
denselben Behandler manchmal zweimal — zwei identische Spalten mit denselben
Terminen, „meistens nachdem ein neuer Termin angelegt wurde".

## Befund

Nicht die Daten. Auf dem Bildschirmfoto steht in der Chip-Reihe genau **ein**
Behandler, in der Ansicht darunter stehen **zwei** Spalten — dieselbe Person
wurde also zweimal gezeichnet.

`renderDayView()` leerte `dvColsWrap` ganz am Anfang und wartete danach auf
Supabase. Die Funktion wird aus mehreren Ecken angestossen (Signal nach dem
Speichern, Realtime, Panelwechsel, Verschieben). Startete ein zweiter Aufbau,
während der erste noch auf die Abfrage wartete, hängten am Ende **beide**
Läufe ihre Spalten in denselben Container.

## Erledigt

- **`module/kalender-team.js`** (neu) — `teamReihenfolge()` (doppelte
  Profilzeilen raus, eigener Zugang zuerst) und `renderEmpChips()` (aus
  `dashboard.js` hierher gezogen). Der Zwilling in der Übersicht
  (`renderOvEmpChips`) wurde **nicht** stillschweigend mitzusammengelegt —
  das ist Kemals Entscheidung.
- **Tagesansicht baut atomar auf** (`dashboard.js`, `renderDayView`):
  Zeitleiste und Spalten entstehen in einem `DocumentFragment` und werden am
  Ende per `replaceChildren` am Stück eingehängt. Treffen zwei Läufe
  aufeinander, gewinnt der letzte, statt sich anzuhängen.
- **Eigene Spalte steht links** — `loadTeam()` schiebt `teamMembers` durch
  `teamReihenfolge(data, currentSession.user.id)`. Damit ziehen Spalten,
  Chips, Farben und die Mitarbeiter-Auswahlfelder gemeinsam um; die Farbe
  hängt weiter an der Position in `teamMembers`, bleibt also konsistent.
- **Nebenbefund behoben:** nach dem Löschen eines Termins rief
  `dashboard.js` `renderDayView()` **ohne Datum** auf — `Invalid Date`, die
  Abfrage bricht ab und der Kalender bleibt leer. Ersetzt durch
  `refreshBookingViews()`.
- `module/kalender-team.test.js` (neu, 6 Tests), Funktionskarte neu erzeugt,
  Cache-Busting `dashboard.html?v=20260827a`.

## Geprüft

- `npm test` → **158/158 grün** (6 neu)
- `npm run typecheck` → stumm
- `dashboard.js` **25.355 → 25.357 Zeilen**; Schranke 25.376 gewahrt

## Offen

- **Nicht im Browser gesehen.** Der Durchlauf, der zählt: Tagesansicht offen
  lassen, mehrere Termine kurz hintereinander anlegen/verschieben/löschen —
  es darf nie eine zweite Spalte desselben Behandlers erscheinen. Dazu:
  Mehr-Personen-Praxis öffnen und prüfen, dass der eigene Zugang links steht.
- Die Ursache lag im Kalender, nicht in `teamMembers`. Sollte in einer
  Praxis trotzdem einmal eine doppelte Profilzeile auftauchen, fängt
  `teamReihenfolge()` sie jetzt ab — die Zeile selbst bliebe aber falsch.

---

# 2026-08-27 (Nachmittag) — Ops-Karten Podologie: Kosmetik, dann §302-LEGS

## Woran gearbeitet wurde

Auftrag: die offenen Podologie-Karten im Ops-Dashboard durchgehen, Erledigtes
schliessen, dann von den einfachen zu den grossen Posten arbeiten.

## Karten geschlossen (gegen den Code geprüft, nicht nur behauptet)

Sechs Karten waren bereits erledigt und nur nie geschlossen worden:

- **Slot-Raster 30 → 15 min (P1)** — `server.js:508` `SLOT_STEP_MINUTES = 15`,
  das Frontend sendet bewusst kein `step` mehr.
- **Toter `buffer_time`-Lesezugriff (P2)** — in `booking.js` keine Referenz
  mehr, Spalte auch nicht im Schema.
- **Logo-Upload** — `praxis_logo_url` wird hochgeladen und in
  `abrechnung.routes.js` an jedem Rechnungs-/Quittungsweg als `logoUrl`
  eingespeist. Vollständig verdrahtet.
- **Label „Google Kalender"** — nachgesehen: der Code ist längst richtig.
  Jedes Vorkommen zeigt auf echte `calendar_integrations`, Gmail-Verbindungen
  heissen getrennt „Gmail-Konto". Karte war veraltet.

Dazu zwei kleine Änderungen (Commit `1b4055d`):

- **Kompakter Modus entfernt** — Toggle (`dashboard.html`), `initCompactMode()`
  samt beider Aufrufe (`dashboard.js`), `.compact-mode`-CSS-Block
  (`dashboard.css`). Er war ein Pflaster anstelle echter Responsivität.
- **Rechnungs-Fusszeile klargestellt** — Platzhalter und Hinweis luden vorher
  dazu ein, Steuernummer und Bankverbindung ein zweites Mal einzutragen;
  beide stehen ohnehin automatisch auf jeder Rechnung.

`dashboard.js` **25.355 → 25.323 Zeilen**, Schranke automatisch nachgezogen.

## Der eigentliche Fund: das Tarifkennzeichen war immer falsch

Die Karte sagte „neun PLZ-Präfixe liefern das falsche Bundesland". Das stimmt,
war aber nicht das Problem. **Auch mit perfekter PLZ-Tabelle wäre das Ergebnis
falsch gewesen.**

`gkv-302` hat es gegen die Verträge geprüft, ich habe die Kernaussage selbst
in der Quelle nachgeschlagen: **der LEGS ist keine geografische Angabe.**
Anlage 1 TP5 V21, 5.5.3.3 (SLLA:B, EHE): die Leistungserbringergruppe stellt
die „Zuordnung zur gültigen vertraglichen Vereinbarung" her und ist
„entsprechend den Vergütungsregeln anzugeben". Alle vier §125-Verträge sind
bundeseinheitlich — Tarifbereich überall `00`. Variabel ist der Status des
Leistungserbringers, nicht das Bundesland.

Der Code leitete aus der Praxis-PLZ ab: NRW → `08` → LEGS **`7108000`**.
Den Schlüssel gibt es in keinem Heilmittelvertrag. Im Vertrag steht
(`Podoloji/Leistungen/20250617_Podologie_Anlage_2.txt:49`):

```
7100501 Podologen
7200501 Med. Fußpfleger (gemäß § 10 Abs. 4 bis 6 PodG)
```

Die Kasse findet über den LEGS die Vergütungsvereinbarung; findet sie keine,
setzt sie ab. Der Preflight hat es nicht gemerkt, weil `08` für sich ein
gültiger Tarifbereich ist — geprüft wurde die Form, nicht die Kombination.

### Behoben (Commit `b4936fc`)

- **`api-backend/billing/codes/legs.js` (neu)** — die Vertragstabelle, 16 LEGS,
  Fundstelle je Fachbereich im Dateikopf. Abrechnungscode und Tarifkennzeichen
  kommen jetzt aus einem Stück; dass sie getrennt bestimmt wurden, war die
  Ursache.
- **Nebenbefund:** `abrechnungscodeFuer()` kannte nur Podo und Physio und gab
  **Ergo und Logopädie stillschweigend den Physio-Code**. Ebenfalls behoben.
- **Preflight prüft jetzt die Kombination** gegen die Vertragsliste (`T:01004`),
  nur für Leistungsbereich B — andere Bereiche haben eigene Verträge.
- Der unbenutzt gewordene `bundesland`-Parameter von
  `mapPrescriptionToDtaShape` ist raus.
- `buildTarifkennzeichen` und `BUNDESLAND_TO_TARIFBEREICH` bleiben, aber mit
  ⛔-Hinweis — sonst ruft sie später jemand wieder auf.

### Geprüft

- `legs.test.js` neu, **26 Tests**, darunter Regressionen auf `7108000` und
  `2208000`.
- Billing gesamt **79/79 grün**.
- Preflight einmal laufen lassen: `7108000` → abgewiesen mit `T:01004`,
  `7100501` → geht durch.
- **Die Fixtures trugen den falschen Wert** (`01001`, `02001`) und bestätigten
  damit das Fehlverhalten. Korrigiert — das ist der Grund, warum vier Tests
  zuerst rot wurden.

### Offen

- **Nicht an einer echten Kassendatei geprüft.** Was zählt: eine erzeugte DTA
  öffnen und im EHE-Segment `22:00501` bzw. `71:00501` sehen.
- **`getBundeslandFromPlz` bleibt und ist weiter kaputt.** Es speist jetzt nur
  noch die *Preisermittlung* über `heilmittel_tarif` — dort ist Geografie die
  richtige Achse, Preise werden je Bundesland verhandelt. Die neun doppelten
  Präfixe ziehen also nicht mehr das falsche Tarifkennzeichen, sondern den
  **falschen Preis**. Die Karte wurde entsprechend umgeschrieben, nicht
  geschlossen.
- Ob die Praxis-PLZ für die *Preis*abfrage die richtige Bezugsgrösse ist,
  wurde **nicht** bestätigt.

## Nebenbei aufgefallen — vier neue Ops-Karten

Beim Vermessen des Podologie-Clusters (für die Modulauslagerung) fielen zwei
echte Fehler auf, die **absichtlich nicht** mitgefixt wurden, damit die
Auslagerung eine reine Verschiebung bleibt:

- **`#podBillingContent` sammelt Listener** — das Element ist statisch, nur sein
  `innerHTML` wechselt. Jedes Neuzeichnen hängt einen weiteren Listener an; nach
  N Durchläufen feuert „§302 erstellen" N Requests. Ob der Endpunkt idempotent
  ist, wurde **nicht** geprüft. → Karte, `hoch`.
- **Podologie liest `verordnungen` ohne `bizScope`** — `module/fussbefund.js`
  kommentiert an gleicher Stelle „bizScope ist Pflicht: ohne sie zeigte die
  Karte Patienten ALLER Standorte". Standort-Leck-Verdacht, in einer
  Mehr-Standort-Praxis zu prüfen. → Karte, `hoch`.
- **`tools/check-namen.sh` startet einen `sha256sum`-Prozess pro Wort** — unter
  Windows dauert ein Commit dadurch Minuten (zweimal ins Timeout gelaufen). Das
  Tor arbeitet korrekt, nur langsam; die Gefahr ist, dass man sich
  `SKIP_NAME_GATE=1` angewöhnt. → Karte, `normal`.
- **Toter Code:** `WAGNER_LEVELS`, `FUSS_BEFUNDE`, `_icdMatchesDgRule`, ~25
  Zeilen. → Karte, `niedrig`.

## Podologie-Cluster ausgelagert (Commit `0952e3c`)

Erstes Ziel der Einkreisung (Konsey 13.08.2026). Reine Verschiebung —
Verhalten unverändert, die bekannten Fehler im Block wurden bewusst
mitgenommen statt nebenbei behoben, damit der Diff prüfbar bleibt.

Zwei Dateien statt einer: `module/diagnosegruppen-regeln.js` (die Regeltabelle
aus `diagnosegruppen` ist fachbereichsneutral — `_wireDgIcdPair` liest sie auch
für Rezept-Formular und Rezept-Scan; läge sie im Podologie-Modul, zöge das
Rezept-Formular seine Regeln aus der „Podologie-Abrechnung") und
`module/podologie-abrechnung.js` für den Rest. Anbindung nach dem Muster von
`module/fussbefund.js`.

**`builder` lief in das Sitzungslimit** und kam nicht mehr zum Commit; die
Arbeit war fertig und lag gestaged im Baum. Prüfung und Commit hier.

### Zwei Stellen, an denen es lautlos hätte brechen können

- **`leads` und `services` als Getter.** `leadsCache` und `ownerServices` sind
  modulweite `let` und werden neu zugewiesen. Als Wert übergeben hätte das Modul
  die beim ersten Aufruf leere Liste dauerhaft festgehalten — Patientensuche und
  Rechnungsbrücke hätten einfach nichts gefunden, ohne Fehlermeldung, ohne
  roten Test. Ist korrekt als `() => leadsCache` übergeben.
- **`rechnungAusVerordnung` las `_podState.verordnungen`.** Die Funktion musste
  in `dashboard.js` bleiben (sie weist den `inv*`-`let`s zu, Import-Bindungen
  sind schreibgeschützt), `_podState` wanderte aber mit. Gelöst über einen neuen
  Export `getPodVerordnung()`. Diese Abhängigkeit stand nicht im Auftrag —
  `builder` hat sie selbst gefunden.

### Geprüft

- Syntax aller drei Dateien (`node --check`).
- **Gegen den Ausgangsblock aus `HEAD` verglichen:** alle 18 Funktionsnamen und
  alle 52 DOM-Ids wiedergefunden. Einziger „Fehltreffer" `invPatientSelect` —
  gehört zu `rechnungAusVerordnung` und liegt richtigerweise in `dashboard.js`.
- Die 78 `pod_*`-Einträge bleiben im Wörterbuch; der `document`-Listener steht
  im Modulrumpf, nicht in `mount()`; das Modul ruft `attachDiagnoseSearch`
  nicht auf (sonst doppelte Bindung und `strict` zweimal angewandt);
  `verordnung-podo.js` unberührt.
- `api-backend` 79/79 grün.
- `dashboard.js` **25.323 → 24.120 Zeilen** (−1.203), Schranke nachgezogen.
  Funktionskarte neu erzeugt (1701 Funktionen / 159 Dateien).
- Cache-Busting `dashboard.js?v=20260827c`.

### Offen

**Nicht im Browser gesehen — das ist der Punkt, der zählt.** Der Durchlauf:
Podologie-Panel öffnen, Verordnung anlegen und bearbeiten, Behandlung speichern,
aus der Verordnungszeile eine Rechnung starten, §302-Ansicht öffnen. Eine reine
Verschiebung kann Syntax und Tests bestehen und trotzdem eine Verdrahtung
verlieren, die erst beim Klick auffällt.


---

## Repo-Inventur: Struktur kartieren und Abweichungen beheben

Auftrag: alle Dateien durchgehen, kartieren was noch nicht kartiert ist, und
beheben was der eigenen Projektstruktur widerspricht — ohne etwas kaputtzumachen.
Drei Fachagenten liefen dazu parallel (`legal-de`, `fonksiyon-ustasi`,
Klassifizierung der unkartierten Ordner).

### Erledigt

**1. Zwei Tore, die nicht schützten**

- `tools/funktionskarte.mjs --check` war seit dem ersten Tag **dokumentiert aber
  nie implementiert**. Das Flag wurde stillschweigend ignoriert: das Skript
  schrieb in jedem Fall und gab in jedem Fall 0 zurück. Wer „ist die Karte
  frisch?" fragte, zerstörte die Antwort beim Fragen und bekam immer „ja".
  Jetzt schreibt `--check` nichts, vergleicht gegen die Platte und beendet mit 1
  bei Drift. Das Erzeugungsdatum wird aus dem Vergleich genommen — ein neuer Tag
  ist kein Drift. Gegengeprüft: Positivlauf 0, künstlicher Drift 1, keine Datei
  angefasst (mtime verglichen). Das neue Tor hat noch in derselben Sitzung
  gegriffen, als fünf Kommentarzeilen in `stripe-live-setup.js` die
  Zeilennummern verschoben.
- `tools/check-namen.sh` öffnete **pro eindeutigem Wort einen eigenen
  `sha256sum`-Prozess**. Bei diesem Commit waren das 805 Prozesse, gemessen über
  zwei Minuten. Ein Tor, das so lange braucht, prüft nicht — es erzieht zu
  `SKIP_NAME_GATE=1`, also genau zu dem, was es verhindern soll. Ersetzt durch
  einen einzigen Node-Prozess: **6,9 s statt >120 s**. Wortauftrennung
  identisch, Umlaute geprüft (Positiv- und Negativtest mit temporärem Test-Hash,
  Hash-Liste danach wiederhergestellt).

**2. Veröffentlichungsfläche — `.vercelignore` war unvollständig**

Der Durchlauf von 2026-06-03 hatte Datei-*endungen* gesperrt (`*.md`, `*.sql`,
`*.py`). Jeder Ordner, der danach dazukam, ist durch dieselbe Lücke gerutscht.
Live gegen `praxura.de` geprüft, alle **HTTP 200**:

```
/ui-audit/after/booking__iphone-se.png
/verordnung%20rezept/heilmittel-diagnoseliste.txt
/onprem/supabase-docker/docker-compose.yml
/tools/.namen-hashes
/ai%20chatbot%20proje/index.html
/funktionen/INDEX.json          (990 KB — vollständige Funktionskarte der App)
/module/kvnr.test.js
/dashboard.js.bak
/lib/supabase.js
```

`.vercelignore` ist jetzt **ordnerweise** geführt. Ausgeliefert werden noch 284
von 1212 verfolgten Dateien. Verifiziert mit einem Skript, das jede
ausgelieferte HTML/JS/CSS nach lokalen Asset-Verweisen absucht und gegen die
Ignore-Regeln prüft: **keine ausgelieferte Seite verlangt eine ausgeschlossene
Datei**, 42/42 kritische Assets weiterhin dabei.

> Beim Schreiben zuerst falsch gemacht: Pfade mit Leerzeichen in Anführungszeichen
> gesetzt. `.vercelignore` folgt gitignore-Syntax, dort sind Anführungszeichen
> **literale Zeichen** — die Regeln hätten nie gegriffen. Korrigiert.

**3. Drittanbieter im Browser (Befund `legal-de`)**

- **GA4 aus `booking.html` entfernt.** Lief dort ohne Consent, mit
  `cookie_flags: SameSite=None;Secure`, auf der Seite, auf der *Patienten* einen
  Therapietermin buchen. `legal-de` hat meine Einschätzung korrigiert: geladen
  wurde es nie, weil `www.googletagmanager.com` nicht in der CSP von
  `vercel.json` steht — heute also kein § 25 TDDDG-Verstoß. Aber im
  On-Prem-Image gibt es diese CSP nicht, dort wäre es losgegangen. Dazu
  Art. 28 Abs. 10: auf dieser Seite sind wir Auftragsverarbeiter der Praxis, ein
  eigener Messzweck macht uns für diesen Teil zum Verantwortlichen. Umami kommt
  dort **auch nicht** hin; Reichweite wird serverseitig über `booking/create`
  gezählt.
- **Cropper.js lokalisiert.** `dashboard.html` lud es von
  `cdnjs.cloudflare.com` — in der eingeloggten App, in der Patientendaten im DOM
  stehen. Jetzt `vendor/cropperjs/`. Integrität nicht geglaubt sondern geprüft:
  sha256 gegen unpkg (npm-Artefakt) verglichen, beide Dateien identisch.
  MIT-Header erhalten. `cdnjs.cloudflare.com` aus `script-src` **und**
  `style-src` entfernt — ohne das wäre die Lokalisierung nur auf dem Papier.
- **CSP entrümpelt:** `fonts.googleapis.com`, `fonts.gstatic.com` (Fonts sind
  seit 08/2026 self-hosted, kein Verweis mehr im Code) und `youtube.com` /
  `youtube-nocookie.com` (nirgends verwendet) gestrichen. Damit ist der
  Altposten „Google Fonts CDN" faktisch geschlossen.

**4. Behauptungen, die nicht mehr stimmten**

| Wo | Stand | Tatsächlich |
|---|---|---|
| `CLAUDE.md` | `dashboard.js` 26.857 Zeilen | 24.120 |
| `CLAUDE.md` · `funktionen/README.md` · `funktionskarte.mjs` | 26.000+ Zeilen, 1300+ Funktionen | 24.000+, 1701 |
| `CLAUDE.md` | erstes Belagerungsziel Podologie `24030-25696` | am 27.08. erledigt |
| `CLAUDE.md` | 78 Tabellen · 153 Policies | 81 · 156 (Kopf des Dumps) |
| `CLAUDE.md` · `db/README.md` | `supabase/migrations/` „5 Dateien" | 10 |
| `CLAUDE.md` | `server.js` „~35 route" | 43 direkt + 32 Sub-Router = 75 |
| `CLAUDE.md` | Web (Vercel) → `infinitymade.de` | `praxura.de` |
| `komponenten.html:548` | Patientenfeld „noch dreifach gebaut" | zusammengeführt, alle über `patient-suche.js` |
| `komponenten.html:900` | `lib/supabase.js` sei „im Backend" | liegt im **toten** Wurzel-`lib/` |
| `archive/README.md:61` | Chatbot-Projekt archiviert | nur die Doku, `index.html` blieb im Wurzelverzeichnis |
| `database_v36_*.sql` | Kopfzeile nennt sich `v33` | Dateiname ist `v36` |

**5. Totes Markup entfernt**

`dashboard.html:3289-3291` hatte noch ein `<datalist id="rzPatientDatalist">` mit
`list=`-Attribut aus der Zeit vor `patient-suche.js`. Zur Laufzeit entschärft
(`patient-suche.js:150` entfernt das Attribut), im Markup aber vorhanden — und
`komponenten.html` führte es als offene Dublette. Beides bereinigt.

**6. `stripe-live-setup.js` läuft nicht**

`package.json` hat `"type": "module"`, die Datei nutzt `require()` →
`ReferenceError`. Funktionierend ist `stripe-live-setup.cjs`; die beiden
unterscheiden sich in **einem Wort** (Fehlermeldung DE/TR). `CLAUDE.md` und
`STRIPE_SETUP.md` zeigten auf die kaputte. Doku korrigiert, Warnkopf in die
kaputte Datei gesetzt. **Löschen ist eine Dubletten-Entscheidung und gehört dem
Nutzer** — nicht still zusammengeführt.

**7. Kartierung**

`CLAUDE.md` → Projektstruktur um `module/`, `vendor/`, `tools/`, `.githooks/`,
`funktionen/`, `fortschritte/`, `konsey/`, `compliance/`, `blog/`, `ops/`
ergänzt, plus eine Tabelle „Haritasız değil, sınıflandırılmış" für die 20+
Ordner, die bisher nirgends beschrieben waren (`ui-audit/`, `mobile-audit/`,
`competitor-research/`, `onprem/`, `.planning/` vs `.plans/`, Wurzel-`lib/`,
`ai chatbot proje/`, die 25 `*.py`, die 39 `database_v*.sql`, die 17 Wurzel-`.md`).
Jede Zeile mit Status: CANLI / REFERANS / ARŞİVLİK / ATIK.

### Geprüft

- `npm test` 158/158 grün — vor und nach jedem Schritt.
- `npm run typecheck` ohne Befund.
- `vercel.json` nach dem Eingriff als JSON geparst.
- `node tools/funktionskarte.mjs --check` grün, Karte neu erzeugt.
- Größentor `tools/check-dashboard-size.sh` grün (`dashboard.js` unberührt).
- Namenstor grün, plus Negativ- und Umlauttest.
- Deploy-Menge per Skript gegen die Ignore-Regeln verifiziert.
- Erreichbarkeit auf `praxura.de` per HEAD-Anfragen belegt (nur lesend).

### Offen / blockiert

**Nicht im Browser gesehen.** Zwei Änderungen berühren ausgelieferte Seiten und
gehören vor dem Merge in einen echten Klickdurchlauf:

1. `dashboard.html` — Logo- und Profilbild-Zuschnitt. Cropper kommt jetzt aus
   `vendor/`; dass die Datei byteidentisch ist, ist geprüft, dass der Zuschnitt
   im Browser aufgeht, **nicht**.
2. `booking.html` — öffnet die Buchungsseite noch ohne den entfernten
   gtag-Block sauber?

Beides ist ein Fall für `canli-test`, nachdem der Deploy durch ist.

### Was noch getan werden muss — Entscheidungen beim Nutzer

Diese Punkte sind **bewusst nicht** angefasst worden, weil sie eine Entscheidung
brauchen, keine Mechanik:

1. **Fußbefund wird von zwei Stellen geschrieben** (Befund `fonksiyon-ustasi`).
   `dashboard.js:9309 saveFussbefund()` schreibt **ohne** `booking_id` und
   `uebernommen_von`, `module/fussbefund.js:577 speichern()` mit. Folge: ein aus
   der Patientenkarte gespeicherter Befund erscheint im Modul als „Ohne Termin"
   und der Termin bekommt kein `✓ Befund`. Rückstand der Migration vom 14.08.
2. **Zuzahlungsrechnung doppelt** — `dashboard.js:20001` und `:20315`, ~14
   Zeilen identisch. Heute synchron; der Kommentar bei `:20329` belegt, dass
   genau dieser Fehler schon einmal passiert ist.
3. **`verordnungen` hat kein `business_id`** — Physio-Verordnungen lassen sich
   nach Standort trennen, Podologie-Verordnungen nicht. Schema-/Produktfrage,
   kein Codefehler → Konsey.
4. **Wurzel-`lib/` ist verwaist** — `business.js`, `plan.js`, `supabase.js`
   werden von nichts importiert, nennen sich aber „single source of truth" und
   liefern 20 Phantomfunktionen an die Funktionskarte. Löschen oder wiederbeleben?
5. **Wistia** (`index.html:1202/1211/1220`) — `legal-de` stuft das als den
   einzigen Befund ein, der heute wirklich Daten abfließen lässt: der Player
   liest beim Start localStorage → § 25 Abs. 1 TDDDG, Einwilligung nötig; in
   `datenschutz.html` nicht genannt (Art. 13 lit. e). Fix: Zwei-Klick-Lösung
   oder Videos selbst ausliefern.
6. **21 i18n-Schlüssel fehlen in EN und TR** (`pod_*`, `fuss_*`, `btn_add_leave`,
   `b2b_ai_welcome`) — dort erscheint der Schlüsselname statt Text. Der saubere
   Fix wäre, `T` (Zeilen 69-661) nach `module/i18n.js` zu ziehen: das befolgt
   „neuer Code in neue Datei", schrumpft `dashboard.js` um weitere ~590 Zeilen
   und macht Platz für die fehlenden Schlüssel. Ein 590-Zeilen-Umzug mitten in
   der laufenden Belagerung gehört aber freigegeben, nicht nebenbei gemacht.
7. **`services`-RLS-Policy** — der Dump markiert sie selbst als fehlerhaft
   (`db/SCHEMA-RLS.sql:136`, `USING (auth.uid() = business_id)`). Gehört in den
   Sicherheitsdurchlauf.
8. **Sentry fehlt in `compliance/VVT.md`** als Auftragsverarbeiter, obwohl
   `dpa.html:89` und `datenschutz.html:113` ihn nennen (Art. 30 Abs. 1 lit. d).
9. `ai chatbot proje/index.html` — Archivumzug zu Ende führen.
   `write_icd.py` ist 0 Byte.

### Commits

Noch keiner — alles liegt im Arbeitsbaum.
