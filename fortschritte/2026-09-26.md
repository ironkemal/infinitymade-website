# 2026-09-26

## Woran gearbeitet wurde

Ops #304, Nachtrag: Fehlermeldung „E11.74, L60.0 setzt die Diagnosegruppe auf DF" (canli-test-Szenarien d/d2).
`a7b1ff3` (25.09.) hatte die Logik in `icd-dg-match.js` schon richtig — der Fehler lag in einem zweiten Schreiber.
Danach auf Wunsch genauer analysiert, die Nachbarlücken mit geschlossen und die offenen Entscheidungen selbst
getroffen (Melih: „entscheide du"). Befragt: `fonksiyon-ustasi` (Bestand, Umzugsfolgen), `podoloji` (zweites
ICD-Feld, Verschieben statt Warnen), `gkv-302` (≥3 Kodes, Reihenfolge, Preflight).

## Erledigt

- **Ursache d/d2:** `module/verordnung-podo.js` `dgAuswahlEingrenzen` schrieb DF ohne die Marke `dataset.dgAuto`.
  Kam seine Regelabfrage vor der von `dashboard.js` zurück, hielt die Verdrahtung DF für ärztliche Angabe und nahm
  es nach `+ L60.0` nie zurück — auch Szenario c (DG geleert) war betroffen.
- **Entscheidung: ein einziger DG-Schreiber.** `rzDg` schreibt nur noch `module/icd-dg-verdrahtung.js`
  (`verdrahteIcdDg`, vorher `_wireDgIcdPair` in `dashboard.js`, dort −132 Zeilen). `verordnung-podo.js` engt nur
  noch die Auswahlliste ein. Die zwei Gründe für den zweiten Schreiber sind in der Verdrahtung gelöst: DG geleert →
  sofort neuer Vorschlag; Fachbereich = angekreuzter der Maske (bei Mandant `praxis` war die Automatik stumm).
- **Beide ICD-Felder zählen** (`rzIcd` + `rzIcd2`); **zwei Kodes im ersten Feld → zweiter wandert nach `rzIcd2`**
  (auch mit Leerzeichen getrennt). Grund: `icd10` hält einen Kode (`preflight.js` V:01002).
- **Entscheidung ≥3 Kodes: warnen, nicht sperren** — `gkv-302`: Anlage 3 k „eines oder mehrerer ICD-10-Schlüssel",
  Anlage 1 V21 DIA 1-n; die Verordnung ist gültig, eine Sperre bringt kein Geld. Hinweis nennt den Diagnosetext.
- **Entscheidung: ein Hinweis statt zwei.** Die rote „passt nicht / zulässig"-Zeile der Podologie-Box ist weg; der
  Hinweis am ICD-Feld nennt dafür die passenden Gruppen mit.
- **Preflight-Lücke** (`gkv-302`): `icd10` leer + `icd10_2` gefüllt → V:01002 auf leerem Feld, einziger Kode nie
  geprüft. Behoben, `preflight.test.js` +1 (rot auf altem Code).
- Tote Übergabe `_wireDgIcdPair` in `podoCtx()` entfernt; Umkreuzen des Fachbereichs wertet den ICD neu aus.
- i18n `pod_icd_nach_feld2`, `pod_icd_je_feld` (de/en/tr); `?v=20260926a` für `dashboard.js` und
  `verordnung-podo.js` (beide Importstellen gleich).
- Tests: `module/icd-dg-verdrahtung.test.js` (19, per Mutationsprobe scharf) · **neue Browser-Probe
  `tools/browser-probe/icd-dg-probe`** — echte Muster-13-Maske aus `dashboard.html`, echter Podo-Teil, Tastatur + Tab
  (echtes `change` beim Verlassen): 15/15 · in `npm run probe` aufgenommen.
- Doku: `funktionen/README.md` (Niyet), `canli-test/REGISTER.md`, `Podoloji/PRODUKT-ENTSCHEIDUNGEN.md`,
  `wissensbank/SPEC-RULES.md`, Kommentare in `diagnosegruppen-regeln.js`/`podologie-abrechnung.js`; Funktionskarte.

Geprüft: `node --test module/*.test.js` alle grün · Backend `billing`+`ai`: grün bis auf 3 Verschlüsselungs-Tests
(`upload-signed-verschluesselung`, `itsg-trust-anchor`, `verschluesselung`), die **auch ohne diese Änderung**
fehlschlagen — nicht untersucht · Browser-Probes modul/verordnung-maske/podo-maske/podo-einheiten/render/icd-dg
ohne ✗ (`/api/config` 500 = lokal fehlende `.env.local`).

## Offen / blockiert

- **Live ungetestet** — keine angemeldete Browser-Sitzung / keine `PRAXURA_QA_*`-Zugangsdaten. Die Browser-Probe
  läuft ohne Login mit Attrappe statt Datenbank.
- **3.+ ICD-Kode hat keinen Speicherort** (`icd10`/`icd10_2`); `gkv-302`: alle Kodes gehören in die DIA-Segmente.
  → Ops-Karte, `db-ustasi` (Array-Spalte oder Diagnosetext).
- UI1/UI2 mit L60.0 **plus** weiterem Kode: „Korrektur erforderlich"? Normtext mehrdeutig (`gkv-302`).
- Drei Verschlüsselungs-Tests im Backend rot (vorbestehend) → Kemal.
- Szenarien h/i aus der Meldung sind im Repo nirgends definiert.

## Was noch getan werden muss

- Angemeldeter Durchgang d, d2, Aufteilen, Fachbereich umkreuzen (`canli-test`).
- Ops-Karten: 3.+ ICD-Speicherort; Verschlüsselungs-Tests.

## Commits

- siehe `git log` vom 26.09.2026 (Ops #304 Nachtrag)
