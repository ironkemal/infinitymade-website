# 2026-08-17

## Woran gearbeitet wurde

Bedienfehler aller Auswahlfelder: stand in einem Suchfeld bereits ein Wert
(Patient, ICD, Diagnosegruppe, Heilmittel, Arzt, Krankenkasse), musste man ihn
erst Zeichen für Zeichen löschen, bevor wieder eine Liste erschien. Grund war in
allen Feldern derselbe: beim Fokus wurde mit dem **fertigen Feldinhalt** gesucht
("Max Mustermann · 01.01.1990", "L60.0 – Unguis incarnatus"), und darauf passt
naturgemäss nichts — das Dropdown blieb wortlos zu.

## Erledigt

**1. `katalog-suche.js` → `attachAutocomplete` (die gemeinsame Dropdown-Mechanik).**
Deckt ICD-10, Diagnosegruppen, Heilmittel, Ärzte (`arzt-suche.js`) und
Krankenkassen (`module/krankenkasse-suche.js`) ab — alle hängen an dieser einen
Implementierung.
- Neu `oeffneAuswahl()`: der Klick in ein gefülltes Feld zeigt die Auswahlliste
  (leere Abfrage) statt nach dem fertigen Text zu suchen.
- Rückfall `codeTeil()`: liefert die leere Abfrage nichts (ICD-Felder — 16.905
  Kodes werden ohne Suchbegriff bewusst nicht gelistet), wird mit dem Kode-Teil
  des Wertes gesucht ("L60.0"), man sieht also die Nachbarn des gewählten Kodes.
- Der alte Wert wird beim Fokus **markiert**; der erste Tastendruck ersetzt ihn.
  `mouseup`-Guard, weil der Fokus-Klick die Markierung sonst sofort aufhebt.
- Zweiter Klick ins bereits fokussierte Feld (oder nach Escape) öffnet erneut.
- `ignoriereEingabe`: das von `selectItem` selbst ausgelöste `input`-Ereignis
  wirft die Suche nicht mehr an. Fremde Zuhörer bekommen es weiterhin.
- `multi`-Felder (Komma-Liste) bleiben unverändert — dort wäre "alles markieren"
  falsch.

**2. `patient-suche.js`** — dieselbe Änderung für die gemeinsame Patientensuche.
Fokus zeigt die Liste (`render('')`), markiert den alten Namen; Klick/Escape wie oben.

**3. `dashboard.js` → `initBkCustomerAutocomplete()` (Termin-Dialog).**
Dieses Feld war die *Vorlage* für `patient-suche.js`, wurde aber als einzige
Stelle nie umgestellt und trug den Fehler weiter. Jetzt hängt es am gemeinsamen
Modul; der Nachbau (`renderList`, eigene Pfeiltasten-/Klick-/Aussenklick-Logik,
~45 Zeilen) ist entfallen. `dashboard.js` **25.555 → 25.500 Zeilen**.
Beim erneuten Öffnen des Dialogs stösst `input._patientSearchApi?.refresh()` den
Bestand an, weil `loadBkLeads()` `window.bkAllLeads` neu zuweist.

**4. `module/fussbefund.js`** — eigener Patienten-Dropdown (nicht am Modul).
Minimal auf dasselbe Verhalten gebracht (Klick zeigt Liste, Wert markiert).

**5. Cache-Busting** auf `?v=20260817` für katalog-suche · patient-suche ·
arzt-suche · krankenkasse-suche · fussbefund, inkl. aller Importstellen
(`dashboard.js`, `booking-request.html`, `komponenten.html`, `module/arzt-register.js`).

**6. Funktionskarte** neu erzeugt (`node tools/funktionskarte.mjs`).

---

# Zweiter Strang: Termine ohne Patientenbezug (Terminzettel + Fußbefund)

## Woran gearbeitet wurde

Zwei Beobachtungen aus dem Betrieb (Kemal): der Fußbefund-Knopf im
Termin-Seitenbereich erscheint beim einen Patienten und beim anderen nicht, und
„Termine drucken" druckt statt aller Termine nur den einen angeklickten.

Beides ist **dieselbe Ursache**, und sie liegt nicht in der Anzeige: die
Serientermin-Endpunkte schrieben `bookings.lead_id` überhaupt nicht. In keinem
der drei Insert-Payloads (`/booking/batch-create`,
`/booking/batch-create-explicit`, `/booking/manual-create`) stand die Spalte.
Jeder per Serie vergebene Termin war damit von der Patientenakte abgeschnitten.

Messung vor der Korrektur: **50 von 274 Terminen ohne `lead_id`, bei den
kommenden 17 von 18.** Der einzige verknüpfte kommende Termin war der einzige
von Hand einzeln angelegte.

Die Sichtbarkeitsregel des Fußbefund-Knopfes (`module/fussbefund.js:1249`,
Podologie + `lead_id` + Modul sichtbar) ist damit **richtig** und wurde nicht
angefasst — sie zeigte nur wahrheitsgemäss an, dass kein Patient am Termin hing.

## Erledigt

**1. Datenkorrektur — 50 Termine nachverknüpft.**
Alle 50 waren eindeutig: sechs Namen, je genau ein passender Patient, kein
mehrdeutiger und kein unauflösbarer Fall. Erst als `SELECT` geprüft und
vorgelegt, dann mit `UPDATE … FROM` gesetzt (nur `lead_id`, keine andere
Spalte). Danach: 0 von 274 ohne Bezug.

**2. Ursache geschlossen — `leadId` geht durch die ganze Kette.**
- `api-backend/server.js`: neue Hilfsfunktion `pruefeLeadId(leadId, ownerId)`;
  die ID kommt aus dem Body, wird also gegen den Mandanten geprüft, sonst
  hängte ein manipulierter Aufruf Termine an eine fremde Akte. Eingebaut in
  alle drei Endpunkte.
- `dashboard.js`: beide Serien-Aufrufe senden den Patienten jetzt mit
  (`leadId: custId` bzw. `custIdAtConfirm || _physioFlow.patient_id`). Beim
  KI-Weg wurde der Patient bisher erst **nach** dem Anlegen ermittelt — für die
  Bestätigungsmail, nicht für die Terminzeile.

**3. Neu `module/termin-patient-bezug.js` — „welcher Patient gehört zu diesem
Termin?" an einer Stelle.**
Laut `fonksiyon-ustasi` gab es diese Auflösung fünfmal (`dashboard.js:3608`,
`4156`, `4471`, `4580`, `5300`) mit fünf verschiedenen Regeln, keine davon nutzte
`customer_phone_normalized`; die reifste Fassung stand in
`module/rechnung-editor.js:45-75`. Deren Regeln stehen jetzt hier:
- `parseNameMitGeburt()` — die Umkehrung von `displayNameWithBirth()`. Das
  Zurücklesen lag viermal inline als `split('·')[0]` und warf das Geburtsdatum
  weg; genau daran scheiterte der Zettel („Klaus Fischer · 1972-07-23" ≠
  „Klaus Fischer").
- `findeLeadIdZuTermin()` — `lead_id` → Telefon (ziffernnormalisiert) → Name,
  bei Namensdopplung entscheidet das Geburtsdatum. **Mehrdeutig heisst `null`:**
  ein falsch zugeordneter Fußbefund ist ein Fehleintrag in einer fremden Akte.
- `ladeKommendeTermineDesPatienten()` — die Datenquelle des Zettels.
- 13 Tests (`module/termin-patient-bezug.test.js`), alle grün; Gesamtstand
  111/111.
- Die fünf Altkopien wurden **nicht** angefasst — sie wandern nach dem
  Belagerungsprinzip hierher, wenn sie ohnehin angefasst werden.

**4. Der Zettel druckt wieder alle kommenden Termine.**
`dashboard.js` (Terminzettel-Knopf) nutzt jetzt die beiden Modulfunktionen; der
Textvergleich auf `customer_name` ist weg. `dashboard.js` ist dabei
**geschrumpft** (Handler 48 → 39 Zeilen).

**5. Der Knopf steht jetzt, wo die Termine stehen.**
„Termine drucken (A5)" sass zwischen „Fußbefund" und „Verschieben / Bearbeiten /
Löschen" und ging dort unter. Er steht jetzt direkt über der Verordnungs- und
Termineliste — bewusst **über** `#bkRxSessionsPanel` und nicht darin: das Panel
ist ausgeblendet, solange keine Verordnung am Termin hängt, im Panel wäre der
Knopf beim Selbstzahler spurlos verschwunden.

## Offen / blockiert (zweiter Strang)

- **Nicht committet, nicht deployt, nicht im Browser geprüft.** Der Arbeitsbaum
  enthält den ersten Strang (oben) unfertig; ein gemeinsamer Commit hätte fremde
  halbe Arbeit mitgenommen.
- `funktionen/INDEX.*` wurde neu erzeugt und enthält deshalb auch die noch
  unfertigen Module des ersten Strangs (`belegnummer`, `verordnung-detail`,
  `verordnung-liste`).
- Verbleibendes Loch: der Termin-Dialog in **`kalender.js`** hat gar keine
  Patientenauswahl, er tippt den Namen. Der Endpunkt nimmt `leadId` jetzt
  entgegen, der Aufrufer hat aber keine ID zu senden. Nächste Quelle
  beziehungsloser Termine, wenn diese Maske benutzt wird.

## Offen / blockiert

- **Ungeprüft im Browser.** Es wurde nur Syntax geprüft (`node --check` auf allen
  geänderten Modulen) und der Code gelesen. Live getestet wurde nichts.
  Zu prüfen: Termin-Dialog (Patient wechseln ohne Löschen), Verordnungsmaske
  (ICD/Diagnosegruppe/Heilmittel/Arzt), Podologie-Abrechnung, Fussbefund.
- Der `mouseup`-`preventDefault`-Griff hält die Markierung auf Desktop-Browsern;
  auf Touch-Geräten kann sie beim Antippen verloren gehen. Nicht kritisch — dort
  öffnet der Klick trotzdem die Liste.
- Der Gruppen-Teilnehmer-Picker im Termin-Dialog (`dashboard.js`, ~Z. 5150) ist
  weiterhin handgeschrieben. Er leert sein Feld nach jeder Auswahl, hat das
  Problem also nicht; die Zusammenführung bleibt Restschuld.
- `module/fussbefund.js` sollte mittelfristig auf `attachPatientSearch` umziehen
  (Hausregel: keine eigenen Patienten-Picker). Heute bewusst nur die Bedienung
  geradegezogen, kein Umbau.

## Was noch getan werden muss

- Push → Vercel-Deploy, danach die vier Masken oben in der Live-Umgebung
  durchklicken (Kemal).

## Commits

Noch nicht committet.

---

# Terminkalender-Karte, Punkt 4: die Lücke bei Drag & Drop geschlossen

*(Eigene Sitzung, parallel zur Autocomplete-Arbeit oben. Ops-Karte „Podoloji · Hoch",
Nachtrag zum 16.08.)*

## Ausgangslage

Die acht Punkte der Karte sind am 16.08. umgesetzt und committet (`182da8e`). Der
Fortschrittseintrag von damals nennt unter „Offen" eine Lücke bei Punkt 4:

> Die Frequenzprüfung hängt am Termin-Fenster. Wird eine Sitzung per Drag & Drop aus der
> Seitenleiste in den Kalender gezogen (`handleRxSessionDropToModal`), ist
> `window._bkGewaehlteRx` nicht gesetzt und es wird **nicht** geprüft.

Nachgeprüft: die Lücke bestand weiterhin — aber aus einem anderen Grund als notiert.
Die Speicherroutine liest inzwischen nicht mehr `_bkGewaehlteRx`, sondern das versteckte
Feld `bkSelectedRxId` und lädt die Verordnung notfalls nach (`dashboard.js`, Frequenzblock).
Der Kommentar dort behauptete, beim Ziehen sei „das Feld gesetzt". Das stimmte nicht:
`prefillBookingModal()` **leert** `bkSelectedRxId`, und `handleRxSessionDropToModal()` rief
es unmittelbar danach auf, ohne es wieder zu füllen. `_pendingRxSession` allein genügt
nicht — daran hängt nur die Verknüpfung der Sitzungszeile, die Prüfung liest es nicht.

Wirkung: **der meistbenutzte Weg zum Folgetermin war der einzige ungeprüfte.** Über die
Verordnungskarte kam die Warnung, über das Ziehen aus dem Seitenbereich nie.

## Geändert (beides in `handleRxSessionDropToModal`, `dashboard.js`)

1. **`bkSelectedRxId` wird beim Ablegen gesetzt.** Damit greift die vorhandene Prüfung
   ohne weitere Änderung; die Warnung bleibt eine Rückfrage, „Weiter" legt den Termin an
   (Nausad, 12.08.2026). Kein neuer Code in `module/frequenz-pruefung.js`.
2. **Dienstleistungsfeld wird auch auf diesem Weg ausgeblendet** (Punkt 1 der Karte, bisher
   nur auf dem Kartenweg umgesetzt), samt Hinweistext „Leistung aus der Verordnung: …".
   Findet der Katalog keine passende Leistung, bleibt das Feld sichtbar — `service_id` ist
   Pflicht, und eine versteckte Pflichtangabe lässt sich nicht ausfüllen.

`dashboard.html` → `dashboard.js?v=20260817c`.

## Nebenwirkung, bewusst so gelassen

Mit gesetztem `bkSelectedRxId` speichert der Termin jetzt `rezeptart = 'kassen'` statt
`null`. Das ist auf dem Kartenweg seit jeher so; der Ziehweg wird damit nur gleichgezogen.
**Aber:** `prescriptions.rezept_typ` kennt laut `db/SCHEMA.sql` auch `privat`, und beide
Wege schreiben unabhängig davon `'kassen'`. Das ist ein **vorbestehender** Fehler des
Kartenwegs, nicht durch diese Änderung entstanden — deshalb hier nicht stillschweigend
mitrepariert. Gehört vor einer Korrektur an `gkv-302` (Abrechnungssemantik).

## Prüfstand

- `node --check dashboard.js` — sauber
- `npm test` — **111/111 grün**
- `npm run typecheck` — sauber
- `tools/check-dashboard-size.sh` — grün (25.464 Zeilen, Basis 25.555)
- **Am Bildschirm nicht geprüft.** Der Live-Durchlauf braucht eine echte Verordnung mit
  `frequenz`-Text und eine offene Sitzung, die man aus dem Seitenbereich zieht.

## Offen

- Damit sind alle acht Punkte der Karte im Code umgesetzt; **ungeprüft bleibt der
  Bildschirm** — das gilt unverändert aus dem 16.08.-Eintrag für alle acht.
- Die zum 16.08. notierte Aufgabe „`gkv-302` zu den Toleranzen befragen" ist erledigt:
  `module/frequenz-pruefung.js` belegt die Schwellen inzwischen mit dem
  Fragen-Antworten-Katalog Podologie Nr. 11 (2 Werktage) und Nr. 36, nicht mehr mit
  geschätzten 80 %.
- `rezeptart`-Frage oben (privat vs. kassen) als Ops-Karte anlegen.

## Achtung: zwei Sitzungen in einer Datei

Während dieser Arbeit hat eine zweite Sitzung `dashboard.js` weiter bearbeitet — die Datei
schrumpfte zwischen zwei Bearbeitungsschritten um rund 60 Zeilen. Beide Änderungen liegen
unverändert nebeneinander im Arbeitsbaum. **Nicht committet**, bewusst: der Baum enthält
zusätzlich die Autocomplete-Arbeit oben sowie mehrere neue Module. Vor dem Commit gehört
beides zusammen durchgesehen (gleiche Konstellation wie am 14./15.08., siehe dortigen
Eintrag).

---

## Die Verordnung bekommt ein Gesicht: Nummer in der Liste, Klick zum Nachlesen

Zwei Befunde von Kemal, beide an derselben Seite:

1. In der Verordnungsliste stand nur „Kemal Demir". Welche seiner Verordnungen
   die Zeile meint, war nicht zu sehen — auch nicht aus zwei Metern Abstand,
   was der eigentliche Punkt ist.
2. Eine gespeicherte Verordnung liess sich **nicht mehr aufmachen.** Was in der
   Muster-13-Maske eingetragen worden war, war danach nur noch auf dem Papier.

### 1 · `1-3` neben dem Namen

Die Nummer `<Patientennummer>-<Verordnungsnummer>` liegt seit dem 15./16.08. in
der Datenbank (`leads.patientennummer`, `verordnungsnummer`, `belegnummer` in
beiden Verordnungstöpfen), stand aber — so der offene Punkt von gestern —
„noch nirgends in der Oberfläche". Jetzt steht sie an vier Stellen:

- Verordnungsliste, als Rosette neben dem Patientennamen
- Podologie-Abrechnung, dieselbe Rosette neben `patient_name`
- Patienten → Tabellenansicht, Spalte „Rezept-Nr" — dort stand bisher
  `rx.id.slice(0,8)`, der Anfang der UUID, auf keinem Papier zu finden
- Detailansicht, oben neben dem Namen

Zusammensetzung und Vorrangregel stehen in `module/belegnummer.js`, damit sie
nicht ein zweites Mal irgendwo entsteht. Die Regel ist: **die gespeicherte
`belegnummer` gewinnt immer.** Sie wird bei der DTA-Erzeugung einmal
geschrieben und danach nie geändert (Anlage 1 TP5 V21 Kap. 7.3) — würde die
Anzeige sie überschreiben, suchte man bei einer Kassenrückmeldung den falschen
Beleg. Erst wenn sie leer ist (jede noch nicht abgerechnete Verordnung), wird
aus den beiden Teilen zusammengesetzt. Fehlt die Patientennummer, erscheint
`#3` statt `3`, damit niemand die Verordnungsnummer für eine Patientennummer
hält. `module/rechnung-verordnung.js` benutzt jetzt dieselbe Funktion; die
zweite, gleichlautende Umsetzung dort ist entfallen.
6 Tests in `module/belegnummer.test.js`, darunter `1-1` (die Werte 0 und 1 sind
hier die gefährlichen) und Markup in der Belegnummer.

### 2 · Klick auf die Zeile öffnet die Verordnung

Es gab bereits eine Detailansicht: `openPatRxDetail()`, erreichbar **nur** über
die Qikbee-Tabelle unter „Patienten", mit zehn Feldern. Diagnosegruppe,
Leitsymptomatik, Einheiten, Frequenz, LANR/BSNR, Zuzahlung, Therapiebericht,
Hinweise — alles, was die Maske sonst noch speichert — fehlte darin.

Statt einer zweiten Ansicht wurde diese eine nach `module/verordnung-detail.js`
gezogen und auf den vollen Feldbestand gebracht (Verordnung · Diagnose ·
Heilmittel · Arzt · Kostenträger und Zuzahlung · Therapiebericht · Hinweise ·
Erfassung, dazu die Sitzungen „x von y erledigt" und die Merkmale
dringlich/Hausbesuch/Blanko/LHB-BVB als Rosetten). `openPatRxDetail()` ruft
jetzt dieselbe Funktion auf — eine Feldliste, nicht zwei; ein neu aufgenommenes
Feld kann nicht mehr an einer der beiden Stellen fehlen.

**Bewusst nur lesen, kein Formular.** Ein Teil der Felder ist nach der
Abrechnung eingefroren, und die Sitzungszeilen hängen an `anzahl_einheiten`
(`module/sitzung-abgleich.js`). Ein Bearbeiten-Pfad muss beides mitdenken und
ist eigene Arbeit, kein Nebeneffekt der Ansicht.

### `dashboard.js` ist dabei geschrumpft

25.555 → 25.444 Zeilen. Die Liste liegt jetzt in `module/verordnung-liste.js`,
die Detailansicht in `module/verordnung-detail.js`; in `dashboard.js` stehen
nur noch zwei weiterleitende Rümpfe. Das Grössen-Tor senkt die Basis beim
nächsten Commit selbsttätig.

### Geprüft

`npm test` 117 grün (111 + 6 neue) · `typecheck` sauber · `node --check` auf
allen berührten Dateien · Funktionskarte aufgefrischt (1663 Funktionen).

### Offen

- **Im Browser nicht nachgeprüft.** Der Arbeitsbaum enthält weiterhin fremde
  Arbeit, deshalb kein Commit und kein Deploy.
- Podologie ist nur mit der Rosette bedient; die Detailansicht dort bleibt der
  bestehende „Bearbeiten"-Knopf, weil `verordnungen` ein eigener Topf ist.
- `rezeptvorderseite.template.js` (Urbeleg) trägt die Nummer weiterhin nicht —
  § 4 Abs. 1 verlangt sie dort. Bleibt offen.
- Verordnung **bearbeiten** gibt es für `prescriptions` nach wie vor nicht.

---

## Termin-Seitenbereich: zwei Wege zur selben Akte, eine Liste zu viel

Zwei Befunde von Kemal, beide im Seitenbereich, den man nach dem Klick auf einen
Termin rechts aufklappt.

### 1 · Zwei Knöpfe, ein Ziel

In der Patientenkarte standen nebeneinander „Patientendaten →" und „Verlauf
(gekommen / nicht gekommen) →". Der Verdacht war, dass beide dasselbe tun. Er
stimmte, und zwar wörtlich: `setzePatientenKarte()` hängte an beide Knöpfe
**dieselbe** Funktion (`oeffneAkte`, `module/termin-aktionen.js`). Die Akte
öffnet ohnehin auf dem Reiter „Verlauf" — deshalb war der zweite Knopf nie ein
zweites Angebot, sondern eine Frage: *worin unterscheiden die sich?*

„Verlauf" ist entfallen, „Patientendaten" bleibt. Die No-Show-Warnung steht
weiter an derselben Stelle; ihre Begründung im Quelltext verwies auf den
gelöschten Knopf und ist mitgezogen.

### 2 · Der Block „Sitzungsübersicht" ist entfallen

Er zeigte eine Tabelle aller Sitzungen der laufenden Verordnung — und lud sie
dafür mit einer **eigenen** Abfrage auf `prescription_sessions` ein zweites Mal
nach. Dieselbe Liste führt der Block „Aktive Verordnung" weiter unten im selben
Panel bereits, und zwar die bedienbare (Einheiten vergeben, auf den Kalender
ziehen). Zwei Listen derselben Sitzungen im selben Bild widersprechen sich
früher oder später — gezeigt hat sich das schon am Aufbau: der Tabellenkopf
hatte fünf Spalten, die Zeilen schrieben sieben `<td>`.

Beim Löschen fiel ein zweiter Rest auf: der Rückgängig-Knopf (`↩`, „Sitzung als
nicht erledigt markieren") hing an `status === 'done'`. Diese Spalte trägt
`completed` / `no_show` / `planned` — der Knopf war seit jeher unsichtbar. Es
geht also keine Bedienung verloren, auch keine versteckte.

### `dashboard.js` ist weiter geschrumpft

25.555 → 25.399 Zeilen (156 weniger; davon 111 aus der Verordnungsansicht oben,
45 aus dieser Arbeit). Das Grössen-Tor senkt die Basis beim Commit selbsttätig.

### Geprüft

`npm test` 117 grün · `typecheck` sauber · `node --check` auf `dashboard.js` und
`module/termin-aktionen.js` · `bkSitzung*`, `bkVerlaufBtn` und `sess-undo`
haben repoweit **null** verbliebene Fundstellen · Funktionskarte aufgefrischt
(1663 Funktionen).

### Zur „zwei Sitzungen"-Notiz weiter oben

Die beiden Sitzungen haben sich gegenseitig blockiert: jede sah fremde Arbeit im
Baum und liess deshalb den Commit aus. Die Arbeit der anderen Sitzung ist
fertig, verdrahtet und geprüft (siehe ihren Eintrag), diese hier ebenfalls —
deshalb geht beides zusammen in einen Commit.

### Offen

- **Im Browser nicht nachgeprüft.** Nach dem Deploy gehört der Seitenbereich
  einmal angesehen: Patientenkarte mit einem Knopf, kein Loch, wo die
  Sitzungstabelle stand.
