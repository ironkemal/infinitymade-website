# 2026-09-14

## Woran gearbeitet wurde
Ops-Karte a8186cb8 (Podoloji, hoch) — "Patient nicht erschienen" ließ die
Einheit weiter als vergeben gelten: Verordnung zeigte 3/6 obwohl nur 2
Einheiten erbracht waren, die ausgefallene Einheit tauchte nie wieder in der
Liste der offenen Einheiten auf, Übersicht/Patientenakte zeigten keine Spur.
Kemal bat darum, mit dieser Karte aus der Ops-Bestandsaufnahme zu beginnen.

## Erledigt
- **fonksiyon-ustasi** hat den Ist-Zustand kartiert (dashboard.js:4514-4552,
  zweiter unvollständiger Weg in kalender.js:543, Verordnung-Zähler-Logik,
  Übersicht-Farbgebung, Patientenakte-Rendering, frequenz-pruefung.js).
- Zwei Nutzerentscheidungen eingeholt (AskUserQuestion): (1) freigegebene
  `prescription_sessions`-Zeile kehrt auf `status='planned'` zurück, die
  no_show-Spur bleibt dauerhaft am `bookings`-Datensatz; (2) der zweite,
  unvollständige "👻 Nicht erschienen"-Knopf in kalender.js wird in dieser
  Karte mit vereinheitlicht statt separat liegen zu bleiben.
- **builder** (Hintergrund-Agent) hat umgesetzt und committet (`2bc7ead`):
  - `module/termin-nicht-erschienen.js` (neu) — einziger Schreibweg,
    `markiereNichtErschienen()` + `rebindeNoShowSitzungen()`, ruft erstmals
    `pruefeVerordnungsfortschritt()` nach einem no_show auf.
  - `dashboard.js` (4514 → 24 Zeilen, Logik ausgelagert), `kalender.js` (zweiter
    Knopf an dasselbe Modul gebunden, bekommt jetzt Grund + Ausfallrechnung-
    Angebot, die vorher fehlten).
  - `module/booking-status-korrektur.js` — Rückweg ("doch gekommen") bindet
    über die neue Rückfahrkarte zurück statt über `booking_id`.
  - `module/kalender-farben.js` — no_show färbt Fläche+Rand rot (Tag/Woche/Monat).
  - `module/patientenkarte.js` — no_show-Zeile rot + Grund im Text.
  - `module/frequenz-pruefung.js:256` — no_show zählt nicht mehr als Nachbar-
    behandlung (Blockierung war schon vorher korrekt, nicht blockierend).
  - Migration `0016_no_show_session_links.sql` (neue Spalte
    `bookings.no_show_session_links`, additiv, Trigger #192 nicht angefasst).
  - Tests: 841 Frontend + 231 Backend grün, `npm run probe` 88/88 Module,
    keine Konsolenfehler.
- **db-ustasi** hat Migration 0016 vor dem Anwenden gegengelesen: additive
  Spalte statt Trigger-Umbau bestätigt richtig, Backfill-Umfang vorab
  berechnet (2 bookings / 2 Sitzungszeilen), Guard-Trigger geprüft (lässt
  `booking_id=NULL` durch), Entscheidung **UYGULA**.
- Commit `2bc7ead` gepusht (main), Migration 0016 per MCP live angewendet
  (Backfill-Zahlen bestätigt: 2/2, wie vorhergesagt), Migrationsdatei-
  Kommentar + `db/SCHEMA.sql`-Kopf nachgezogen, Commit `689f044` gepusht
  (`SKIP_MIGRATION_GATE=1` — rein dokumentarische Nachpflege, keine neue
  DDL, von db-ustasi bereits freigegeben).
- `node tools/funktionskarte.mjs` lokal gelaufen (nicht committet — Diff war
  durch parallele Session in `dashboard.js`/`module/buchungskonten.js` etc.
  verunreinigt, siehe unten).
- **fonksiyon-ustasi** nach Schreiben benachrichtigt (Pflichtschritt): neue
  Funktionen registriert, zwei Kartierungslücken für später notiert (Aufruf-
  kante über Default-Parameter-Injection wird von `funktionskarte.mjs` nicht
  erkannt; ein Kommentar in `booking-status-korrektur.js:89` wurde fälschlich
  als echter Funktionsaufruf gelesen).
- **Zwei veraltete Ops-Karten geschlossen** (bei der Bestandsaufnahme vorher
  entdeckt, nicht Teil dieser Karte, aber verifiziert erledigt): "Preise
  automatisch aktuell halten" (preise_pruefen.mjs/preise_autoupdate.mjs/GH
  Action bereits am 04.09. deployed) und "Migration kostentraeger_typ" (bereits
  am 03.09. angewendet, Schema-Dump am 06.09. aufgefrischt).

## Offen / blockiert
- **Live-Klick-Test noch nicht bestätigt** — an `canli-test` delegiert, Ergebnis
  steht bei Abschluss dieses Eintrags noch aus.
- Ein Akzeptanzkriterium der Karte ist bewusst NICHT umgesetzt: die
  freigegebene Einheit landet zwar wieder in der "unvergeben"-Liste, aber ohne
  rote Markierung dort — `booking_id` ist NULL, die Zeile hat keinen visuellen
  Bezug mehr zum gescheiterten Termin. Als eigene, kleine Folgekarte notiert
  (noch nicht in Ops angelegt).
- `funktionen/INDEX.json`/`.md` lokal neu generiert, aber NICHT committet —
  Diff enthielt unfertige Änderungen einer parallelen Session
  (`dashboard.js`, `module/buchungskonten.js`, `api-backend/server.js`,
  `api-backend/ai/azureClient.js`). Muss nachgeholt werden, sobald diese
  Session ihren Stand committet hat.
- `api-backend/db/migrations/README.md` Regel 6: Wortlaut deckt "reines
  Spalte-hinzufügen ändert keine Zähler" nicht explizit ab (db-ustasi-Hinweis,
  nicht blockierend, Klarstellung steht noch aus).

## Was noch getan werden muss
- Ops-Karte a8186cb8 in `ops_todos` als erledigt markieren, sobald der
  Live-Test grün ist.
- Kleine Folgekarte anlegen: rote Markierung in der "unvergeben"-Liste für
  no_show-Herkunft.
- `funktionen/INDEX.json`/`.md` nachziehen, sobald die parallele Session ihren
  Stand committet hat (dann sauberer Diff möglich).

## Commits
- `2bc7ead` — fix(termine): "Patient nicht erschienen" gibt die Einheit wieder frei (Ops a8186cb8)
- `689f044` — docs(db): Migration 0016 canlıya uygulandı — dosya notu + şema dökümü tazelendi
