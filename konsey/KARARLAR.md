# Konsey Karar Kaydı

> `/konsey` toplantılarından çıkan kararların dizini. Tam tutanaklar `konsey/tutanak/` altında.
>
> **Konsey toplanmadan önce bu dosyaya bakılır.** Buradaki kapatılmış bir karar, yeni bir olgu
> (mevzuat değişikliği, mimari değişiklik, eşik aşımı, müşteri talebi) olmadan yeniden
> tartışmaya açılmaz.

| Tarih | Karar | Oturanlar | Ödün | Tutanak |
|---|---|---|---|---|
| 2026-08-28 | **Podoloji Verordnung listesi PRAXISWEIT — bu bir karar, unutulmuş filtre değil. Migration ERTELENDİ.** Ops kartı "`bizScope` uygula" diyordu, **uygulanamaz:** `bizScope` `business_id` kolonuna filtre atar, `verordnungen`de o kolon **yok** (`podologie_behandlungen`, `prescription_sessions`, `pat_fussbefund` de yok; physio havuzu `prescriptions` **var**). Standort podolojide yalnız hastadan türetilebiliyor (`lead_id → leads.business_id`). Seçilen yol **C**: varsayılan praxis geneli — gerekçesi kodda yazılı (`module/standort-zuschnitt.js`): **Muster 13'te Standort alanı yok, kasaya giden IK praxis'in IK'sı, Standort'a ait olan Verordnung değil ona bağlanan seans.** Filtre yalnız inhaber `data_sharing_settings.patients`'ı açıkça "ayrı" yaptıysa VE >1 Standort varsa çalışır; `lead_id` NULL ve `business_id` NULL satırlar **her Standort'ta görünür** + "Praxisweit" rozeti. Belirleyici: `podoloji` (Verordnung praxis'e aittir) + `gkv-302` (listeden düşen Verordnung 28/14-gün Frist uyarısını da düşürür → Frist kaçar, GoBD Vollständigkeit bozulur) birbirinden bağımsız aynı sonuca vardı → **eksik göstermek fazla göstermekten pahalı**, B'nin "düşürme" hâli beş üyenin hiçbirince kabul edilmedi. Kararı çivileyen karşı-olgu (`fonksiyon-ustasi`): **`abrechnung` tablosunda `business_id` kolonu VAR ve tek bir sorguda bile filtre değil** — yani A'yı yapmak tek başına sorunu çözmez, asıl iş çağrı yerlerinde (owner-geniş 12 yol). Anlaşmazlık: `gkv-302`+`legal-de` "A şimdi", `deger-mi`+`podoloji` "ertele, küçüğünü yap" → chairman **erteleyenler lehine** (A = 2–4 gün + Katman 4, bugün semptom yok, A tek başına yetmiyor). Chairman ikinci kez çoğunluğa karşı: `legal-de`'nin "ikinci Standort onboarding'ini teknik olarak kapat" şartı **uygulanmadı** — o kapı uygulanmayan bir vaadi telafi içindi, vaat artık uygulanıyor, satılan Enterprise özelliğini kapatmak orantısız; diğer iki şartı (TOM daraltma + süreli Risikoakzeptanz) uygulandı. **A'nın tetikleyicisi:** ilk çok-Standort'lu podoloji müşterisi imzaladığında, onboarding'inden ÖNCE, **tek parça** (dört tablo + 12 çağrı yeri + IK/LEGS ekseni). Kör noktalar: **`gkv-302` — owner başına tek IK varsayımı sabitlenmiş** (`abrechnung.routes.js:409-427`, `onConflict:'owner_id'`); ikinci Standort başka Bundesland'daysa Tarifkennzeichen/LEGS yanlış üretilir (Anlage 3 TP5 V21 §8.1.5.2) → sessiz yanlış tutar veya Absetzung, **bu karttan büyük ayrı risk** · `bizScope` NULL-toleranslıdır, 12.08 "Podologie Nord" kaybı ondan değil hard `.eq`'ten geldi · `abrechnungsstatus.js:164` owner-geniş okuyor ama görünür sızıntı üretmiyor. ⚠️ **`muhalif` oturamadı** (oturum limiti 429) — tutanakta "Eksik ses" başlığı altında ne sorulamadığı yazılı | gkv-302, legal-de, podoloji, deger-mi, fonksiyon-ustasi (muhalif oturamadı) | Standort ekseni türetilmiş kalıyor (hasta taşınırsa Verordnung'ları onunla taşınır); `podologie_behandlungen` hâlâ Standort'suz, "hangi şubede yapıldı" cevaplanamıyor; zuschnitt istemci tarafında, RLS engeli değil; `arzt-register.js:463` Zuweisung sayıları Standort'lar arası toplam | [2026-08-28-podologie-standort-zuschnitt](tutanak/2026-08-28-podologie-standort-zuschnitt.md) |
| 2026-08-28 | **`business_services` kaldırılacak — `services` tek hizmet tablosudur (Seçenek B), ama iki adımda.** `fonksiyon-ustasi` tartışmayı olguyla bitirdi: tabloyu **okuyan yok** (tüm okuyucular `services`'te; tek okuma `onboarding.js`'in kendi yazdığını prefill için geri okuması) ve **hiçbir FK** `business_services(id)`'ye bakmıyor — `bookings`/`booking_requests`/`warteliste`/`employee_services` hepsi `services(id)`'ye. Kaldırmak hiçbir kaydı kırmıyor. **Bugün yapılan (DB gerektirmeyen kısım):** iki `service_role` ayna yazımı silindi (`api/stripe/webhook.js:285`, `api-backend/server.js:2840`) → `business_id` sütunu **tek anlama indi** (yalnız onboarding, hep `auth.uid()`; önceden webhook gerçek `businesses.id` yazıyordu ve RLS `auth.uid() = business_id` olduğu için o satırlar praxis'e **görünmüyordu**) · `api/dsgvo.js` filtresi `owner_id`→`business_id` (kolon yoktu, Art. 15 Auskunft bu tabloyu sessizce atlıyordu) · `onboarding.js` ayna yazımı **bloklayıcı olmaktan çıktı**. **Kör nokta — canlıda doğrulanmadı:** `muhalif` buldu, `fonksiyon-ustasi` doğruladı — onboarding payload'ı `code` alanı yazıyor ama o kolon `business_services`'te yok (PGRST204) ve FK `business_id → businesses(id)` iken `userId` yazılıyor (23503); ikisi de `throw` ediyordu, yani Dienstleistungen adımı kullanıcıyı kilitliyor olabilirdi. Müdahale bu yüzden "düzelt" değil "bloklama" oldu: hangi varsayım doğru çıkarsa çıksın kullanıcı ilerliyor. **MCP/DB gelince:** `code`+FK doğrulanır · onboarding tek tabloya iner · DROP + döküm + harita, tek migration. Tetikleyici: podoloji uçtan uca test PASS. Zamanlamada `muhalif` (tek commit) ile `deger-mi` (bugün küçük parça) ayrıldı, **chairman `deger-mi` lehine** — DROP için DB yok, ve DSGVO düzeltmesini o commit'e kadar bekletmek yükümlülüğü uzatırdı. | muhalif, deger-mi, fonksiyon-ustasi (⚠️ `legal-de` oturum limiti nedeniyle oturamadı) | `is_active`/`display_order`/`follow_up_days` yalnız bu tabloda — kimse okumuyor ama onboarding'deki "aktif" kutucuğu tablo gidince görünür biçimde işlevsiz kalır (ürün kararı, ertelendi); `onboarding.js:581` eşleşmesi isme düşer; tablo bugün hâlâ duruyor — yarım durum, kayıt bu yüzden burada | [2026-08-28-business-services](tutanak/2026-08-28-business-services.md) |
| 2026-08-27 | **Beta müşteri adları public depodan tamamen kaldırılacak — çalışma ağacı + git geçmişi + commit mesajları.** Sıra bölünmez: (1) rumuz kuralı + pre-commit isim kapısı (kanamayı durdurur), (2) HEAD temizliği 34 dosyada ad→rumuz — **alıntı ve tarih aynen korunur**, (3) `git filter-repo --replace-text` + 4 kirli commit mesajı + force-push + GitHub cache purge. Değişecek dizgiler **elle listelenir, kör `sed` YASAK** (`demo-dashboard.html` uydurma hastası "Stefan W." 21 satır ve `plz-orte.json` yer adı hariç tutulur). **Depoyu private yapmak (C) bu turda YAPILMAZ** — Vercel kırılması 2026-06-10 çözülmedi, on-prem penceresine notlandı. Paralel: iki müşteriden yazılı referans onayı istenir (onay gelirse konu kökten kapanır + referans hakkı kazanılır). Denetim makamına bildirim YOK (Art. 33(1) istisnası: sağlık verisi yok, 2 kişi), Art. 33(5) iç kaydı zorunlu. Belirleyici üç ölçüm: **fork=0/network=0** (erteleme gerekçesini çürüttü), isimler **hiçbir testte assertion/fixture değil** (HEAD temizliği risksiz), **4 commit mesajı kirli** (HEAD-only yetersiz). Kör nokta turunda muhalif + deger-mi görüş değiştirdi, oybirliği. Kör nokta: asıl mesele isim değil, **public depoya ne gittiğini denetleyen kapı yok** — 2026-08-05 denetiminde aynı boşluktan Fal AI anahtarı sızmıştı | legal-de, muhalif, deger-mi, fonksiyon-ustasi | Force-push iki klonu bozar (çalışma durur, taze klon); ~2-4 sa kurucu zamanı, bir günlük podoloji ince ayarı ertelenir; commit SHA-ları değişir; rewrite "hiç olmamış" yapmaz, "GitHub-da görünmez" yapar | [2026-08-27-klarnamen-public-repo](tutanak/2026-08-27-klarnamen-public-repo.md) |
| 2026-08-16 | **Podoloji slot süresi = Regelleistungszeit (78010→35, 78020→50) — Seçenek A, kapandı.** Beş üye itirazsız A'da birleşti. **C reddedildi:** Anlage 1a Teil 1 Nr. 4 Vor-/Nachbereitung'un Therapiezeit **içinde** yapılmasını yasaklıyor, C bu ayrımı takvimden siliyor. **B reddedildi:** `services.buffer_time` kolonu YOK, `booking.js:219/221/327` olmayan kolonu okuyor (daima 0), backend `buffer` parametresi hiç beslenmiyor — buffer bloğun içindeyse zaten A, dışındaysa `no_overlapping_bookings` korumasının dışı. **Ama sorulan soru yanlış soruymuş:** `fonksiyon-ustasi` kapasite kaybının süreden değil **ızgara çözünürlüğünden** geldiğini gösterdi — `step=30` her yerde sabit (`server.js:503/749-760/775`, beş çağıranın hiçbiri başka değer göndermiyor), 35 dk randevu 09:00–09:35 olunca 09:30 adayı eleniyor ve sıradaki teklif 10:00 → **her hastada 25 dk boşa gidiyor.** Bu olgu kör nokta turunda üç üyenin de pozisyonunu değiştirdi (`muhalif` kendi preset önerisini geri çekti, `deger-mi` "önce sor"u bıraktı — *"hiç-yapmama seçeneğini bedava sanmıştım"*, `podoloji` `step=15` istedi). Chairman `muhalif`'in "preset'ler hiçbir fark üretmez" **sonucunu düzeltti** — `step=30` altında 25 dk ile 35 dk arasında gerçek fark var (25 dk 09:30 adayını yaşatır), kazanç sadece ızgara düzeltilmeden küçük kalıyor → preset'ler iptal değil, P1'in arkasına alındı. **Sıra: P1** `step` 30→15, beş çağıran hizalanır (K2) · **P2** ölü `buffer_time` okuması silinir, kolon EKLENMEZ (K0) · **P3 koşullu** `price_config.durations` GKV podolojide açılır (mekanizma ZATEN VAR: radio `dashboard.js:5802-5840` + backend `server.js:1147-1155`, GKV formu `price_config:null` ile kapatıyor `dashboard.js:10570`), preset "Solo 35/50 / mit Assistenz 25/40", kaynak tek soru *"Wird die Vor-/Nachbereitung von einer Assistenz übernommen?"* — önce `10149`/`10354` üzerine-yazması susturulur (K2). **Kalıcı kural (`gkv-302` şartı):** slot uzunluğu 78010↔78020 seçimini ASLA belirlemez — bugün zaten sağlanıyor (`therapiezeit` yalnız etiket, seçim elle checkbox `dashboard.js:24628`). **Yapılmayacak:** 20+15 taralı görsel (podoloji: gürültü), serbest sayı girişi (destek yükü). Anlaşmazlık: preset zamanlaması — `deger-mi` "şimdi", `muhalif` "hiç dokunma", chairman `muhalif` lehine (iki değişiklik aynı anda = hangisi işe yaradı ölçülemez). Kör noktalar: `dashboard.js:10149`/`10354` süreyi katalogdan yeniden yazıyor (kilit kalksa ayar sessizce ezilirdi) · takvim tıklama hedefleri 30 dk sabit (`2637-2638`, `2679-2681`, `2820`), `step` düzelse bile manuel randevu 30'luk kalır · FAK Nr. 25 teyit: salt Nagel-/Hornhautbearbeitung her zaman 78010+78030, kodda doğru | gkv-302, podoloji, muhalif, deger-mi, fonksiyon-ustasi, dış göz | Podolog bu turda hâlâ slot uzunluğunu ayarlayamıyor (`locked:true` kalıyor, "ilk gün şikayet" uyarısı bilinçli kabul); `step=15` slot listesini iki katına çıkarır; 35/50 blok 15'lik ızgarada hâlâ 10 dk artık bırakır, tam verim ancak P3 ile; beta sorusu artık kararı bloklamıyor ama P3'ü geciktirebilir | [2026-08-16-podoloji-slot-suresi](tutanak/2026-08-16-podoloji-slot-suresi.md) |
| 2026-08-14 | **Yeni kilitli hasta modu YAPILMAYACAK — kiosk ZATEN VAR, sertleştirilecek ve onam/imza onun içine bindirilecek.** `fonksiyon-ustasi` konseyi ters yüz etti: `dashboard.html:1943` + `dashboard.js:22783-22960` (8 fonksiyon) + `profiles.tablet_kiosk_pin` canlıda; diğer dört üye "yapalım mı"yı bunu bilmeden tartıştı. **Seçenek C (oturumsuz token'lı ayrı sayfa) RAFA** — kör nokta turunda `legal-de` ve `muhalif` C'yi bıraktı (ikinci kod yolu + ikinci oturum modeli + on-prem'de ikinci dağıtım hedefi), `deger-mi` "kilidi çıkar"ı geri aldı. Belirleyici tespit: **kilit güvenlik sınırı değil kaza önleyicidir** (terapist oturumu cihazda açık kalıyor); hukuken zorunlu olan kilit değil **imza + sürümlü onam metni**. İki paket: **P1 güvenlik borcu (önce, P2'yi beklemez)** — `handleKioskPinForgot` (`22940`) PIN'i doğrulamadan çıkarıp `tablet_kiosk_pin`'i `null`'a çekiyor → `signOut()`; `handleKioskPinConfirm` (`22927`) `!storedPin` kısa devresi = PIN yoksa her giriş kabul; PIN düz metin → hash + doğrulama `api-backend/server.js` (G8 temiz); `fullscreenchange` guard; `bookings-realtime` kiosk'ta unsubscribe; `appendChild` `try/finally`. **P2 onam+imza** — tek akış **iki ekran/iki imza** (§630d Behandlungsvertrag+Ausfallgebühr / Art. 7 Datenschutz; birleştirme Koppelungsverbot), yeni **`patient_consents`** tablosu (`consent_log` GENİŞLETİLMEZ — o praxis sahibine ait, B2B), `text_version`+`text_sha256` değişmez, imza raster PNG → mevcut `patient-documents` bucket, **IP YOK**, 10 yıl (§630f), hastaya PDF kopya + geriye dönük arama v1'de. Kod `module/patienten-einwilligung.js` (dashboard.js büyümez). legal-de 🔧 KOŞULLU (9 bağlayıcı şart, ⛔ yok): einfache elektronische Signatur YETER, QES orantısız; bugünkü kiosk Art. 32 TOM yetersizliği ama **Art. 33 bildirimi gerekmez** (fiilî erişim kanıtı yok — loglama da yok, asıl sorun bu); VVT'ye yeni işleme faaliyeti + TOM güncellemesi zorunlu. Anlaşmazlık: `podoloji` düzeltmeden sonra C'yi destekledi (chairman rafa kaldırdı — "hasta için fark yok" nötr gerekçe, "ikinci kod yolu" negatif gerekçeyi yenmez) ve tek imza istedi (legal-de'nin ayrı-imza şartı kazandı, "tek akış/iki ekran/tek Weiter" uzlaşması). Kör noktalar: **kiosk bugün hastaya Anamnese formunu uzatıyor** ama podolojide anamnez podologla birlikte doldurulur (diyabet/Marcumar soruları hasta tarafından yanlış anlaşılıyor) → bugünkü içerik muhtemelen yanlış · `bookings-realtime` toast'ı başka hastanın randevusunu hastanın gözü önüne getiriyor · ESC/F11 fullscreen'i kırıyor · kiosk metinleri i18n sözlüğünde **hiç yok** (üç dil kuralı bugün ihlal) | legal-de, muhalif, deger-mi, podoloji, fonksiyon-ustasi | Kilit güvenlik sınırı olmuyor ve olmayacak (oturum cihazda açık) — kartın "diğer hasta verisi erişilemez" maddesi kaza-önleyici seviyeye daraltıldı; C'nin token altyapısı ödenmiyor; podoloğa 1 yerine 2 imza; ekran görüntüsü hiç engellenmiyor; mevcut PIN'ler migrasyonda geçersiz olacak | [2026-08-14-patienten-uebergabe-einwilligung](tutanak/2026-08-14-patienten-uebergabe-einwilligung.md) |
| 2026-08-13 | **React'a GEÇİLMEYECEK, 27k satır YENİDEN YAZILMAYACAK — eksik katmanlar vanilla kalınarak eklenir.** Dört acıdan üçü (çok yerden yazma, tip hataları, dosya kavranamaması) framework'le ilgisiz; React'ta da aynen yaşanır. Sıra: **S1** `CLAUDE.md` kuralı "yeni kod yeni dosyaya" + **pre-commit kapısı** (`dashboard.js` satır sayısı artarsa commit reddi — kural değil kapı) · **S2** ~50 satır olay/sinyal katmanı (projede hiç yok; "kaydettim ama ekran eski" acısını kapatan tek madde) · **S3** CDN yerelleştirme, `?bundle&target=es2020`, **8 yer** + FullCalendar + airgap testi — düz indirme transitive import yüzünden kırılır, 0,5 değil ~1,5 gün · **S4** `jsconfig.json`+`checkJs`, JSDoc önce §302 tip sözlüğü (Positionsnummer/Abrechnungscode/Tarifkennzeichen **string**, IK 9 hane, cent-mi-euro, `YYYYMMDD`). **S5 (veri katmanı yazma yolları) ERTELENDİ** — ilk tur sadece okuma+olay, yazanlar dokunuldukça göç; ilk hedef tablo `profiles` değil **`verordnungen`+`podologie_behandlungen`** (gelir orada, `profiles` hatası görünür/dosya reddi, seans hatası sessiz). Chairman'ın ilk önerisi iki noktada bozuldu: K0 ölçümü ve K3'ün riski. Anlaşmazlık: S3 zamanlaması — `deger-mi` ertele dedi, `legal-de` Art. 32 (tedarik zinciri: CDN ele geçirilirse tarayıcıda keyfi kod → tüm hasta verisi) gerekçesiyle bu haftaya çekti, **chairman legal-de lehine karar verdi** (deger-mi'ye Art. 32 çerçevesi verilmemişti). Kör noktalar: **`lib/supabase.js` ölü** — veri katmanı bir kez yazılıp terk edilmiş, S5'te üçüncü kopya yazılmaz · `profiles` yazma yollarında **4 farklı yetki bağlamı** var, "hepsi aynı" varsayımı yanlıştı · `dashboard.js` tematik ayrıma direniyor (447 fn/12.5k satır sınıflanamıyor), bitişik **tek** küme podoloji 24030–25696 → ilk kuşatma hedefi · ⚠️ **`verordnungen` GoBD değişmezlik trigger'ı YOK** (sadece `belegliste` korunuyor) → `abgerechnet` sonrası UPDATE DTA ile DB'yi ayrıştırır, ayrı iş | muhalif, deger-mi, fonksiyon-ustasi, legal-de, gkv-302 | Melez kod tabanı kabul (iki desen yan yana, geri göç yasak); eski 27k satır bu turda düzelmiyor; `profiles`'ın 10 yazma yolu birleşmiyor; frontend veri katmanı kurulsa bile yazımların ~%25'ini kapsar (onboarding.js 8 + kalender.js 1 + backend service_role 52 dışarıda); işe alımda "React" denemiyor | [2026-08-13-frontend-mimari-katman](tutanak/2026-08-13-frontend-mimari-katman.md) |
| 2026-08-12 | **Belge zinciri üç kovaya bölündü, bu turda sadece Kova 1.** Sahte sabit IBAN (`DE89 … Musterbank`) iki çağrı yerinden silinir, banka satırı `profiles.iban/bic/bank_name`'den kurulur; **veri yoksa boş basılmaz, fatura yazdırma bloklanır** (kullanıcının "boş göstersin" isteği bilinçli olarak aşıldı — muhalif: "sessiz-eksik IBAN belirtiyi kırmızıdan griye çevirir"). Profil select'ine `steuernummer, ust_id, iban, bic, bank_name` eklenir; §14 Abs.4 Nr.2 için `steuernummer` **veya** `ust_id` yeter. `abrechnungscode:'22'` sabiti üç yerde sector helper'ına bağlanır (podoloji `'71'`). `rechnung_eigenanteil` hesabı yazılana kadar menüden **kaldırılır** (tam tutar basıyor = fazla talep). Rezeptvorderseite'ye "Kopie — nicht zur Vorlage bei der Krankenkasse" damgası. `rechnung_ausfall` seed'e eklenir. **Kova 2 (aynı hafta, ayrı iş):** fortlaufende Nummer — tenant başına atomik sayaç + tahsis edilen numaranın kaydedilmesi (GoBD). **Kova 3 (Ops):** podoloji salt-render yazdırma · Vorlagen WYSIWYG · Muster 13 hizalama **yapılmayacak**. **Logo:** mevcut Cropper modalı + `avatars` bucket yeniden kullanılır (G8 temiz), varsayılan serbest oran, daire preset. Kör nokta: §33 UStDV Kleinbetragsrechnung (≤250 €) muafiyeti — hard-block pratikte kimseyi durdurmuyor; `quittung_zuzahlung` aslında Quittung değil Rechnung basıyor | legal-de, gkv-302, podoloji, muhalif, deger-mi | Kullanıcının iki açık isteği aşıldı (boş IBAN → blok; dairesel kırpma → preset); Eigenanteil tipi geçici kayboluyor; podolog bu turda hâlâ belge basamıyor; Vorlagen paneli WYSIWYG değil; Kova 2'ye kadar numaralar fortlaufend değil | [2026-08-12-belge-vorlage-zinciri](tutanak/2026-08-12-belge-vorlage-zinciri.md) |
| 2026-08-10 | **Podoloji Privat/Selbstzahler: GKV alanları GİZLENMEZ, KATLANIR.** `rezeptart ≠ kassen` iken sadece **Abrechnungs** alanları (KK, Diagnosegruppe, ICD-10, Zuzahlung-Befreiung) varsayılan kapalı `GKV-Angaben` bölümüne girer; DG zorunluluğu `rezeptart='kassen'`e bağlanır, boşsa **NULL** yazılır (FK var). **Wagner/Fußbefund klinik alandır, her zaman görünür.** Opsiyonel serbest metin `behandlungsanlass` (varsayılan "Podologische Komplexbehandlung"). Frontend yetmez: `abrechnung.routes.js`'e `rezeptart NOT IN ('privat','selbstzahler','bg')` guard'ı + DB `CHECK`. `insurance_type` ↔ `rezeptart` **senkron edilmez**, tek yönlü ön seçim + uyarı. PKV ≠ Selbstzahler ama ayrım **fatura katmanında** yaşar. Rechnung köprüsü **ertelendi (Faz 3)**. Kör nokta: bugün sunucuda hiç koruma yok — `abrechnung.routes.js`'te `rezeptart` geçmiyor | gkv-302, legal-de, podoloji, muhalif, deger-mi | Müşteri "hiç görünmesin" dedi, "kapalı ama 1 tıkla açılır" veriliyor; §1.6'nın "doğrudan Rechnung" maddesi bu turda kapanmıyor; BG yarım kova (Unfalltag/Az. yok); `insurance_type`'a `selbstzahler` Faz 2'ye kaldı | [2026-08-10-privat-selbstzahler-akisi](tutanak/2026-08-10-privat-selbstzahler-akisi.md) |
| 2026-08-06 | **Eş-kurucu Aşama-1: iki belge, ikisi de ISLAK İMZA, müşteri görüşmesinden ÖNCE.** (1) `Vertraulichkeits- und Verpflichtungserklärung` — NDA §23 GeschGehG + §53 BDSG Datengeheimnis + §203 StGB, son ikisi kendi başlıklı ayrı paragraf, nachvertraglich/unbefristet. (2) `Mitarbeits- und Rechteübertragungsvereinbarung` — ausschließliche Nutzungsrechte (§31a UrhG dahil, 2026-08-05'e geri dönük) + **yazılı Gegenleistung** (karşılıksız devir §32 UrhG'ye açık) + **ausdrücklicher GbR-Ausschluss** + "nur Testdaten". Ayrıca bağlayıcı olmayan 1 sayfa Term Sheet. **Yeni olgu: InfinityMade = Einzelunternehmung** → bugün devredilecek pay YOK, GbR-Ausschluss ertelenemez. UG/noter/Steuerberater ertelendi | legal-de, muhalif, deger-mi | E-posta onayı yöntemi kullanılamaz (Schriftform §126 BGB); "anlaşma çıkmazsa … EUR" fallback rakamı bugün konuşulmak zorunda; UG yok → sınırlı sorumluluk yok (bilinçli kabul) | [2026-08-06-esk-kurucu-asama1-belgeleri](tutanak/2026-08-06-esk-kurucu-asama1-belgeleri.md) |
| 2026-08-05 | **Onboarding "Dienstleistungen" adımı kalır ama SADECE Selbstzahler adımı olur ve atlanabilir.** GKV'ye onboarding hiç dokunmaz (yazmaz/silmez/fiyat sormaz) — katalog `autoSeedGkvServices()` tekelinde. `onboarding.js:660-678` delete-then-insert daraltılır; eski KOBİ şablonları + GKV adlı kalemler temizlenir. Gerekçe: §302 zinciri `services`'ten Positionsnummer/Vergütung okumuyor, sorun duplicate "KG" satırından doğan **sessiz gelir kaybı**. Kör nokta: `bookings.service_id` FK'sı NO ACTION → randevusu olan owner adımı hiç tamamlayamıyor | gkv-302, podoloji, muhalif, deger-mi | Akış kısalmıyor (7 adım kalır); GKV fiyatları onboarding'de görünmeyecek; adım atlanınca `booking.html` ilk gün sadece GKV gösterir | [2026-08-05-onboarding-dienstleistungen](tutanak/2026-08-05-onboarding-dienstleistungen.md) |
| 2026-08-05 | **Blanko: Fachbereich tablosu YAPILMAYACAK.** `GUELTIG_WOCHEN=16` sabit kalır; `validate.js` yönlendirmesine guard eklenir, Podologie tek anlaşılır mesajla reddedilir. Gerekçe: Podologie §125a Blankoverordnung **yürürlükte değil** (KBV Praxiswissen 2026 s.1144; Diagnoseliste Bölüm 2 sadece Ergo 04/2024 + Physio 11/2024) | gkv-302, podoloji, muhalif, deger-mi, dış göz | Sözleşme geldiği gün kod hazır olmayacak; Fachbereich soyutlaması ertelendi | [2026-08-05-blanko-fachbereich](tutanak/2026-08-05-blanko-fachbereich.md) |

**Yeniden değerlendirme tetiği (frontend mimarisi):** "React'a geçilmeyecek" kararı şu ikisinden
biri gerçekleşirse yeniden açılır — **(a)** iki geliştirici `dashboard.js`'te düzenli merge
çakışması yaşamaya başlarsa, **(b)** `dashboard.js` 30.000 satırı aşarsa. S5 (veri katmanı yazma
yolları) tetiği ayrıdır: Melih'in ilk PR'ı **veya** `profiles` yazma çakışmasından doğan bir hata.

**Yeniden değerlendirme tetiği (eş-kurucu / şirket formu):** UG kuruluşu + noter + Steuerberater
şu eşiklerden **hangisi önce gelirse** aynı hafta açılır: 5 ödeyen müşteri · ~500 €/ay MRR ·
ilk on-prem/Enterprise sözleşmesi · dış yatırım görüşmesi. Gelir/gider paylaşımı ve
Kleingewerbe-fatura modeli **Ekim 2026'da ayrı konsey** konusudur.

**Yeniden değerlendirme tetiği (onboarding):** 3 aktif müşteri onboarding'i bitirdikten sonra bu
adımdan gelen ortalama private hizmet sayısı **0** ise adım tamamen kaldırılır (Seçenek A).

**Yeniden değerlendirme tetiği (Privat/Selbstzahler):** Faz 2 (`insurance_type`'a `selbstzahler`,
otomatik ön seçim) ancak Beta-1 Faz 1'i kullanmaya başladıktan **ve** verisinde Selbstzahler payı
%5'in üstünde çıktıktan sonra açılır. Faz 3 (Behandlung→Rechnung köprüsü, KDV seçimi) tetiği:
Faz 1 canlıda **ve** haftada ≥5 Privatrechnung.

**Yeniden değerlendirme tetiği (hasta onamı — Seçenek C):** Oturumsuz ayrı sayfa + tek kullanımlık
token şu üçünden **biri** gerçekleşirse açılır: (a) bir müşteri tableti hastayla gözetimsiz
bıraktığını bildirirse, (b) "tablet terapistin değil, hastanın kendi cihazı" gerçek bir talep
olarak gelirse, (c) 5. aktif praxis'e ulaşılırsa. **P1 (kiosk güvenlik borcu) bu tetiğe bağlı
değildir — koşulsuz ve önce yapılır.**

**Yeniden değerlendirme tetiği (podoloji slot süresi):** Slot = Regelleistungszeit kararı kapalıdır.
**P3 (süre preset'leri)** şu ikisinden biriyle açılır: (a) beta podologlarından biri "35/50 bana
uymuyor / kendim ayarlamak istiyorum" derse, (b) 3. aktif podoloji müşterisine ulaşılırsa.
**P1 (ızgara adımı) ve P2 (ölü `buffer_time`) bu tetiğe bağlı değildir — koşulsuz ve önce yapılır.**
**Kaynak bazlı planlama** (terapist ve oda ayrı bloke) ancak 2+ tedavi odalı bir praxis müşteriye
dönüşürse açılır.

**Yeniden değerlendirme tetiği (Blanko):** Podologie §125a Blankoverordnung sözleşmesi yayımlanır **ve**
en az 1 podoloji müşterisi Blanko rezept getirirse. O gün doğru şekil ayrı motor
(`blankoPodoRules.js`), Fachbereich tablosu değil.

---

## Konsey nasıl çalışır (özet)

`/konsey <soru>` → tarafsız çerçeveleme → ilgili uzmanlar paralel görüş → (çelişki varsa)
kör nokta turu → Chairman sentezi → **KARAR** → `builder` uygular.

**Daimî üyeler:** `muhalif`, `deger-mi`
**Konuya göre:** `legal-de`, `gkv-302`
**Dönüşümlü alan uzmanı:** `podoloji` (ileride `physio`, `logo`, `ergo`)
**Dışarıdan göz:** `agy`/Gemini — proje bağlamı olmadan, bedava

**Veto ağırlıkları:**
- 🔒 `legal-de` ve `gkv-302` ⛔ = **sert veto**, aşılamaz — ancak etrafından dolaşılır
- ⚠️ diğer üyelerin ⛔'ü = güçlü sinyal, bilinçli olarak aşılabilir

**Her üyenin mutlak kuralı:** çıkmaz sokak bırakmak yasak. "Olmaz" diyen, **ne olur** onu da
söyler — daraltılmış kapsam, manuel ikame, %20'lik sürüm, denk çözüm. Hedef her zaman
*"hem meşru hem ucuz hem işe yarar"* olan yol.

**Kapsam kayması freni:** Konsey sorulan soruyu cevaplar, yeni özellik önermez. Çıkan ekstra
fikirler tutanaktaki "Backlog" bölümüne düşer, karara karışmaz.

Detay: `.claude/skills/konsey/SKILL.md`
